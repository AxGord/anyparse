package anyparse.query.format.line;

@:build(anyparse.macro.Build.buildWriter(
	anyparse.query.format.line.MetaLineList,
	anyparse.query.format.line.MetaLineListWriteOptions
))
@:nullSafety(Strict)
class MetaLineListWriter {

	private function new() {}
}
