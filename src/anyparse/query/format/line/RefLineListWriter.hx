package anyparse.query.format.line;

@:build(anyparse.macro.Build.buildWriter(
	anyparse.query.format.line.RefLineList,
	anyparse.query.format.line.RefLineListWriteOptions
))
@:nullSafety(Strict)
class RefLineListWriter {

	private function new() {}
}
