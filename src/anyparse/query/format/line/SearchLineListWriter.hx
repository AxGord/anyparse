package anyparse.query.format.line;

@:build(anyparse.macro.Build.buildWriter(
	anyparse.query.format.line.SearchLineList,
	anyparse.query.format.line.SearchLineListWriteOptions
))
@:nullSafety(Strict)
class SearchLineListWriter {

	private function new() {}
}
