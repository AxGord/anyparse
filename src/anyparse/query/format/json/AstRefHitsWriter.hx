package anyparse.query.format.json;

@:build(anyparse.macro.Build.buildWriter(
	anyparse.query.format.json.AstRefHits,
	anyparse.query.format.json.AstRefHitsWriteOptions
))
@:nullSafety(Strict)
class AstRefHitsWriter {

	private function new() {}
}
