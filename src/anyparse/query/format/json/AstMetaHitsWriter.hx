package anyparse.query.format.json;

@:build(anyparse.macro.Build.buildWriter(
	anyparse.query.format.json.AstMetaHits,
	anyparse.query.format.json.AstMetaHitsWriteOptions
))
@:nullSafety(Strict)
class AstMetaHitsWriter {

	private function new() {}
}
