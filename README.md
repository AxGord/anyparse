# anyparse

**Structural code tooling for Haxe — query and transform source through typed ASTs instead of text.**

anyparse parses source into a typed AST and gives you two things on top of it:

- **`hxq` / `apq` — a structural alternative to grep.** Find code by its *shape*
  (every `return $x;`, every `a * b`, every declaration named `area`) instead of by
  regex over lines.
- **Scope-correct, AST-level editing.** Rename, inline, extract, reorder parameters,
  move types between files — each resolved against real scopes and re-parse-validated,
  so the rewrite is correct by construction and the rest of the file stays byte-for-byte
  intact. These are the kind of edits that automated tools — and AI coding assistants —
  can apply safely, because the tool understands the code rather than pattern-matching it.

Underneath sits a macro-based **platform**: describe a grammar declaratively as Haxe
types with metadata, and a `@:build` macro generates a specialized parser, writer and
AST-transform for it. The Haxe grammar that powers `hxq` is itself just one such
plugin; JSON and the `ar` binary archive format are others.

## Status

**Working and dogfooded** — `hxq`/`apq` are used to query and restyle this repo's own
source — not the "walking skeleton" earlier revisions described.

- `hxq`/`apq` ship a full structural-query CLI plus a set of scope-correct
  refactoring operations (listed below), all exercised by the test suite.
- **3179 tests / 7355 assertions / 0 failures** on the JS target.
- The Haxe writer round-trips against the AxGord/haxe-formatter corpus
  (945 fixtures: 755 byte-exact, 113 formatting deltas, 75 not-yet-parsed) with a hard
  no-regression invariant.

What is **not** done yet, stated plainly:

- The **query/refactoring CLI is Haxe-only** today. The grammar platform handles
  multiple formats (JSON / Haxe / `ar` binary / S-expr), but `hxq`/`apq` resolve only
  the Haxe grammar plugin — JSON-aware refactoring is not wired up.
- The Haxe grammar covers the language broadly but not exhaustively (59 corpus fixtures
  still fail to parse).
- The generic `buildTransform` walks plain typed ASTs; a *format-preserving* transform
  (carrying comments/spans through) is a later slice.

This is **Phase 3** of the roadmap (Haxe grammar + formatter). See
[`docs/roadmap.md`](docs/roadmap.md).

## Install / build

Requires [Haxe](https://haxe.org) 4.x and Node.js (for the JS-based CLI).

```sh
git clone https://github.com/AxGord/anyparse
cd anyparse
haxe bin/apq-js.hxml          # builds bin/apq.js — the query/refactor CLI
```

`bin/hxq` is a thin launcher that runs `apq` with the Haxe grammar preselected. Put it
on your `PATH` for a global `hxq`:

```sh
ln -s "$PWD/bin/hxq" ~/.local/bin/hxq
```

After that, `hxq <cmd> …` == `node bin/apq.js <cmd> --lang haxe …`. The launcher
auto-rebuilds when `src/` changes.

## Quickstart

Given `Sample.hx`:

```haxe
class Sample {
	public function area(width:Int, height:Int):Int {
		final total = width * height;
		return total;
	}
}
```

**Find by structure, not regex.** Match every multiplication, every `return`, or a
declaration by name:

```sh
$ hxq search '$a * $b' Sample.hx
Sample.hx:
  3:16: match (a=width, b=height)

$ hxq ast Sample.hx --select 'FnMember:area'
(FnMember
  area
  (Required width)
  (Required height)
  (Named Int)
  (BlockBody
    (FinalStmt total (Mul (IdentExpr width) (IdentExpr height)))
    (ReturnStmt (IdentExpr total))))
```

**Resolve a symbol with scope awareness** — declarations, reads and writes, each
pointing back at its binding:

```sh
$ hxq refs total Sample.hx
Sample.hx:
  3:2: [decl] total
  4:9: [read] total -> 3:2
```

**Refactor at the AST level.** Operations take a `<line>:<col>` cursor (the exact
coordinate `refs` prints), print the rewrite to stdout, and apply in place with
`--write`. Everything but the touched span stays byte-identical, and a result that does
not re-parse is rejected rather than written.

```sh
$ hxq rename Sample.hx 3:8 prod          # rename the binding at the cursor + its uses
class Sample {
	public function area(width:Int, height:Int):Int {
		final prod = width * height;
		return prod;
	}
}

$ hxq inline Sample.hx 3:8               # inline the local into its single use
class Sample {
	public function area(width:Int, height:Int):Int {
		return (width * height);
	}
}

$ hxq change-sig Sample.hx 2:18 1,0      # reorder params (+ resolvable call sites)
class Sample {
	public function area(height:Int, width:Int):Int {
		final total = width * height;
		return total;
	}
}
```

Every query and most refactors accept `--json` for machine-readable output — the
combination of stable line:col coordinates, JSON, and re-parse validation is what makes
these operations safe to drive from scripts or coding agents.

For the full flag set on any command: `hxq <cmd> --help`.

## Features

### Structural query (read-only)

| Command | Finds |
|---|---|
| `ast` | the parsed AST (S-expr or `--json`), or a subtree by `--select` / `--at` |
| `search` | expression/statement **shapes** with `$x` metavars (`recv.add($x)`) |
| `refs` | value bindings — `--decls` / `--reads` / `--writes`, scope-resolved. A member access (`Type.f()`, `obj?.f()`) is NOT a value binding, so it never appears here; when the scope holds some, `refs` says so on stderr and points at `mentions` — an empty result is never on its own proof that a name is unreferenced |
| `uses` | type-position references (fields, params, type params) |
| `blast` | full change-impact for a type (uses + refs + member access) |
| `mentions` / `lit` / `cases` | every named occurrence / string-or-ident leaves / switch case-patterns |
| `callees` / `callers` | transitive call tree out of / into a function (`--depth`), over the approximate project-wide call graph (name + declared-type resolution, `Null<T>` unwrapped; virtual edges to subtype overrides; `Ref` edges for lambdas / method values / `.bind` with the receiving call recorded as `via`; out-of-scope targets become `[external]` nodes; unresolved sites counted honestly) |
| `reach` | shortest call path(s) `--from A --to B` (repeatable `--to`, `Type.*` patterns) over the same graph — "does main-context code reach this blocking call, and through which chain" |
| `clusters` | partition analytics for one type over the same graph — "along which lines does this god-type split": members grouped by undirected intra-type call-edge connectivity after high-fan-in hubs are extracted into a utils bucket (`--hubs <n>`, `0` = off, default auto; lambdas condensed into their enclosing member); each component reports its directional hub traffic (the future module interface), plus resolved/unresolved coverage counts |
| `meta` | declarations carrying a given `@:metadata` |
| `diff` | structural AST diff between two files |
| `source` | raw verbatim lines — `--range`, or one node's source by name/position (`--select` / `--at`); no-parse mode reads files the grammar can't yet read |

### Scope-correct refactoring (rewrites)

All resolve against real scopes (never by-name text replace), preserve formatting
(span-splice; everything else verbatim), and are re-parse-validated.

| Command | Operation |
|---|---|
| `rename` | rename the binding at the cursor + its in-file occurrences; `--scope <dir>` renames cross-file whatever the cursor lands on — a **type** (decl, type positions, `new`/cast/`extends`, import/using segments, `T.staticMethod()`) or a **member** (method/field: decl, in-type refs, `Src.member` for statics, `obj.member` for type-resolved instance receivers) atomically |
| `inline` / `inline-method` | inline a local variable into its reads / a single-return function into its call sites |
| `extract-var` / `extract-method` | hoist an expression into a `final`, or a statement run into a local function |
| `change-sig` / `add-param` / `remove-param` | reorder params + call-site args / append a backward-compatible param / drop a param + its arguments |
| `move` | move a type declaration to another file (any package), carrying deps and repointing every importer's import path (`a.T` → `b.T`); a cross-package move refuses a fully-qualified `pkg.T` code reference that would dangle |
| `move-member` | move one or more members to another type in the same package — the Apply verb of the `clusters` decomposition loop. Static: `Src.member` callers rewritten across `--scope`, bare in-file callers qualified, sibling references qualified back (`@:access` for private targets). Instance (sibling-fields contract): remaining bare callers rewired through a `--via` field of type Dest (auto-detected when unique), final-field reads kept bare when the destination declares the same field, calls inside the moved set kept bare. `--closure` first grows the move set to the transitive closure of the instance methods the seed members call, so a self-contained cycle moves in one shot instead of one refusal per missing dependency. `--scaffold` generates a missing destination final field + constructor and a missing via field (with `new Dest(...)` wiring in the source constructor) into an empty destination instead of refusing. Visibility promoted when callers remain, imports carried; atomic (all rewritten files re-parse or none). A STATIC-only move may cross packages (source gains the dest import, dest gains the src import for its sibling calls); a fully-qualified `pkg.Src.member` caller is refused |
| `extract-interface` | generate an interface from a class's public instance methods (or a `--members` subset) and make the class `implements` it — signatures sliced `[FnMember, body)`, referenced imports carried, the interface written to a sibling file; no call sites change (additive) |
| `pull-up` / `push-down` | move an instance member along the inheritance axis (to a direct super/subclass) — no call sites change (inheritance keeps it visible); pull-up refuses when the body references a subclass-only member, push-down warns that superclass-typed callers stop compiling |
| `extract-superclass` | generate a superclass, pull a chosen `--members` set up into it (bodies + carried imports), and make the class `extends` it — no constructor on the superclass (the source constructor is unchanged), no call sites change; refuses stranded references and a class that already extends one |
| `safe-delete` | remove a member only when no reference to it survives across the scope — the guarded, cross-file, any-visibility form of `remove-member`; any `x.member` access or bare in-type reference blocks the deletion (self-recursion excluded) and is listed |
| `encapsulate-field` | turn a stored `var` field into an `@:isVar` property with `get_`/`set_` accessors — the field stays the backing storage so no reference is renamed; requires a plain, non-final, non-static, explicitly-typed instance field |
| `make-final` | turn a mutable `var` field into `final` when it is never reassigned after its single initialisation (any write outside the constructor, cross-file `obj.field =` included, refuses) — unblocks the `move-member` instance path, whose final-fields contract needs it |
| `introduce-parameter-object` | replace a contiguous run of a function's parameters with one object parameter of a generated `typedef` — rewrites the signature, the body's references (`x` → `point.x`), and every resolvable in-file call site (`f(1, 2)` → `f({x: 1, y: 2})`); reuses the `remove-param` call-site completeness proof; a short `$x` string-interpolation use is refused |
| `symbols` / `importers` / `declares` | list top-level type declarations across a scope / files importing a module / the declaration site(s) of one named type |

A second family — `add-member`, `add-import`, `add-element`, `replace-node` — *inserts*
new code: the snippet is placed at an AST-resolved position, then the file is re-emitted
through the writer so the new code is formatted by the grammar's own rules and
re-parse-validated in one step. Their inverses `remove-element` / `remove-import` /
`remove-member` delete a node (with its doc-comment) the same way. `set-doc` adds or
replaces a declaration's doc-comment, and `set-modifier` flips its visibility / adds or
removes modifiers (`public`, `+static`, `-inline`, …) — both at a cursor, without
retyping the declaration (the safe replacement for editing a modifier via `replace-node`).
`set-comment` is the comment counterpart of `set-doc`: it replaces the comment at a
cursor — a block comment whole, a contiguous run of full-line `//` comments as one unit,
or a trailing `//` — reaching inline comments that aren't declaration doc-blocks.
`patch` is the surgical counterpart of `replace-node` for small edits: it replaces ONE
unique fragment inside the addressed node (the payload is the old fragment, a `====`
separator line, and the new fragment), so changing three lines does not resend the whole
declaration — and several pairs can be applied in one call by alternating sections
(`old1 ==== new1 ==== old2 ==== new2`), all matched against the original node text with
non-overlapping ranges. A fragment is matched byte-exact first, then line-wise with
indentation ignored (a multi-line fragment copied from the dedented `source --select`
output works as-is), and must occur exactly once within the node.

`rewrite '<pattern>' '<replacement>'` is structural search-and-replace — the fusion of
the structural `search` with a span-replace. Every node matching the pattern (with `$x`
metavariables) is rewritten from a template in one pass: `$x` / `${x}` expand to the
captured node's source, and `${x+N}` / `${x-N}` shift an integer-literal metavariable by
N (`gofmt -r` / comby for the grammar's own AST).

`comment-rewrite '<find>' '<replace>' <path>…` is the write-twin of `lit`: a text
find/replace scoped to comment **bodies** — the gap `rewrite` (AST nodes only) and
`set-comment` (one block at a time) leave open. `--regex` makes `<find>` a regex and
`<replace>` a template where `${1}` expands a capture group and `${1+N}` shifts an
integer group by N. A literal `<find>` matches even across a comment's line breaks (a
phrase wrapped over two ` * ` doc lines). Code and the comment delimiters are never
touched; strings are skipped. A mechanical convention change cited across many doc-comments (e.g. bumping a
coordinate) becomes one command instead of a hand-rolled script.

### File creation & formatting

The create-side and whole-file counterparts of the insert ops — a new file gets the same
guarantees: it parses or is rejected, comes out byte-canonical, and is written atomically.

| Command | Operation |
|---|---|
| `new` | create a new module deterministically — `--kind class` (default; `--implements <iface>` stubs every method with its real sliced signature and carries the imports so it type-checks) / `interface` / `enum` / `typedef` / `abstract` (`--underlying <T>` [`--from`/`--to`]), plus `--extends <T>` (class superclass / interface super-interfaces / typedef `> Base` struct extension, qualified names imported), `--open` (non-final class), `--field` verbatim members, and `@@` stdin sections (`@@ <method>` bodies / `@@ members` free-form member block / `@@ imports` / `@@ doc`). `--raw -` instead takes the COMPLETE file from stdin — the validated atomic equivalent of a raw write, for shapes no spec covers |
| `fmt` | canonicalise files / dirs through the writer round-trip — gofmt-style `--write` (rewrite in place) / `--list` (report drift) |

Because raw text editing has no such guarantee, `new` is the way to create a file and
`fmt` the way to bring one back to canonical form.

**The round trip is fail-closed on COMMENT LOSS — this governs every rewrite in the tool,
not just `fmt`.** The Trivia parser captures a comment only where a capture slot exists, so
an inline comment in a slot-less seam — `if (/* c */ x)`, `return /* r */ x;`, a type
annotation, a class header — never reaches the AST and would be deleted by the re-emission.
Rather than hand back those bytes, `writeRoundTrip` refuses, and each caller keeps the
original bytes:

- `fmt` names the file and the comment on stderr, leaves the file byte-identical and exits
  non-zero (`--write` / `--list` also count it in their `N failed` tally; note a refused
  file never appears on `--list`'s stdout, so check the exit code, not just the list);
- every op that canonicalises through `RefactorSupport` — `add-member`, `rename`,
  `set-doc`, … and `lint --fix` — refuses its edit rather than writing the file without the
  comment. One slot-less comment therefore makes a whole file un-editable by the ops until
  the comment moves; that is the trade, and the message names the comment;
- `new` / `new --raw` reject the content instead of creating a file that lost part of it.

Freezing a file's formatting is recoverable; deleting an author's comment is not. Set
`APQ_ALLOW_COMMENT_LOSS=1` to decline the guard while developing the writer itself — it is
process-wide and re-arms the deletion on the write paths too, so `fmt` and `lint --fix`
print a warning when they run under it.

### Analysis (lint)

`lint <scope> [--rule <id>] [--fix]` runs grammar-agnostic checks and reports violations
grouped by file; `--fix` applies the auto-fixable subset (re-parse-validated like every
rewrite), iterating to a fixed point so a fix that exposes another finding — a deleted
dead-code run leaving a local unused, a de-nested `else` revealing the next — is resolved
in the same run. A check is a plugin — a new one is a new class, not a core change.

A finding whose span sits inside a `macro …` QUOTATION is dropped, for every rule at once — source written there is the AST the surrounding program builds, not code the file runs, so a rewrite that is equivalent anywhere else silently hands the macro different data (`macro switch x { case A \| B: … }` reifies its label as one `EBinop(OpOr, …)` value where `case A, B:` reifies as two; `default:` reifies as `ESwitch.edef` where `case _:` becomes another entry of `cases`), and a report-only finding there is un-actionable besides. The drop is enforced centrally — at `Linter.run`, at the `RiskyFix` verifier, and at the oracle-assisted batch, the three points a check's own findings enter the pipeline — so it covers rules not yet written; the compiler oracle cannot stand in for it, since an edited quotation still typechecks. A check that analyses quotations AS DATA opts out with the `QuotationAware` marker (nothing does today).

Findings are suppressible inline: a trailing `// noqa` (or `// noqa: <rule>,<rule>` for
named rules) clears any finding whose source span covers its line — so a directive the
writer reflowed onto a continuation line still lands — and `// CHECKSTYLE:OFF` …
`// CHECKSTYLE:ON` clears a region (matching the finding's reported line) — so one stubborn false positive never makes a rule unusable. `--fail-on
<error|warning|info>` exits non-zero when a finding at or above that severity survives
(default: report-only, exit 0), and `--format <text|json|checkstyle>` switches the
output — checkstyle XML the same CI tooling that reads `checkstyle.json` can ingest.
`--list-rules` prints every registered check as `id  description`, one per line, in
registration order and exits — the machine-consumable listing (review tooling
subtracts linter-owned rules from manual checklists by it; the check table below is
the prose counterpart).

An `apqlint.json` is discovered by walking up from each linted file (per-file, directory-memoized — a `test/apqlint.json` governs only the files beside and below it, like a nested `hxformat.json`) and configures that file's checks: per
rule it can disable the rule (`"enabled": false`, dropped from the default set — an
explicit `--rule` still runs it), override its reported severity (`"severity":
"error|warning|info"`, applied before `--fail-on` and the report), or set a
rule-specific option (e.g. complexity's `"max"`, which takes precedence over a
`checkstyle.json` threshold). A missing file is a no-op; a malformed one (invalid
JSON, or a wrong-typed top-level key — the file is read through a declared typed
schema) degrades to defaults wholesale and reports one stderr line naming the
file. Wrong-typed values INSIDE a rule's own options stay per-field lenient.

A top-level `"resolutionRoots"` (array of directory paths, relative to the
config file resolved to absolute) declares a separate RESOLUTION scope: the
`.hx` under those roots join the cross-file `SymbolIndex` that the type /
inheritance checks (`redundant-this`, `prefer-index-access`, `map-keys-lookup`)
and the field-access checks (`prefer-final-field`, `prefer-final-public-field`,
`prefer-read-only-field` — a subtype declared in a root that WRITES an inherited
field vetoes the rewrite, exactly as a report-scope one does)
resolve against — so a class `extends openfl.display.Sprite` is seen to inherit
`addChild` even though OpenFL is not being linted. The resolution scope is
never reported and never edited by `--fix`; only the files named on the command
line are. The roots are read lazily — a run whose checks demand no cross-file
resolution never touches them.

A top-level `"resolutionLibs"` (array of haxelib NAMES) is the preferred form
for a library that is installed through haxelib: each name is resolved to that
library's actual source directory via `haxelib libpath <name>` (honouring a
`haxelib dev` link and the currently-selected version) and its `haxelib.json`
`classPath`, so the config stays correct on every machine regardless of whether
the library is a released version, a git install, or a dev link — no brittle
machine-specific path. Resolution is LAZY and happens inside the same
resolution-scope thunk: the `haxelib` lookup runs only when a check actually
demands the cross-file index (a `--rule prefer-single-quotes` run never spawns
`haxelib`), and a name that does not resolve (a typo / uninstalled lib) is
skipped with a stderr note while the lint proceeds. `resolutionLibs` and
`resolutionRoots` coexist and union into the same scope — reach for
`resolutionRoots` when the library is a sibling checkout not registered with
haxelib, and `resolutionLibs` otherwise.

The installed Haxe **std** joins that same scope IMPLICITLY, with no key at all
— discovered through `HAXE_STD_PATH`, the `which haxe` sibling, or a known
install prefix, and contributed as its target-agnostic core (toplevel `*.hx`
plus `haxe/` and `sys/`). Whether `haxe.io.Bytes` resolves must not depend on
the project happening to declare an unrelated library, so a project with
NEITHER resolution key still gets it. That makes the resolution scope no longer
opt-in: a config-less run is inert only in the sense that it spawns no
`haxelib` and pays at most one memoised `which haxe` probe per process.

Decline the std with a top-level `"resolutionStd": false` (per project) or the
`APQ_NO_STD` environment variable, any value but empty or `0` (per process, and
it also cuts the std-derived inference tables). Reach for one of them when the
sources target a different Haxe version than the machine's — otherwise there is
no way to refuse, since discovery falls through to the known install prefixes
however `HAXE_STD_PATH` and `PATH` are set.

A top-level `"compilerOracle"` (a path to an `.hxml`, relative to the config
file resolved to absolute) opts the run into treating the Haxe compiler as
ground truth: `apq` runs `haxe <hxml> --no-output` and folds the verdict back
in — report mode prints a `compiler-confirmed` note on a clean build and FAILS
the run with the compiler's errors on a broken one, while `--fix` uses it to
verify each risky autofix and revert the ones that stop the project
typechecking. Without the key the compiler is never spawned. The compile
directory is PROBED, because `haxe <path>` resolves an hxml's relative `-cp`
entries against the process cwd and real projects write them against two
different conventions: the hxml's relative classpaths are checked for existence
under the hxml's own directory and under the config's, and the candidate
resolving strictly more of them wins (a tie keeps the hxml's own directory). So
a config in a subdirectory naming a parent build file (`"../build.hxml"`, whose
`-cp src` lives next to the hxml) and a root config naming a lime-generated
`dist/<target>/haxe/debug.hxml` (whose `-cp src` is relative to the project
root the build is invoked from) both compile with the classpaths their authors
meant.

A top-level `"compilerOracleServer"` (boolean, default `false`) moves the
REPORT-mode oracle onto a WARM Haxe compilation server shared by every `apq`
process on the machine, instead of a fresh compile per run. The first lint
spawns a detached `haxe --wait <port>`, drives its initial compile, and records
the port and pid under the OS temp dir (`apq-oracle-<digest>.json`, keyed by the
hxml and its compile directory); every later lint typechecks through
`--connect`. Measured on this repo (median of 3): the oracle alone 14.6s → 0.4s,
a single-file `hxq lint <file> --all` 17.9s → 3.2s, a whole `hxq lint src/ --all`
45.7s → 32.5s; the very first call, which spawns the server and drives its full
compile, costs 18.3s — one run's worth of the cold path it then replaces. It is
opt-in because the server DELIBERATELY outlives the run
that started it — `APQ_NO_ORACLE_SERVER` (any value but empty or `0`) declines
it per process, and the recorded pid is how a daemon you no longer want gets
stopped. Every condition the warm path cannot decide under (nothing recorded, a
dead pid, no free port, or a port that answers as something other than a
compilation server) falls back to the cold compile, so the key changes what a
verdict COSTS, never what it is.

Two exclusions are deliberate. The `--fix` risky-fix verification always uses a
fresh compile: a compilation server compares modification times at one-second
granularity, so a file written in the same second as the compile that read it
stays frozen at its previous content — exactly the question a post-write
verification asks. For the same reason every linted file modified since the last
compile is `server/invalidate`d before the typecheck, which leaves as a residual
only a file OUTSIDE the linted set written within that same second.

```json
{ "resolutionLibs": ["openfl"] }
```

```json
{ "compilerOracle": "../build.hxml" }
```

```json
{ "compilerOracle": "build.hxml", "compilerOracleServer": true }
```

```json
{ "resolutionStd": false }
```

```json
{ "resolutionRoots": ["../lib/openfl/src"],
  "rules": {
    "naming":                        { "severity": "error" },
    "complexity":                    { "max": 15 },
    "fold-adjacent-string-literals": { "enabled": false }
  } }
```

| Check | Flags |
|---|---|
| `unused-import` | an import whose bound name is never referenced in the file. An occurrence that is the TAIL of a dot-qualified path (`haxe.macro.Context.currentPos()`) is NOT a reference — an import binds a SIMPLE name, while a dotted path resolves from its root, so a file mentioning the name only fully-qualified holds a deletable import. Only a SINGLE dot qualifies: the `...` of a range / rest is not a qualifier, so `for (i in 0...Limit.MAX)` IS a bare reference, and the ROOT of a path (`Mod.VALUE`) is exactly what the import provides. A `using` counts as referenced when one of its extension methods is called (`.method`) — a stdlib module (`StringTools` / `Lambda`) is read from the installed std, and a module the LINT SET itself declares resolves from its own indexed members, so in both cases a verified-unused `using` is a deletable `Warning`; only a module declared nowhere the run can read stays an unverifiable `Info`. A `.member` occurrence whose receiver is the `using`'s own bound name (`Limit.MAX` under `using a.b.Limit`) is a fully-qualified static access reaching the type through no import, so it never counts as the extension call that keeps it. A plain `import pkg.Enum;` of an enum / enum-abstract is ALSO kept when one of its constructors is referenced as a bare identifier (`Assert.equals(Private, m)`, expected-type resolved) — even though the type name never appears — resolved only when the enum's module is itself in the lint set, so run `--fix` project-wide. A NAMED import whose declaring module is not in the lint file set is an unverifiable `Info` advisory (secondary types / bare enum constructors are unknown), never deleted by `--fix` — lint with the source module in scope for the deletable `Warning`. A `#if`-GUARDED import runs through exactly these same arms: the reference scan reads the raw text of EVERY branch, so a bound name absent from the whole file is unused whichever branch compiles, and its secondary types / bare enum constructors / extension methods resolve as usual. Its verdict is then capped at `Info` and never touched by `--fix` — deleting a span inside a `#if` region is the one place the canonicaliser does not re-normalise what the deletion leaves behind (the emptied line survives as a second blank; emptying the region leaves a bare `#if`/`#end` pair welded to the next declaration), so a guarded finding carries its real reason and is deleted by hand. A project `checkstyle.json` `UnusedImport.ignoreModules` skip-list is honoured — a listed module (matched by full path or last segment) is never flagged |
| `unused-local` | a local `var` / `final` never read in its enclosing scope. Every binding of a multi-variable declaration counts (`var a = 1, b = 2;` reports `b`), but the autofix DELETES BY LINE, so any binding SHARING its line with another is reported and left in place — cutting one would take its live siblings with it |
| `duplicate-import` | an import / using declared more than once in the same file |
| `redundant-import` | a SUB-MODULE type import (`import pkg.Mod.Sub;`) whose module is already imported in the same file — a plain `import pkg.Mod;` binds every TOP-LEVEL type the module declares, not only its main one, so the second statement binds nothing new and `--fix` deletes it. Every gate demands POSITIVE evidence: the statement must be a plain, UNALIASED, unguarded import whose leaf is upper-initial (a lower-initial leaf is a static / field import, which a module import does not bring in) and whose parent segment is upper-initial (a lower-initial parent is a package, so the path is a main type); the same file must carry an unguarded, unaliased `import <module>;` or `using <module>;` (a `using` IS an import plus static extension, so it binds the module's types too — an ALIASED module import binds only the alias and never qualifies, and a `using pkg.Mod.SubTools;` is a static extension the module import does not restore, so it is never a candidate); the resolution index must PROVE the module declares a top-level type of that name, so an unindexed module (outside the lint scope) yields no finding and an enum-constructor / static import (`import pkg.Colors.Red;`) fails by construction — `Red` is a member, not a type; and nothing ELSE in the file may bind the same simple name AS FAR AS THE RESOLUTION INDEX CAN SEE (another plain import or `using` whose module declares it, an alias of that name, a duplicate of this very path, or a type the file itself declares) — Haxe accepts two imports of one simple name and lets the LAST win, so removing one where a second binder exists could retarget the name. A WILDCARD is deliberately not a binder: an explicit module import outranks `import pkg.*;` in either statement order (measured) and the module import survives the deletion, so vetoing on one would only suppress a true finding. That last gate is only as complete as the index — a competing module OUTSIDE the lint scope contributes no veto, and the deletion changes the winner only when such a module sits positionally BETWEEN the qualifying module import and this one — which is a property of the RUN rather than a structurally-provable shape, so the check is a `RiskyFix`: its deletions are applied speculatively and reverted per file when the project's `compilerOracle` says the build broke, and stay report-only where no oracle is configured. The rule is the report side of the fact `TypeRefPrinter` prints by: an insert that a fixer would make redundant is now not made at all |
| `import-order` | a contiguous block of plain `import` statements carrying NO recognisable order — neither plain codepoint order nor the case-FOLDED one an IDE's "organize imports" writes (`ImportOrder`, the same reading every import INSERT preserves). The typical shape is one import appended past a block that was otherwise in order. Import layout is a project convention rather than a defect, so DEFAULT OFF: opt in with `"import-order": { "enabled": true }`. A BLOCK is a maximal run of top-level plain imports on consecutive lines; it ends at a blank line (a project's visual groups are ordered independently and never merged), at any other top-level declaration between two imports (a `using`, a wildcard `import pkg.*;`, an aliased `import a.B as C;`, a `#if` region, a type declaration), and at a BLOCK comment. A `using` is never part of a block and its order WITHIN the `using` group never changes — Haxe ranks static extensions in REVERSE declaration order, so one `using`'s position relative to another is semantics, not layout. But a `using` group WEDGED between two import runs does not make them two blocks: each run reads as ordered on its own, so the shape was a fixed point no finding described and no fix repaired. Such a wedge is reported and, by default, merged — the runs become ONE sorted block and the wedged `using` lines move BELOW it, keeping their own relative order (`"usingAfterImports": false` restores the pre-wedge reading). A merge needs the gap between two runs to hold NOTHING but `using` lines and blank lines, and adds three refusals to the two below: every chained run HEAD must be free of an absorbed leading comment (not only the block's first line), a wedged `using` may not OVERTAKE an import it currently loses a simple name to, and the `using`'s own module must be KNOWN to the RESOLUTION-scoped index — the last-segment fallback would answer "no collision" on no evidence for exactly the multi-type facade modules a project writes `using` for, so declare the library in `resolutionLibs` to unlock it (the Haxe std is always in scope). `--fix` is a pure REORDER of complete lines: each import moves as its own whole line together with the whole-line `//` comments directly above it and any trailing `// …` on it, nothing is reflowed, added or deleted, so the output is a permutation of the block's own lines. An import sharing its line with anything but a trailing comment is not separable as a line, so it ends a block too. REFUSED (report-only) when the order is load-bearing or a comment cannot be attributed: two imports binding the same SIMPLE name — Haxe accepts both and lets the LAST win, so their order decides what the short name means, and since a plain module import binds EVERY type its module declares the name set is read from the resolution index (this is what catches two modules each declaring a same-named SECONDARY type; an unindexed module contributes only its last segment, the residual limit), a duplicated path being the same refusal by construction; and a block whose FIRST import carries a whole-line comment above it — that comment belongs to the block (a header, a license banner, a `CHECKSTYLE:OFF` marker), and a reorder could neither move it nor leave it behind without saying something false. Option `order` — `any` (default) accepts EITHER recognised order and reports only a block explained by neither, `ascii` requires codepoint order, `case-insensitive` requires the folded one; under `any` the fix sorts a block under whichever order it breaks LEAST often, so one appended line moves instead of the whole block |
| `import-outside-guard` | an `import` / `using` written ABOVE the `#if … #end` region that guards the module's WHOLE body — the island a debug- or platform-only module ends up with when a header line is added by hand, or by a fixer that could not see the guarded header; `Info`, `--fix` MOVES the stranded declarations inside the guard. The shape is `UsingScan.headerOf`'s `guard` — one top-level region with no type declared outside it, the region declaring a type itself, and no `#else` / `#elseif` seam of its own — which is what makes the move sound: every build that compiles any of this file's code has the condition true, so a header line inside the region is in scope for all of it and one outside it is in scope for nothing else. ONE finding per file, on the first stranded declaration, and the fix moves the whole stranded header: Haxe ranks static extensions in REVERSE declaration order, so moving one of two `using` lines past the other would flip which module supplies a same-named extension method — and a per-declaration finding could be `// noqa`'d one line at a time, which is exactly that partial application. Placement keeps the original order: imports go ABOVE the guard's first import, `using` lines ABOVE its first `using` (written earlier = lower priority, and above is where that stays true), falling back to the guard's last import and then to the `#if` directive's own line end — never the first child's span, which would splice between a declaration and its doc comment. Blank lines are the writer's (the region body's import↔using↔type policy); the merged import run is NOT re-sorted, which is `import-order`'s job. A declaration that cannot be lifted as a whole LINE — its line carries other code, a block comment follows it, the file ends without a newline — leaves the finding report-only, as does an unliftable line anywhere else in the header, since the relocation is all-or-nothing |
| `naming` | a declaration name violating the convention — the built-in default, or one adapted from a project's `checkstyle.json`. `--fix` renames when a normalization is derivable and provably safe: an underscore-missing private field gains the `_` prefix, and an underscore-prefixed private constant (static final) or private METHOD drops it when the stripped name passes the rule's own format and is not a Haxe keyword (`_forceBuild` → `forceBuild`, `_FORCE_BUILD` → `FORCE_BUILD`, `__onMouseOver` → `onMouseOver`; `_new` → `new` is refused). An UPPER_SNAKE **local** `final` over a compile-time-constant initializer (the scalar proof `inline-constant` owns, or a plain string literal) is not camelCased but HOISTED: the declaration moves to its enclosing class as `private static inline final`, keeping the spelling the author chose — at class level that IS the Constant convention (a String constant drops the `inline`: an inlined String duplicates its bytes per use site on hxcpp), all of a class's hoists landing as ONE insertion after its body brace, a trailing same-line comment traveling along; every gate failure falls back to the ordinary rename silently — the declaration must be a DIRECT child of a statement block (a brace-less `if` / `for` / `else` body owns its body slot: deleting its line would hand that slot to the next statement, a semantic change the re-parse gate accepts), single-binding, outside any `#if` region (the other branches would gain a member they never declared), no comment on the line directly above (ambiguous ownership), its enclosing type a CLASS declared in this file that may hold statics (`@:generic` cannot) and PROVABLY lacking the name across the full supertype closure (Haxe rejects a static named like an inherited instance field, so an unresolvable closure refuses), the name occurring nowhere else in the file, and the policy's Constant rule — selected by the modifier run the member actually carries — accepting it. A private method takes two gates a field does not: an `override` binds the name to the supertype's declaration, and an annotation-bearing (`implicitlyReachable`) member can be reached by NAME from a macro / `@:keep` / framework — both stay report-only, as does a method whose de-prefixed name any supertype already declares (Haxe's "should be declared with 'override'"); the rename is scope-correct and refuses on a name collision — asked as an OCCURRENCE-CLASSIFIED query (`RefactorSupport.nameBoundInRange`), so comment text, inert string-literal content and the member-name slot of a dotted access (`o.scaleX`) are not mistaken for bindings, while a `#if` body, an interpolating literal and an `import` path's last segment still count — a cross-file escape (subtype / `@:access` / `@:allow`), a reflection-string mention of the old name in any other indexed file, or any residual occurrence the resolver cannot rewrite (bare `$name` interpolation, in-file reflection string). A non-confined private field renames ACROSS files (declaring file + every subtype / `@:access` grant file) as one atomic edit set, and every occurrence in those files must be attributed to a provable owner: one binding to the owner's hierarchy renames, one binding to a PROVABLY unrelated type is left alone, and anything in between refuses the whole rename. An AMBIGUOUS simple name is in between — when two modules declare a type of the same name, the subtype relation is not provable either way, so the rename stays report-only rather than committing the declaring file alone |
| `no-underscore-prefix` | a function PARAMETER or a LOCAL binding (a `var` / `final` local, a loop variable, a comprehension variable, a local `function` / `inline function`) whose name carries a leading underscore (`_event`, `__selectedIndex`, `__finish`) — the `_` prefix is a private-FIELD marker and carries nothing on a binding scoped to one function. Opinionated, so DEFAULT OFF: opt in with `"no-underscore-prefix": { "enabled": true }`; `params` / `locals` (both default `true`) narrow it to one kind, and `allowInheritedShadow` (default `false`) cedes the supertype-shadow veto described below. Never flagged: an all-underscore discard (`_`, `__`) or dunder name, an anon-STRUCTURE field, and a CATCH variable — a `_`-prefixed catch variable is `swallowed-exception`'s intentional-discard marker, so de-prefixing it would re-open a silenced finding. `--fix` strips EVERY leading underscore (`_event` → `event`, `__selectedIndex` → `selectedIndex`), reusing `naming`'s rename machinery: the scope-aware collision proof refuses a target name reachable from the binding's scope (a sibling parameter, an overlapping local, an own member, a static, an import — this is what keeps `function new(_x) { x = _x; }` from becoming a parameter self-assignment; see `naming` for what does NOT count as reachable — a `': items: '` trace, a `previousMonthImage.scaleX` and a STRUCTURE-FIELD name (`typedef Zoom = { x:Float }`, `{ x: 1 }`, `case { x: n }`) are text or members, not bindings), a LOCAL FUNCTION binds into its ENCLOSING body, never its own span, so a SIBLING local function already holding the target name is a real collision, and — Haxe not hoisting one — an occurrence resolved BEFORE the declaration refuses the rename outright (it binds to whatever the local function shadows); the supertype closure refuses an INHERITED member of that name — a STYLE stance, not a correctness gate (a local / parameter / catch variable legally shadows an inherited member, and the recapture hazard that would matter — the enclosing function already reading that member through a bare identifier — is what the scope-aware proof above already refuses), so `allowInheritedShadow` (default `false`) cedes it for exactly those three categories: a UI codebase where nearly every type inherits a display-object member (`x`, `y`, `width`) has the stance silence the rule wholesale — a declared top-level type of that name refuses too, and the occurrence set must be provably complete (CODE in a `#if` raw region the resolver could not bind, a string literal, a `noqa` line or a resolver-missed occurrence leaves the finding report-only — a COMMENT or string inside a `#if` region is classified lexically, so a commented-out line there no longer blocks every rename of that name in the file). A simple `$name` interpolation read is indexed by the reference walker, so it arrives resolved and renames along; a read whose identifier token is not locatable in the raw bytes (an escape-spelled `$` or name) refuses the whole rename, since the splice would strand it on the removed name; a `"$name"` in a double-quoted string is literal text and blocks. A stripped name that lands on a grammar RESERVED WORD (`RefShape.reservedWords`, checked unconditionally — a policy adapted from a `checkstyle.json` carries no normalizer, and every camelCase format matches `dynamic` as readily as `event`), is not an identifier, or violates the applicable naming rule's own format is refused; so are two flagged bindings differing only in underscore count (`_a` / `__a` both strip to `a`, and neither is visible to the other's collision proof — renaming both would merge two bindings into one, compilably). Note the deliberate overlap with `naming` on LOCALS: the built-in default policy's local format already forbids a leading `_`, so both rules report one; the genuinely new reach is PARAMETERS, which that policy permits to carry the prefix. Cross-rule loop guard: while `unused-parameter`'s silencing `_<name>` rename is live (that rule enabled AND its `renameSilence` option resolving `true` — the knob defaults **false**, so an absent option leaves the guard OFF and an unreferenced `_event` is flagged and stripped like any other parameter), a parameter UNREFERENCED in its function is not reported — otherwise the two fixes would ping-pong `_event` ↔ `event`. With the guard off, note the downstream reach: `unused-parameter` SKIPS an `_`-prefixed name as the intentional-discard convention, so stripping the prefix hands the parameter back to it, and on the autofixable subset (a local function / confined private method) its `Warning` arm then REMOVES the parameter and its arguments. Intended — the prefix carries nothing on a function-scoped binding — but a project that wants the marker respected opts into `renameSilence` |
| `unused-private` | a `private` field / method never referenced — flagged only when the type is provably confined to its file (no subtype, `@:access`, `@:allow`, or skip-parse) |
| `unused-public-member` | a PUBLIC METHOD of a class whose NAME occurs nowhere in report UNION resolution scope outside its own declaration — the public-facing sibling of `unused-private`, whose confinement proof stops at the file boundary. `Warning`, DEFAULT OFF (opt in via `apqlint.json` `"rules": { "unused-public-member": { "enabled": true } }`, or an explicit `--rule`), `--fix` DELETES the method with its modifier/metadata run and its leading doc comment, whole-line. The reference test is TWO complementary raw-text mechanisms: a global identifier-token count map built ONCE per run over every source in scope (the cost-bounded pre-filter — a per-candidate scan of a scope carrying the configured libraries would be quadratic), which must count the name EXACTLY as often as the member's own exclusion region does; then the authoritative confirm `SymbolIndex.nameOccursOutside` on the REPORT index, whose `referencedInRange` also sees the `\x24name` interpolation ESCAPE the plain tokenizer reads as one long token. The exclusion region is `docExtendedSpan(declGroupSpan(…))`, so the member's own doc comment and modifier run do not read as references to it. Raw text means the rule is conservative in the SAFE direction — a name that merely OCCURS in a comment, a string literal, a library, a `#if` branch, or as an unrelated same-named member elsewhere suppresses the finding, so a common name (`update`, `run`, `dispose`) is practically never reported and what survives is a name unique to its own declaration. Member gates (the modifier/metadata run is walked as `orphan-accessor` walks it, RESETTING at every member so a `public` or `@:keep` on a preceding FIELD never answers for the next method): no explicit `public` keyword (an unqualified Haxe member is private — `unused-private`'s), `override` (reached polymorphically), ANY `@:meta` in the run, a guarded one included — the region is descended into, so its metadata surfaces as an ordinary sibling, and a region that merely CARRIES a modifier out makes the run uncertain and skips the member instead — metadata makes a member implicitly reachable, the grammar's own `isImplicitlyReachable` rule, treated as a class rather than a `@:keep` whitelist that would leak by category, and the names the LANGUAGE calls with no written token: `new` / `main` / `toString`, the `for`-desugaring names `iterator` / `keyValueIterator` / `hasNext` / `next`, the `haxe.Serializer` pair `hxSerialize` / `hxUnserialize`, a dunder `__x__`, plus every `get_` / `set_` prefix, which is `orphan-accessor`'s territory and is skipped outright so the two rules can never claim one method. A member a FRAMEWORK reaches is skipped through the very predicate `unused-private` uses (`NamingSupport.frameworkReachable` — a utest `test*` method of a class transitively extending `Test`), so the two unused-* rules cannot disagree about one member. Class gates: `@:keep` / `@:build` / `extern` on the class, `SymbolIndex.transitivelyCarriesRtti`, an ANCESTOR carrying `@:autoBuild` (it generates into DESCENDANTS, so it is read walking the chain UPWARD), unresolvable anything → skip (unlike `orphan-accessor` there is no report-only third arm), and a supertype link that fails to RESOLVE. There is deliberately NO supertype / interface / subtype member query: the text scan SUBSUMES all three, since a same-named declaration anywhere in scope — above, beside or below the class — is itself an occurrence of the token. ANY REPORT file that skip-parses aborts the whole run: it could hold the only call. The RESOLUTION scope needs no such gate, because the token map is built from its RAW SOURCES (`SymbolIndexHost.resolutionFiles`), which a skip-parse does not withhold — reading them off the index instead would drop such a file from both `allFiles` and `sourceOf` and make a call inside it read as absent. The map is also a strict SUPERSET of the report set (unioned in by path), without which the `global == own` count comparison can hold by coincidence against library occurrences the report-scope confirm cannot see. **`RiskyFix`**: the reachability gate list is a NEGATIVE one (every entry is a shape somebody found leaking — the implicitly-called `iterator` / `hasNext` / `hxSerialize` family was exactly that) and the fix DELETES a public method, so the edits are applied only through the compiler oracle's typecheck-and-revert pipeline, and the rule is report-only where no `compilerOracle` is configured. The DELETE adds one gate of its own: no static `Literal` FRAGMENT of an INTERPOLATED string in report scope may be CONTAINED IN the name (`'do$suffix'` may name it at runtime; fragments under 4 characters carry no intent and would veto every deletion). A reflection literal carrying the WHOLE name needs none — its content is raw source text, so the reference test already refuses to REPORT the member, a strictly stronger outcome than `orphan-accessor`'s. The honest gap is a name no single literal spells: `Reflect.field(t, 'zqxwvDo' + 'Thing')` defeats both mechanisms and both gates and breaks SILENTLY at runtime; asking containment the other way round for every whole literal would veto far too much, so `RiskyFix` is the answer. Scope is class bodies (`class` / `final class` / `abstract class`); an interface declares a contract, an abstract's members reach the underlying type by rules the index does not model. Registered in the `--fix` loop's full-scope set: every gate is whole-project, so on an active SUBSET a call site in an unchanged file reads as absent — run it whole-project, a subdirectory run is a preview, not a verdict A method written inside a member-position `#if … #end` is scanned too, branch by branch (the grammar emits one node with every branch's members flattened; boundaries are recovered from the directive text, as `member-order` and `missing-visibility` do) — a call in ANY branch still counts as a reference, since the token scan reads whole files, and a method whose `public` only SOME builds see is skipped rather than judged. Its deletion carries the doc / modifier run from inside the region, and is withheld when it — together with the pass's other deletions in the same class — would take a region's member count to zero (an emptied region is a shape the grammar does not model, and the rejected splice would drop every other edit the pass had for that file). |
| `unused-parameter` | a function parameter never referenced in its body — purely structural. `Warning` (with a delete `--fix`) for the autofixable subset — a named local function, or a confined private method (the same `isPrivateMemberConfined` proof `unused-private` uses) — whose call set can be proven complete WITHIN the file by the shared `RemoveParam.paramSlotEdits` core; `fix` removes the parameter AND its positional argument at every in-file call site (one parameter per function per pass, the rest follow as the fixed-point loop re-runs the proof). Everything else stays `Info` and is REPORT-ONLY by default: a public / unconfined method is a cross-file signature change the `remove-param` op performs with its own advisory. Setting `renameSilence` (an `apqlint.json` option on the rule, default **false**) adds the conservative `_<name>` silence-rename to the `Info` bucket — a decl-site-only, cross-file-safe edit skipped on a `_<name>` collision; it is opt-in because it only HIDES the finding (no dead weight removed, no behaviour changed), so a blanket `--fix` does not rewrite every unprovable signature. Skipped entirely: an `_`-prefixed name (the intentional-discard convention), a body-less interface / abstract method declaration, a method of a type carrying a supertype clause (`extends` / `implements`), whose signature may be a fixed contract — a local function and a method of a supertype-less type stay in scope — and a function CAPTURED AS A VALUE in the same file (its name referenced outside callee position: `addEventListener(E, onFoo)`, assigned to a var / field, returned, `.bind`-partialled, or reached through a COMPUTED callee such as `(cond ? f : g)(x)` — a merely parenthesised callee `(f)(x)` stays a direct call), whose signature is fixed by whatever consumes the reference, making the finding unactionable; the capture scan is by name and file-scoped, so it only ever drops a finding (never invents one) and a capture in ANOTHER file stays invisible |
| `complexity` | a function whose cyclomatic complexity exceeds the threshold — the built-in default (20), or a `CyclomaticComplexity` max adapted from a project's `checkstyle.json` (decision points: `if`/`while`/`for`/`catch`/`&&`/`\|\|`/`?:`/`??`, plus a `switch` counted ONCE — cognitive-complexity style, identified by `RefShape.switchKinds` — rather than once per `case`, so a flat dispatcher / arg-parser is not a false hotspot; branches nested inside case BODIES still count) — report-only |
| `oversized-type` | a type declaration grown past a member-count or line-extent threshold — the first TYPE-level metric (the per-function checks cannot see a god-file): either limit trips ONE `Warning` on the type header naming every exceeded threshold — a decomposition candidate (see `hxq clusters`). Covers the grammar's class-like bodies (`RefShape.visibilityContainerKinds` — Haxe: class / abstract class / abstract; an interface / enum / enum abstract / typedef is deliberately out of scope — a grammar-sized enum is a definition table, not a decomposition candidate). Members are the type body's `RefShape.memberDeclKinds` children, `#if`-guarded members counted too (`RefShape.conditionalMemberKind` recursion, an `#if` and its `#else` branches all counting); modifier siblings are not counted. Defaults 50 members / 2000 lines; an `apqlint.json` configures `maxMembers` / `maxLines` per rule. The reported span is the type's HEADER LINE only, so suppressing takes a deliberate `// noqa: oversized-type` on that line — an unrelated bare `// noqa` deep inside the body cannot silently swallow the type-level finding (with a whole-body span it would, and the god-files this check targets are exactly the ones that accumulate those). Report-only — splitting a type is a design decision (`hxq move-member` / `clusters` territory), not a mechanical autofix |
| `magic-number` | a numeric literal (`IntLit` / `FloatLit` / `HexLit`) used in logic — inside a function body — whose value is not a small conventional one, which should be extracted into a named constant; `Warning`, report-only (a literal cannot be auto-named). Exempt: the values `{0, 1, 2}` (a negative literal parses as a negation over a non-negative one, so `-1` / `-2` are exempt too), plus any number in the `magic-number` `ignore` array of a discovered `apqlint.json`; a literal outside any function (a member field initializer, an enum-abstract value, a typedef default, a metadata argument), a literal directly naming a local binding (`var x = 5000` / `final x = 5000`, which already is the named extraction), a literal in the index slot of a subscript (`args[3]` — a position, not a hidden quantity), a literal in a size comparison against a collection-`length` field (`args.length == 3` / `>= 3` — a structural element count) or in additive arithmetic against one (`s.length - 3` — a count offset), and a literal reaching a string-position method argument (`RefShape.positionMethodNames` — `substr` / `substring` / `charAt` / `charCodeAt` / `indexOf` / `lastIndexOf` / `hex`), directly or through `+` / `-` offset arithmetic (`s.charCodeAt(i + 5)`, `s.substr(0, 4)` — a position, not a hidden quantity), are left alone. A project `checkstyle.json` `MagicNumber.ignoreNumbers` (default `[-1, 0, 1, 2]`, compared by magnitude) replaces the built-in exempt base; an `apqlint.json` `ignore` list adds to it |
| `prefer-enum-abstract` | a class / abstract declaring three or more `static [inline] final <PREFIX>_*` numeric constants that share a name prefix (`RANK_ACCESSOR` / `RANK_CONSTRUCTOR` → `RANK`) **and are used interchangeably** — they read as a closed enumeration but carry no distinct type, so any `Int` flows where one is meant; `Info`, report-only, suggesting an `enum abstract`. The prefix alone is not enough: a domain namespace of independent tuning knobs (`FUZZY_MAX_DIST`, `FUZZY_TOP_K`, …) shares a prefix yet each is its own magnitude, only ever a comparison operand, never an alternative to a sibling. A group is flagged only when two or more of its members appear as a **result value** feeding one sink — a `return` of one function, or an assignment to one lvalue — reached through result-preserving containers (parentheses, ternary branches, switch / case values) but never as an operand of a comparison / arithmetic / call. One finding per prefix group. Left alone: an existing enum abstract, a mutable `static var`, an instance `final`, a non-numeric constant, a prefix-less name, a sub-threshold (< 3) group, and a knob namespace with no shared sink. Report-only because the enum-abstract flavour depends on usage the check does not decide — ordinal `<` needs `@:op(A < B)`, bit-flags need `from Int to Int`, pure `==` / `switch` needs neither |
| `fold-adjacent-string-literals` | a string concatenation whose segmentation does not match the file's own `maxLineLength` (read from its `hxformat.json`). Both directions, one decision: adjacent literals and expression operands MERGE into one interpolated literal (`"a" + "b"` → `"ab"`, `'a' + name` → `'a$name'`), and a literal whose line overflows SPLITS back out at its seams — a `${ … }` interpolation, or an embedded `\n` ESCAPE, which stays with the left fragment (`'one\ntwo'` → `'one\n' + 'two'`). The `\n` seam is what makes a lone over-long string TOKEN layout-fixable at all: the writer can wrap a `+` chain, nothing can wrap one literal. `Info`, `--fix` re-segments; the candidate is VERIFIED through the writer, so a plan is only applied when it actually narrows the construct. A merge runs unconditionally; a split or a same-count re-cut needs the source lines to be genuinely over-long. Skips: reification and annotation arguments, `case` PATTERNS and parameter DEFAULTS (a concatenation is not legal there), enum-abstract VALUES (folding one costs the type its `case` patterns), a comment inside an OPERAND's own span (a comment BETWEEN operands is not a refusal — it locks that group boundary, and the gap's source is re-emitted verbatim, so a chain whose every line ends in a `//` still folds the adjacent pairs inside each line), and an operand that cannot enter a `${ … }` block. A MACRO argument is reported but not fixed — a macro matching `EConst(CString)` breaks silently on a concatenation — unless the macro is named in `concatFoldingMacros` (`["pkg.Type.method"]`, the claim being that it folds an `OpAdd` chain of constants before it reads the string) |
| `dead-code` | a statement made unreachable by a preceding unconditional `return` / `throw` / `break` / `continue` in the same block — purely structural (no type information); `--fix` deletes the whole dead run (every statement from the first unreachable one to the block's end) |
| `if-false` | an `#if false … #end` conditional-compilation region — dead code on every target, at ANY scope (member run / statement / case group / expression / list element); `--fix` deletes the region, or replaces it with the `#else` branch when one exists — but ONLY when the branch a fix would ERASE is trivial (empty, or a bare `;`); a non-trivial eliminated branch (real code, which can encode unimplemented intent) is left report-only with a human-review note appended to the message (`#elseif` chains stay report-only regardless) |
| `cond-assign-merge` | a STATEMENT-scope `#if … #else … #end` region whose EVERY branch is a single assignment to the same target — five lines to say one thing, that the value differs per target — merged into ONE statement whose r-value carries the directives: `#if mobile hitAreaScale = 2; #else hitAreaScale = 1.2; #end` → `hitAreaScale = #if mobile 2 #else 1.2 #end;`. The sibling DECLARATION shape merges the same way, keyword / name / type kept (`#if mobile var q:Int = 2; #else var q:Int = 3; #end` → `var q:Int = #if mobile 2 #else 3 #end;`) — a conditional-compilation branch is textual, not a scope, so the declared local is visible after `#end` either way and the merge moves no binding. The EXIT arms take the same region shape with a value `return` (or a `throw`) per branch, the keyword kept once: `#if release return true; #else return ok(); #end` → `return #if release true #else ok() #end;` — a value-less `return;` is a different kind and refuses the region, and a `return` branch beside a `throw` branch is refused too, since the merged statement can spell only one keyword. The exit arms ALONE also take a region with NO `#else` that is IMMEDIATELY followed by an unconditional exit of the same keyword — the follower is the implicit else (dead inside the region's conditions, since the branch exits) and the merge consumes it, the finding spanning both statements: `#if windows return '\r\n'; #end return '\n';` → `return #if windows '\r\n' #else '\n' #end;`; an `#elseif` chain closes the same way. That implicit-else shape is UNSOUND for the assignment / declaration arms — both statements execute, so the follower would win rather than the branch — and stays refused there. `Info`, `--fix` rewrites it; OPINIONATED, so DEFAULT OFF — opt in with `"cond-assign-merge": { "enabled": true }` (or an explicit `--rule`). Gates, each failing closed: the region is a DIRECT child of a statement list (never a member run, a case group or an expression slot, where the merged form would not be a statement); `#else` is PRESENT, or the exit follower supplies it — without either the statement is conditional and merging would make it unconditional, so a bare `#if … #end`, and an `#elseif` chain that never reaches `#else`, are refused (a chain WITH a final `#else` merges, each clause keeping its own condition); every branch holds EXACTLY ONE statement (an empty branch would silently gain a value, a second statement has nowhere to go); that statement is a BARE plain-`=` assignment whose r-value is not itself an assignment (`a = b = c` would leave the inner one inline), or a single-declarator declaration WITH an initializer, or a value return / throw — a compound `+=` is a different kind and never matches; the text before the value is IDENTICAL in every branch, compared VERBATIM (the l-value, the `<keyword> <name>[:<type>] =` prefix, or the exit keyword), so a branch spelling its target differently is a safe miss rather than a guess about which spelling to keep — and the same equality keeps the arms apart, an assignment / declaration prefix always ending in `=` where an exit prefix never does; no branch statement contains a `#`, which refuses a value already carrying directives (the merge would nest them) and, with it, every string literal the directive scan could have mistaken for a branch marker; every branch VALUE is single-line, since a value broken across lines would splice its continuation between two directives where it reads as belonging to a branch it does not; and each branch header is single-line, since a condition broken across lines cannot go inline verbatim. Branch boundaries come from the region's own DIRECTIVE LINES, not from the tree — a conditional region projects its branches as FLAT children with no boundary node — scanned nesting-aware and required to reach the region's own `#end` at depth 0 with nothing but that keyword left, so a marker inside a string that unbalances the scan fails the region closed. A COMMENT anywhere in the merged span (which for an implicit-else merge reaches past `#end` to the consumed follower) leaves the finding REPORT-ONLY with a note appended to the message: a comment is trivia, never a child, so the rebuilt statement would drop it |
| `cond-region-merge` | two ADJACENT conditional-compilation regions whose conditions say the same thing — one region spelled twice, merged by splicing the boundary between them (`#end` immediately followed by `#if`) and nothing else, so no member or statement moves: `#if mobile a(); #end #if mobile b(); #end` → one region; `#if mobile a(); #end #if !mobile b(); #end` → the boundary becomes `#else`. An `#else` branch IS the negation of its own `#if`, so a region closing with `#else … #end` absorbs a following `#if !cond … #end` whole. `Info`, `--fix` rewrites it. The splice is sound only when the LAST branch of the first region and the FIRST branch of the second are the same condition, which is the four accepted combinations (`A` / `B` the regions, `C1` / `C2` their conditions): `A` plain + `B` plain + `C1 == C2`; `A` plain + `B` with `#else` + `C1 == C2` (`B`'s `#else` becomes the merged region's, meaning `!C1` exactly as it meant `!C2`); `A` with `#else` + `B` plain + `C2 == !C1`; `A` plain + `B` plain + `C2 == !C1` (the `#else` case). Everything else is refused, because the splice alone would change what runs and the order-preserving rewrite moves code: `A` with `#else` under `C1 == C2` (`B`'s body would land under `!C1`), `B` with `#else` under `C2 == !C1` (its `#else` body belongs to `C1`, so it would have to jump ahead of `B`'s own body), `#else` on both sides, and an `#elseif` anywhere in either region (the merged chain's `#else` would then mean `!(C1 \|\| C2 \|\| …)`). Adjacency is the load-bearing gate: the regions must be consecutive in the directive scan AND the source between their directive lines blank or comment — a single statement in that gap is what separates a real pair from a verbose-log region before a lock acquire and another after it, which share a condition and are two lines apart. Whatever the gap holds is preserved verbatim, in place. A directive must own its line: code before `#end` refuses the pair outright (the splice is line-based). A COMMENT the splice would absorb leaves the finding report-only with a note instead: one written in the GAP would end up inside the merged region, where a doc comment belonging to whatever follows the second `#end` would become conditional (a real site: a `#if macro` import block and a `#if !macro` build-meta block with the interface's own doc between them), and one trailing either DIRECTIVE sits on a line the splice deletes. An unbalanced scan — an `#end` with nothing open, a region left open at end of file — refuses the whole file, since the region model is what decides which branch a body belongs to. A pure source scan over `CondDirectives`, so it reaches every position a directive can take (module level, class body, statement list, another region's branch) and works on a file the grammar cannot parse; `member-order`'s conditional regrouping is not the same job — it is about ORDER, compares conditions by text rather than semantically, and never sees a statement-scope region |
| `empty-block` | an empty control-flow block — an `if` / `else` / loop / `try` / `catch` body written as `{}` with no statements (a comment-only block is left alone, an empty function body is not flagged) — purely structural; `--fix` removes the provably-safe subset (an empty `else {}`, and an empty no-else `if (cond) {}` with a side-effect-free condition), leaving an empty loop / `try` / `catch` body report-only. Disabled for a file whose `checkstyle.json` `EmptyBlock.option` is `empty` (the project deliberately allows empty blocks) |
| `swallowed-exception` | a statement-context `catch` (`try { … } catch (e) { … }`) whose non-empty block silently swallows the caught exception — it neither references the exception variable, rethrows, nor returns (a generic log, an unrelated side effect) — `Warning`, report-only; purely structural. Only the statement form is considered: an expression-position try (`var x = try … catch (e) fallback`) recovers by producing the catch value and is never a swallow. Skipped: an empty catch (left to `empty-block`), a body that uses the variable (handled), a body that rethrows / wraps-and-rethrows or returns a fallback (deliberate escalation / recovery), and a `_`-named variable (the explicit intentional-discard convention) |
| `identical-operands` | a binary operator whose two operands are textually identical (`a == a`, `a != a`, `a && a`, `a.x == a.x`) — almost always a bug; an operand containing a call (`g() == g()`) is left alone — report-only |
| `self-assignment` | a LOCAL variable assigned to itself (`x = x`), a provable no-op — flagged only when `x` resolves to a local declaration; a field / property self-assignment (`p = p` or `this.x = this.x`) may invoke a setter and is left alone; `--fix` deletes a flagged local self-assignment |
| `duplicate-case` | a switch branch whose pattern repeats an earlier branch in the same switch (dead code); guarded branches with the same pattern but different guards are left alone. `--fix` deletes the later (dead) arm. A finding inside a `macro …` quotation is dropped (`ReificationScan`) — there the switch is AST the macro builds, and deleting an arm changes the `cases` a macro reads |
| `case-pattern-separator` | a switch case label whose TOP-LEVEL pattern separator is not the configured one — Haxe spells the same label `case A, B:` and `case A \| B:`, and a codebase mixing them reads as though the two meant different things. DEFAULT OFF (`DefaultOff`) — the rule PICKS A SIDE, so running it unasked would rewrite whichever side a codebase already settled on (309 findings over this repository, every one the same way): opt in with `"case-pattern-separator": { "enabled": true }`. `style` picks the target: `comma` (the default, and the reading of any unrecognised value) flags a pattern whose direct child is an or-pattern; `pipe` flags a branch carrying two or more leading patterns — note its layout cost on a LONG label, where the comma form fills its continuation lines while the joined form is an expression chain the writer breaks ONE OPERAND PER LINE (measured over this repository: the comma direction takes 149 lines out, the pipe direction puts 11 back). `Info`, `--fix` rewrites the separator character and leaves the spacing to the writer — except in a verbatim region (`@formatter:off`, a string-interpolation literal), where the bare character edit survives as `case A , B:`: correct, merely uncanonical, and not worth widening the edit span to rejoin lines a user pinned on purpose. Only the TOP level is touched — a nested `case Some(E \| F):` has no comma spelling at all — and a MACRO QUOTATION is skipped whole: there the label is AST the macro builds, and `macro switch x { case A \| B: … }` reifies as ONE `EBinop(OpOr, …)` value where `case A, B:` reifies as TWO, so respelling it silently changes what a macro reading `Case.values` sees. Splitting an or-pattern is always sound, so the comma direction flags on shape alone; JOINING is not (an `=`-capture binds looser than `\|` and tighter than `,`, so `case v = A \| B:` compiles where `case v = A, B:` does not), so the pipe direction gates on a positive whitelist — a constructor-spelled identifier, a dotted constant path, a constructor call over the same, a string / numeric / boolean / null literal, a negated numeric, an or-pattern over the same — and skips the WHOLE branch unless every pattern passes. Refused shapes include every capture, an extractor, an array or structure pattern, and the bare wildcard, which keeps the ubiquitous `case null, _:` idiom as written |
| `prefer-case-wildcard` | a `default:` switch branch replaceable with the idiomatic Haxe `case _:` — consistent with pattern-matching syntax; `Info`, `--fix` swaps the `default` keyword token for `case _`, leaving the trailing `:` and the branch body untouched. Grammar-agnostic over `RefShape.defaultBranchKind` (a distinct node kind from a `case` branch). A `default:` and an unguarded `case _:` are equivalent — both match every subject the earlier cases did not — and `default` is position-independent, so the textual swap is safe regardless of the branch's position; a branch already written `case _:`, or a switch with no default, is a safe miss. Reaches nested and expression-position switches. NOT safe inside a `macro …` quotation — `default:` reifies as `ESwitch.edef` where `case _:` becomes another entry of `cases` — so a finding there is dropped (`ReificationScan`) |
| `redundant-parens` | parentheses that cannot affect the parse, in two shapes: one pair wrapped directly in another (`((e))` / `(((e)))`), and a LONE `(e)` in a DELIMITED position — a slot the surrounding construct already bounds on both sides, so no operator can bind across it (grammar-declared via `RefShape.delimitedAllChildKinds` / `delimitedTailChildKinds` and the existing condition seams: var / final initializer, assignment right-hand side, `return` value, call and `new T(…)` argument — never the callee — array element, object-literal field value, `if` / `while` / `do … while` condition). Precedence is irrelevant there (`f((a + b))` unwraps as safely as `f((x))`) but syntax is not: a construct that eats the slot's separator (`separatorGreedyExprKinds` — Haxe's `macro`-quoted declaration) and a reification whose arity depends on being un-wrapped (`spliceSensitiveExprKinds` — Haxe's `$a{…}`) keep their parens. `Info`, `--fix` unwraps to a single pair outside a delimited position and to nothing inside it (`final m = (-1);` → `final m = -1;`). A third shipped shape is PRECEDENCE-GATED: a lone `(e)` wrapping a TERNARY CONDITION (`(a < b) ? 1 : -1` → `a < b ? 1 : -1`) — an operand slot, not a delimited one, so the drop is proven per-content and fires only for a bare inner that binds strictly tighter than `?:` (`RefShape.ternaryConditionUnwrapKinds`), never for a loose or right-greedy inner (assignment, a nested ternary, an arrow, an `untyped` / `macro` / metadata wrapper) that would absorb the `? … : …` on unwrap. Left alone: the `switch` subject, `case` patterns and guards, and every operand of a unary or binary operator, where unwrapping re-associates (`(a + b) * c` ≢ `a + b * c`). One finding and one non-overlapping edit per site — the outermost paren of a chain is flagged and not descended into. Four further arms are OPT-IN per project and reach the OPERAND positions the shipped arms refuse, each replacing a precedence model with a per-site proof: `"atoms": true` drops a pair around an ATOM — a self-delimiting expression (`RefShape.atomExprKinds`: an identifier, `this`, a literal) or a chain of transparent links bottoming out in one (`RefShape.atomChainKinds`: a call-free dotted access) — which nothing outside can bind into, so it is inert in EVERY position (`(a) + c`, `-(a)`, `arr[(i)]`, `(g)(1)`, `c ? (a) : (b)`, and the common defensive divisor `(x - y) / (r.width)`); a call, an index or a `?.` step breaks the chain (`(a?.b).c` and `a?.b.c` disagree on the short-circuit) and a regex literal is excluded (its closing `/` welds onto a following `/` into a line comment). `"sameOperatorLeft": true` drops a pair around the LEFT operand of a binary operator whose content is a binary operator in the SAME left-associative precedence family (`RefShape.leftAssociativeBinaryFamilies` — Haxe `['Mul','Div']` and `['Add','Sub']`), which left-associativity already grouped that way, so `(a * b) / c` re-parses to the tree it had; the RIGHT operand is never a candidate (`a / (b * c)` is a different computation), families never mix (`(a + b) * c` stays), a parenthesized right operand VETOES the left one (`Math.abs((px - x) + (py - y - h))` — both pairs are one symmetry the author wrote and only the left is removable, so firing would leave the expression lopsided), and Haxe's `%` is in NO family — the language binds it tighter than `*` and `/` (`2 * 7 % 4` is 6) and the grammar models that as a prec-10 tier of its own for `%`, so `%` shares a tier with no other operator; a single-member `['Mod']` family is provable but not admitted. `"comparisonOperands": true` drops a pair sitting on EITHER side of a COMPARISON-tier binary operator (`RefShape.comparisonOperandHostKinds` — Haxe `Eq` / `NotEq` / `Lt` / `LtEq` / `Gt` / `GtEq`, the prec-5 value comparisons; `is` takes a type on its right and `...` abuts numeric-literal `.` lexing, so neither is a host) whose bare content is ARITHMETIC or a POSTFIX in/decrement (`RefShape.comparisonOperandUnwrapKinds` — Haxe `Add` / `Sub` / `Mul` / `Div` / `Mod` / `PostIncr` / `PostDecr`), a fail-closed whitelist of kinds binding strictly tighter than a comparison in Haxe AND in every C-family language, so `(px - x - w) > (py - y - h + r)` and `(a++) < 36` re-parse to the trees they already had on either reading. Both sides are judged TOGETHER: unlike `sameOperatorLeft` a parenthesized sibling is no blanket veto — one whose own content is on the whitelist is provable in its own right, so both pairs drop in the SAME pass; the veto fires only when the sibling pair is NOT provable, which is what keeps the arm from ever leaving a comparison lopsided (`(a - b) > (c ?? d)` keeps both). The BITWISE tier stays off the whitelist for CORRECTNESS — Haxe binds `&` / `|` / `^` tighter than a comparison but C binds them LOOSER than `==` / `!=`, so `(x & m) != 0` reads differently there without its pair, and that pair is cross-language insurance an author wrote on purpose; the SHIFT tier binds tighter than a comparison in C exactly as in Haxe and WOULD be provable on both readings, so it stays off on READABILITY alone — a shift operand is habitually parenthesized and the arm declines to take that away; the PREFIX `++x` / `--x` are provable too and stay off on a READABILITY ground of their own (`(++b) < 36` bare reads `++b < 36`), but only as a ROOT — unlike the leading minus, which `RefShape.unaryMinusKinds` refuses at any depth, a prefix in/decrement has no left-spine gate, so `(--b * c) > d` still drops; only the postfix spelling puts the operand first, and its own `++` / `--` token closes the content against a greedy tail; atom content is off it too — the `atoms` arm owns atoms, and the two converge over `--fix` passes. `"additiveOperands": true` is the same shape one ARM down: a pair on EITHER side of an ADDITIVE operator (`RefShape.additiveOperandHostKinds` — Haxe `Add` / `Sub`, the prec-8 tier) whose bare content is on the strictly tighter multiplicative tiers or is a POSTFIX in/decrement (`RefShape.additiveOperandUnwrapKinds` — Haxe `Mul` / `Div` (tier 9), `Mod` (tier 10) and `PostIncr` / `PostDecr`), which C agrees bind tighter than `+` and `-`, so `a * s - (w / 2.0)` and `(a++) + b` re-parse to the trees they already had on either reading; a chain's MIDDLE operand is reached on the same terms (an additive chain nests to the left, so a middle operand is simply the right child of the inner node), and the sibling SYMMETRY rule is `comparisonOperands`' unchanged — a parenthesized sibling that is itself provable (same whitelist, or an atom when `atoms` is on too) goes in the same pass, one that is not vetoes the drop, so `(a * b) + (c - d)` keeps both pairs rather than being left lopsided. Five exclusions, each with its own reason: SAME-TIER content (`a + (b - c)`, `a - (b + c)`) is out for CORRECTNESS — the drop RE-ASSOCIATES, changing float rounding and, under `-`, the value outright; content whose LEFTMOST token is a unary minus (`RefShape.unaryMinusKinds`) is provable and out on READABILITY, since the bare form reads `a - -b` — a LEFT-SPINE test rather than a root one, so `a + (-b * c)` is refused alongside `a - (-b)`, and `comparisonOperands` no longer drops `(-a) > b` either (a deliberate narrowing of shipped behaviour, one readability rule instead of a root test that could not see the nested case); the PREFIX `++x` / `--x` are left off on that same READABILITY ground (`a - (--b)` bare reads `a - --b`) but by the WEAKER mechanism of mere absence from the root whitelist, so — unlike the minus — they are not refused at depth and `a - (--b * c)` still drops; what that leaves on the whitelist is the POSTFIX spelling, which puts the operand first and whose own `++` / `--` token closes the content, so neither edge gate has anything to refuse; the multiplicative operators are deliberately NOT hosts — Haxe binds `%` tighter than `*` and `/` but C makes the three ONE tier, so a bare `a * b % c` reads `(a * b) % c` to a C-trained eye and the pair in `a * (b % c)` is what makes the two readings agree (the same cross-language trap as the bitwise exclusion above; `%` CONTENT under `+` / `-` is fine, C agrees there); and bitwise / shift are out on both sides for two DIFFERENT reasons — as CONTENT they bind LOOSER than the additive tier, so a drop re-associates outward, which the fail-closed whitelist settles; as HOSTS a drop would be provable (`(a * b) & c` bare re-parses to the tree it already had, here and in C) and they stay out on the READABILITY ground that keeps shifts off the comparison whitelist. Every PRECEDENCE-GATED slot — the three opt-in arms AND the shipped, default-on ternary condition — additionally refuses two things a content ROOT cannot see. A RIGHT-GREEDY TAIL (`RefShape.rightGreedyExprKinds` — Haxe's `untyped` / `macro` / `cast` / `throw` / `return` / `inline` prefixes, a metadata annotation, the function literals and arrow lambdas, `if … else` / `for` / `while` / `try` and the ternary's else branch): such a construct at the right edge of the content eats whatever followed the pair, so `a + (b * untyped c) - d` has an arithmetic root and still evaluates to 9 with the pair against 7 without. A METADATA PREFIX: this parser models `@:m` as wrapping everything that follows while the compiler binds it to the immediate primary, so a pair on the LEFT EDGE of an annotated expression is what holds the annotation over its operand — `@:privateAccess (a * b) + c` compiles and the bare form fails with `Cannot access private field`; a pair further right (`@:privateAccess a + (b * c)`) still drops. Both gates exist because a before/after AST comparison is BLIND to them — this parser's model of `cast` and of a metadata annotation disagrees with the compiler about exactly these extents, so both trees compare equal while the real ones do not (recorded on `HxExpr`'s `CastExpr` / `MetaExpr`). All four opt-in arms are additionally suppressed inside `RefShape.parenOpaqueSubtreeKinds` (a `macro` quotation, where a pair reifies as `EParenthesis` data; a case PATTERN, matched structurally) and at a direct child of `RefShape.parenRequiredHostKinds` (a `case` guard, a `switch` subject, metadata). Every arm refuses a pair whose parentheses are not flush against their content at each layer of the chain — the fix reproduces the content's source and drops the rest, so a comment between `(` and the expression would be deleted |
| `redundant-condcomp-parens` | parentheses around a conditional-compilation condition that is ONE bare flag — `#if (sys)` → `#if sys`, `#elseif (mobile)` → `#elseif mobile`. A single identifier is self-delimiting, so nothing in the directive grammar can bind into it and the pair cannot change how the condition parses. `Info`, `--fix` replaces the CONDITION span and nothing else — the keyword, the indentation, a trailing comment, an `#elseif` chain and any nested region are outside it by construction — inserting a separator when the parenthesis was the only token boundary (`#if(sys)` → `#if sys`, never `#ifsys`; `#if (js)g();` → `#if js g();`). Kept by design: a compound condition (`#if (cpp && debug)`, `#if (a || b)`), a version comparison (`#if (haxe_ver >= "3.1.0")`), a negation (`#if (!flag)` — whether the bare form is accepted is a per-grammar question this rule does not answer), a redundantly nested pair, and a DOTTED define (`#if (target.unicode)` — provably safe to unwrap, but "one bare flag" is the shape the rule committed to; widening it is deliberate future work). Detection is a pure source scan through `CondDirectives`, the shared directive reader (`RefShape.conditionalIfKeyword` / `conditionalElseKeywords` / `conditionalEndKeyword` — a grammar declaring no opener makes the rule a no-op), which masks comment / string / regex text so a commented-out `#if` is not a directive, and delimits the condition without swallowing the code that follows an inline `#if … #end` region. It needs NO parse, so unlike its expression sibling `redundant-parens` it also reports on a file the grammar cannot parse — which is also why it is a rule of its own rather than an arm of that one: a directive is not a node, so it would share neither the paren-node walk nor the span-indexed fix, and an arm would inherit that rule's parse gate. A finding lands on a directive LINE, which the writer keeps as its own line, so a trailing `// noqa` is relocated off it during canonicalisation and cannot suppress this rule — use a `CHECKSTYLE:OFF` / `ON` region or disable it in `apqlint.json` |
| `redundant-this` | a `this.field` access whose `this.` is redundant — nothing in the enclosing member shadows `field`: not a local / parameter / loop or catch variable / local function, nor the three shapes that bind WITHOUT a named declaration node (a case-pattern capture, projected as a bare identifier inside the pattern or as a `case var x:` binder node beside it; the single bare parameter of a thin-arrow lambda; and the slots a binder header DROPS — the value of a key-value `for`, recovered from the header text) — and `field` is a member of the enclosing type, so it reduces to bare `field`; `Info`, `--fix` drops the `this.`. Membership is the same-file member scan first, then — on a miss — an in-scope `SymbolIndex` walk of the `extends` / `implements` chain (so `this.addChild(x)` in a class `extends Sprite` is flagged once OpenFL is in the lint scope). The membership gate is prove-or-stay-silent: it flags only on positive, UNAMBIGUOUS proof, resolving each supertype reference import / qualified-path aware, so a base outside the set, or a name that resolves to more than one same-simple-named type, stays silent — a `using` static-extension name (`this.getClass()` under `using Type`) is a member of nothing and is never stripped. A shadowed access (the `this.x = x` constructor pattern) is kept, and a compile-time `abstract`'s `this.field` (where `this` is the underlying value) is never matched |
| `constant-condition` | a boolean literal as an `if` condition (`if (true)` / `if (false)`) — a branch always or never taken; loops are left alone (`while (true)` is an idiomatic infinite loop) — purely structural; `--fix` replaces the `if` with the always-taken branch (`if (true) A [else B]` → `A`, `if (false) A else B` → `B`, a no-else `if (false) A` statement is deleted) — but ONLY when the ELIMINATED (not-taken) branch is trivial (empty, or a bare `;`); a non-trivial eliminated branch (real code, which can encode unimplemented intent) is left report-only with a human-review note appended to the message, alongside the existing report-only case of a no-else `if (false)` in expression position |
| `empty-statement` | a stray empty statement (a lone `;`); `--fix` deletes it (the whole line when the `;` sits alone, otherwise just the `;`) — purely structural |
| `redundant-trailing-comma` | a `,` between the LAST element of a comma-separated list and its closing delimiter — `[1, 2,]`, `[k => v,]`, `{x: 1, y: 2,}`, `typedef T = {x:Int, y:Int,}`, `new D(1, 2,)`, `g(1, 2,)`, `function f(a:Int, b:Int,)`, `@:meta(a, b,)`, an enum constructor's parameter list. Haxe accepts one in every one of those positions and it means nothing there, so the removal preserves the parse AND the semantics (the positions that REJECT one — a type-parameter list, a `case` pattern list — never reach the check, having failed to parse). The third case — a separator that is MANDATORY after the last element — is declared out by `RefShape.mandatoryTrailingCommaChildKinds`: Haxe's anon-type extension entry, where `{> Base,}` compiles and `{> Base}` is `Expected ,` (with two entries only `{> A, > B,}` is accepted). The veto is keyed on the LAST ELEMENT, not on the host, because the same host mixes both — `{> Base, x:Int,}` ends in an ordinary field and its comma IS redundant. The writer already knows that fact from the other side (`anyparse.format.TrailingCommaPolicy`: such a construct never carries the removable flag), and the two layers must agree — a kind missing from the veto is a `--fix` that emits source the compiler rejects. Sibling of `empty-statement`'s absorbed `;`: the parser folds the comma into the HOST's own span, past its last child, so no node marks it and detection is span arithmetic rather than a tree walk. The trivia writer re-emits it verbatim for the literal hosts (`[1, 2,]` is writer-canonical), so before this rule the shape was invisible to BOTH `lint` and `fmt` — hence a lint rule with an autofix rather than a writer change, which would silently reformat every file carrying one. Two grammar-declared arms: `RefShape.trailingCommaHostKinds` names the hosts that OWN their closer as their own last byte, and requiring the comma to be the last significant thing before exactly that byte is what separates a trailing comma from a separator (a separator always has an element after it); `RefShape.paramKinds` reaches the parameter lists, whose host's span runs on past the `)` over a return type and a body, and whose closer is therefore asked of the source. `Info`, DEFAULT OFF (opt in via `apqlint.json` `"rules": { "redundant-trailing-comma": { "enabled": true } }`, or an explicit `--rule`) — whether a list ends on a comma is a project style call, not a defect, the same call `shorten-type-ref` makes about qualification, and anyparse's own house style KEEPS the trailing comma (1041 would-be findings across `src` + `test`). `--fix` deletes the comma — the whole physical line when it sits alone on one (no blank residue), otherwise just the comma; a comment between the comma and the closer is trivia and survives (`[1, 2, /* t */]` → `[1, 2 /* t */]`). Fail-closed misses: an empty list, and a `#if … #end` region as the last element — that region projects as one node ending AT `#end`, so the gap before the closer holds no comma, and the comma INSIDE the region is left alone (it terminates the list's last element only under some defines). For the hosts whose trivia the writer does NOT preserve (`Call`, `MetaCall`, an enum constructor's parameters) a finding can only appear in a file that is not yet writer-canonical, where `lint --fix` is refused by the canonical gate and `fmt --write` is the remedy — the report still names them so nothing is silently missed |
| `empty-comment` | a content-free comment — a `//` with only whitespace after the slashes, an empty `/* */`, or an empty doc comment (including a multi-line one whose interior is only stars and whitespace); `Warning`, `--fix` deletes it (the whole physical line(s) when the comment is alone on them, the comment plus the whitespace before it when it trails code, the comment alone when code follows it on the same line). A pure comment-token scan (`RefactorSupport.collectCommentTokens`, string-aware — a slash-slash inside a STRING literal is never visited), so no parse is needed and a conditional-compilation line, which is not a comment, never matches. Content keeps a comment, which is what leaves a dashed divider run and a `// noqa` directive alone. **The PARAGRAPH SEPARATOR is kept too**: a bare `//` on its own line with a comment-only line directly above and another directly below is the blank line of a `//` prose block — content-free by every character test, but deleting it MERGES two paragraphs, a change to what the comment says rather than a cleanup. The gate is positive and purely lexical (this token and BOTH comment-stream neighbours are line comments, each alone on its line, on three consecutive lines), so an unlisted shape fails by construction: a bare `//` at the head or tail of a run is padding and still reports, as does one whose neighbour shares its line with code, sits a blank line away (the blank already separates), or is a block comment (a block carries its paragraph breaks inside itself). Measured on this repository's own 636 files, every one of the 136 findings the rule produced WITHOUT that gate was a paragraph separator — an unguarded `--fix` would have silently reflowed 136 prose blocks. A pure trivia edit: comments never affect compilation |
| `redundant-else-after-return` | an `else` whose `if` then-branch always exits (`return` / `throw` / `break` / `continue`), so the `else` is needless nesting — flagged only for a block-statement `if` (an expression `if` keeps its required `else`); `Info`, `--fix` de-nests the else body, skipping a body that declares a local (its scope would widen) |
| `comparison-to-boolean` | a comparison against a boolean literal (`x == true` / `x != false`) where the literal adds nothing, or a comparison where BOTH operands are boolean literals (`true == true`) — a constant, always reported (two literals need no type gate) and folded by `--fix` to its value (`true == true` → `true`, `true != true` → `false`). SEVERITY TRACKS FIXABILITY: a finding `--fix` can rewrite is `Warning`, one it can only surface for a human is `Info` (so an `eqKind`-less grammar, where `fix` cannot tell `==` from `!=`, drops every finding to report-only `Info`). `--fix` rewrites a comparison whose operand is provably non-null `Bool`, by any of three proofs. STRUCTURAL: a boolean-operator result (`&&` / `||` / `!` / a comparison, possibly parenthesized) — `(a && b) == true` → `(a && b)`, `x < y == false` → `!(x < y)`, `!flag != false` → `!flag` (a non-atomic negated operand is parenthesized; a bare ident, a paren, a field access and a call are atomic); or a bare identifier through the declared-type gate (`TypeResolver.isProvablyNonNull`) — `disabled == false` with `public var disabled(get, set):Bool` → `!disabled`. RESOLVED MEMBER TYPE: a field access whose receiver type resolves and whose member is declared plain `Bool` — `o.flag != false` → `o.flag`, the receiver resolving through `cast(o, T)` and the member through inheritance. RESOLVED RETURN TYPE: a method call whose receiver type resolves and whose method is annotated to return plain `Bool` — `o.isReady() == false` → `!o.isReady()`, resolving cross-file, through inheritance and through `cast(o, T)`. Haxe's own std leaves `haxe.ds.Map.exists` / `remove` UNANNOTATED (`public inline function exists(key:K) return this.exists(key);`), so no resolution scope can read a return type for them; the curated `RefShape.instanceMethodReturns` seam supplies the `Bool` their forwarded-to `IMap` members declare, behind the index and only for a receiver name no indexed type shadows — so `map.exists(k) != false` → `map.exists(k)` while `map.get(k) == true` (a `Null<V>`) stays untouched. Report and fix ride the SAME proof, so the fixer strips exactly what the report flags as a `Warning`. Never proven, so never reported and never stripped: a `Null<Bool>` local / member / return (its `== true` is load-bearing under strict null-safety — `if (x)` would not compile), an optional `?x:Bool` param, an unannotated / unresolvable identifier, receiver or return, any `?.` in the operand (`obj?.flag == true`, `o?.has(k) != false` — the result is `Null<Bool>` whatever the annotation says), a `Map.get` / array-element result, a bare `f()` with no receiver to key the lookup on, an anon-struct receiver (`@:optional` is invisible in the member table), a simple-name homonym receiver, a `#if`-guarded member declaration, and comparisons inside macro reification. Without a `TypeInfoProvider` the report keeps an unresolved bare ident for a human at `Info`, and the fixer still never strips without proof |
| `collapsible-if` | an `if` whose sole then-branch is another `if`, neither carrying an `else` (`if (a) { if (b) … }`) — the two conditions merge with `&&`; `Warning`, `--fix` rewrites to `if (a && b) …` (behaviour-preserving via short-circuit), parenthesizing a lower-precedence operand (`if (a || c) if (b)` → `if ((a || c) && b)`) |
| `collapsible-else-if` | an `else` whose body is a block holding exactly one statement, and that statement is an `if` (`} else { if (b) p(); }`) — the braces buy nothing, the chain already reads as `else if`; `Info`, `--fix` collapses it to `else if (b) p();` by replacing the block's span with its interior, i.e. stripping ONLY the braces (and the whitespace they carried), so a comment inside the block survives verbatim — rebuilding the text from the inner `if`'s span would drop it, a comment being trivia and never an AST child. Gated on POSITION as well as shape, and the position gate is a correctness gate: the `if` owning the `else` must be a direct child of a statement list (`ControlFlowSupport.blockKinds`) or the else branch of another `if` that itself passes the gate (the `else if` chain case, which inherits its head's position and IS flagged). Anywhere else the flagged `if` may be the brace-less body of an enclosing statement carrying a trailing `else`, and de-bracing would hand that `else` to the inner `if` — in `if (x) if (a) p(); else { if (b) q(); } else r();` the `r()` would go from `!x` to `x && !a && !b`. Deliberate safe misses: that dangling-exposed position and its loop-body / `case`-body / `#if`-region-direct-child variants, a block whose interior opens with a COMMENT ahead of the `if` (the collapse would strand it between `else` and `if` and push the `if` onto its own unindented line — a comment AFTER the `if` merely trails the collapsed statement and stays flagged), a block with more than one statement, a block whose sole child is a `#if … #end` region (which projects as its own node, so the sole child is not an `if`), a block wrapping another block (`unnecessary-block` owns that layer), and expression-position `if` (excluded from `RefShape.ifStatementKinds`). An `else` on the INNER `if` is still flagged — it stays that `if`'s `else` after the collapse. Brace layout on the collapsed chain is NOT this check's business: the interior goes out as written and the canonicalizing writer round-trip settles it, so under a `singleStatementBraces: remove` policy the collapsed body gains braces when the sibling then-branch keeps them and stays bare when it does not (`SingleStmtBraces` gate 7, both directions) |
| `double-negation` | a redundant double logical negation, read through parentheses (`!!x`, `!(!x)`, `!(!(a && c))`); `Info`, `--fix` strips the redundant pair keeping the operand's own source (`!!x` → `x`, `!!!x` → `!x`, `!(!(a && c))` → `(a && c)`) — but only when the operand is provably non-null (its subtree reaches no `Call` / `FieldAccess` / `?.` access), since `!!maybeNull` coerces to a definite Bool that bare `maybeNull` would not. A negated COMPOUND (`!(!a \|\| b)`) — or a negated single comparison (`!(x < 0)`) — is `simplify-negated-compound`'s node, never this rule's; that rule's operand whitelist excludes the not-kind for the same reason, so the two tests are disjoint |
| `simplify-negated-compound` | a top-level `!(...)` rewritable to strictly fewer negations, over either of two operand shapes: a boolean COMPOUND that De Morgan distributes (`!(!a \|\| b)` → `a && !b`), or a SINGLE comparison whose operator flips (`!(x < 0)` → `x >= 0`, `!(a == b)` → `a != b`). `Info`, `--fix` rewrites via the guard family's shared inverter with its type-gated comparison flip (`< 0` → `>= 0` only when the operand's nominal is TOTALLY ORDERED by `<` — `Int`/`UInt` — provable through a method-call tail such as `String.indexOf`; `==` / `!=` flip unconditionally, NaN and `null` agreeing with the wrap either way). `String` is deliberately NOT a licensed nominal: it carries no NaN, but Haxe has no non-nullable string type, and with a null operand `!(s < t)` is `true` where `s >= t` is `false` (measured on `--interp`, `js`, `neko`). Inside a compound a partial result is offered when it still strictly drops a `!` (`!(!a \|\| f < 0.5)` → `a && !(f < 0.5)`); a lone unprovable ordered comparison has no partial form and is refused OUTRIGHT (`!(f < 0.5)` with `f:Float`, `!(s < t)` with `s:String`, both stay put) — the wrap it would emit IS the input. Refusals: worth ties (`notDelta >= 1`, incl. pure distributions `!(a \|\| b)`), operands outside the whitelist (`\|` / `&` bitwise, `is`, `??`, calls, bare identifiers, and `!` itself — `!(!x)` stays `double-negation`'s), comments in the span, `#if` in the span, stranded null-narrowing (an operand depending on an earlier operand's narrowing), non-outermost nests (outer wins, fixpoint picks up the rest) |
| `prefer-null-coalescing` | a null-guard ternary that `??` replaces — `x != null ? x : y` / `null != x ? x : y` / `x == null ? y : x` / `null == x ? y : x` all collapse to `x ?? y`; `Info`, `--fix` rewrites it (a guarded value containing a call is left alone — `??` evaluates it once, the ternary twice; a bare-ternary fallback is parenthesized since `??` binds tighter than `?:`) |
| `prefer-array-literal` | an empty-argument `new Array()` / `new Array<T>()` replaceable with the array literal `[]`; `Info`, `--fix` rewrites it (the element type carries through the assignment target's annotation; an unannotated `var x = new Array<Int>()`, whose only type source is the constructor's own parameter, is left alone). A plain ASSIGNMENT `x = new Array<T>()` / `this.x = new Map()` also rewrites when the lvalue resolves — shadow-guarded, via `TypeResolver.identDeclaredTypeSource` (typed local / param / own field) or `SymbolIndex.memberTypeSourceOf` for `this.<field>` — to a declaration whose explicit annotation's outer nominal is `Array` (resp. `Map`); a `Dynamic`/non-collection or unresolvable target is never rewritten |
| `prefer-map-literal` | an empty-argument `new Map()` / `new Map<K, V>()` replaceable with the map literal `[]`; `Info`, `--fix` rewrites it (same annotation caveat as `prefer-array-literal`) |
| `prefer-map-type` | a concrete `haxe.ds` map type written where the unified `Map<K, V>` syntax says the same thing — `IntMap<V>` → `Map<Int, V>`, `StringMap<V>` → `Map<String, V>`, `ObjectMap<K, V>` / `EnumValueMap<K, V>` → `Map<K, V>`, in an annotation (field / local / parameter / return / type argument) and in `new T<…>()`; `Info`, `--fix` rewrites it, `RiskyFix` (verified under a `compilerOracle`, report-only without one). `haxe.ds.WeakMap` is never touched — weakly-held entries are different semantics. A bare `new IntMap()` becomes `new Map()` only when an annotation pins the target type (`NewLiteral.pinnedByTypeHint`, shared with `prefer-map-literal`). Gates, all fail-closed: the short name must be PROVEN to mean the `haxe.ds` type (exact `import haxe.ds.IntMap;`, `import haxe.ds.*;`, or a fully-qualified reference — a module-declared, aliased or foreign-imported name is not reported at all); `Map` itself must be free (a file binding `Map` elsewhere yields a report-only finding); an `ObjectMap` / `EnumValueMap` key must send `Map`'s `@:multiType` chain (`K:String` → `K:Int` → `K:EnumValue` → `K:{}`) back to that same implementation, so `ObjectMap<Dynamic, V>` / `ObjectMap<String, V>` stay report-only (rewriting them yields a StringMap — it typechecks and throws at runtime); `#if` regions and non-empty constructor arguments are skipped. Positions where `Map` cannot stand — heritage, `is`, `cast`, `catch`, `(x : T)` and type-parameter constraints — are not reported: a concrete map class used AS a class is correct code. Imports are never rewritten (`unused-import` removes the dangling one on its own evidence, and `prefer-map-literal` then collapses an annotation-pinned `new Map()` to `[]`) |
| `prefer-index-access` | a `Map` read / write spelled as a method call replaceable with index access — `m.get(k)` → `m[k]`, `m.set(k, v)` → `m[k] = v`; `Info`, `--fix` rewrites it. Type-aware and it must be: index access `x[k]` / `x[k] = v` lives on Haxe's `Map` ABSTRACT only (its `@:arrayAccess` operators), so the concrete `haxe.ds.StringMap` / `IntMap` / `ObjectMap` — which carry `.get` / `.set` but NO array access — are safe misses; the receiver may be a plain identifier OR a path of plain field accesses over one (`this.m`, `obj.m`, `a.b.c`), whose final-segment outer-nominal type — the identifier's own declared type, or each path segment resolved cross-file through `SymbolIndex` — is a `RefShape.mapAbstractTypeNames` (`Map`) or a `RefShape.nullableWrapperTypeNames` (`Null`) unwrapping to one (`Null<Map<…>>`); an unresolvable receiver (never flagged without positive Map proof, since `[]` on a non-`Map` is a compile error), a `StringMap` / `IntMap` / other concrete-map path, or a link that is a call / index / `?.` is left alone; requires `plugin is TypeInfoProvider`. The cross-file resolution means the rule runs full-scope in the `--fix` fixed-point loop. A `get` needs exactly one argument, a `set` exactly two; the `set` rewrite is an assignment so `--fix` emits it only in statement position (a `set` used as an expression is flagged but not fixed). Nested matches (`m.get(n.get(k))`) fix the outer first, the inner on the next `--fix` pass |
| `prefer-interpolation` | a single-argument `Std.string(x)` replaceable with string interpolation (`'$x'` for a simple identifier, `'${expr}'` for any other interpolation-safe expression); `Info`, `--fix` rewrites it (an argument whose source carries a quote or `$` is left alone). A string-producing `+` CONCATENATION chain containing at least one string literal folds into ONE single-quoted interpolated string — `'<xml>' + xhtml + '</xml>'` → `'<xml>$xhtml</xml>'` — with the semantics-critical guards: operands BEFORE the first string literal keep their arithmetic grouping (`a + b + 'x'` → `'${a + b}x'`, never `'$a${b}x'`); `$` in literal parts escapes to `$$`; a double-quoted part merges with `'`→`\'` and `\"`→`"`; a bare identifier renders `$name`, any other operand `${expr}`; `Std.string(x)` inside a chain unwraps to `${x}`. Conservative skips: an operand already interpolating, an interp-source carrying `$` or a newline, a comment between operands, macro/reification contexts, and a pure-literal chain (`fold-adjacent-string-literals`'s job — no double report). Descends into `${…}` interpolation blocks: a call that is the block's sole expression is PEELED (`'v=${Std.string(x)}'` → `'v=${x}'`), a deeper one (ternary branch) nests `'${expr}'` — compile-verified at any depth. `RiskyFix`+`OracleRelaxable`: with a `compilerOracle` the null-safety gate drops (bare field access becomes a candidate) through the typecheck-and-revert pipeline; without one the safe subset applies unchanged |
| `split-var-declaration` | a local declaration binding several variables in one statement, splittable into one declaration per statement — `var uid:StringBuf = new StringBuf(), a:Int = 8;` → `var uid:StringBuf = new StringBuf();` + `var a:Int = 8;`; `Info`, `--fix` rebuilds it. Each declarator is copied verbatim from its own source slice, so the keyword, a per-declarator `:type` and a bare (uninitialized) declarator all survive; the split is semantics-neutral because the declarators already evaluate left to right and a later one may still read an earlier one. The candidate gate asks the AST for a continuation node, so a comma inside a generic type (`var m:Map<Int, String> = null;`) — or inside a declarator's own value (`var a = f(1, 2), b = 3;`) — is not a declarator separator, and the rebuild walks the RIGHT-RECURSIVE continuation chain to the end (in `var a = 1, b = 2, c = 3;` the node for `c` is a CHILD of the node for `b`) so no binding is dropped. Safe misses: a comment overlapping the statement (nothing would be DROPPED — the slices cover every byte but the commas and the `;` — but a DANGLING line comment before the separator would swallow the appended `;`, so the veto stays blunt), a `#if` directive overlapping it (precautionary — every shape that would break the slice arithmetic is already a parse error), a statement with no trailing `;` (`{ var a = 1, b = 2 }` as a block expression's value), a reification subtree (`opaqueKinds`, matching `prefer-final` and `unused-local` — splitting there is churn no downstream check would follow, and the reified `EVars([a, b])` would become two nodes a macro can observe), a declaration that is not a direct child of a statement list (the bare body of an `if` / `while`, or a `case` branch's statement run — the first two would move the second declaration outside the branch, the third is merely not reached), and the `@:meta var a = 1, b = 2;` form, which IS such a child but projects as an expression statement rather than a declaration. A comment AFTER the `;` is outside the replaced span and stays on the last emitted declaration. Does ONE job — splitting — and thereby unblocks two checks that skip a multi-declarator wholesale: `prefer-final` upgrades each never-reassigned binding on the next pass (so one `--fix` yields `final uid:StringBuf = new StringBuf(); var a:Int = 8;`) and `unused-local`'s refusal to cut a binding that shares its declaration stops applying. Reads the BRANCH-AWARE projection, so a multi-declarator inside a `#if` region is split too |
| `prefer-final` | a local `var x = …` never reassigned in its scope, replaceable with `final`; `Info`, `--fix` swaps `var`→`final`. Reassignment is detected with the scope-resolved write walker (complete — every write is a structural `=`/`+=`/`++` node), so the fix is always sound. Only a single `var` with an initializer is a candidate (a no-init or multi-declaration `var a = 1, b = 2` is skipped); a never-read `var` is left to `unused-local` (the read gate keeps the two from overlapping); a generic-typed `var x:Map<K, V>` is conservatively skipped (its type-parameter comma is indistinguishable from a declaration separator without tracking `<>`) |
| `prefer-ternary-return` | an `if (cond) return a;` whose immediately-following sibling is a `return b;`, collapsible to a single `return cond ? a : b;`; `Info`, `--fix` rewrites the pair. Only a no-else `if` that is a direct block statement with a value-returning then-branch (`return e;`, or a `{ … }` wrapping exactly one) and an immediately-following value `return` qualifies — a value-less `return;`, an intervening statement, or an inline non-block `if` is left alone. A pair whose one return is a boolean literal and the other is not a provably non-null `Bool` (`if (c) return true; return g();`) is left alone too — collapsing it would make a stuck `c ? true : g()` boolean ternary with no typer-free reduction; a fully-reducible boolean chain is `simplify-boolean-return-chain`'s job, a value ternary still collapses here. A null-narrowing guard condition refuses only a bool-literal collapse for the same reason (a value ternary keeps the in-condition narrowing under Strict). The condition is parenthesised only when it binds no tighter than `?:` (a ternary or an assignment); the `else`-form (`if (c) return a; else return b;`) is `redundant-else-after-return`'s job, which de-nests it into the form this check then collapses |
| `prefer-ternary-assignment` | an `if/else` whose two branches assign the SAME lvalue with a plain `=`, collapsible to a single assignment with a ternary RHS — `if (c) x.f = a; else x.f = b;` → `x.f = c ? a : b;`; `Info`, `--fix` rewrites it (also reducing the lvalue's evaluations from two to one). Each branch must be exactly one plain `=` assignment (braced or unbraced) to a textually identical (whitespace-normalized) lvalue; compound operators are excluded (a short-circuit `??=` would stop evaluating the condition when the lvalue is non-null, and a compound whose r-values don't unify to one type breaks the ternary — plain `=` flows the lvalue's type into both branches). An else-if chain is `prefer-if-expression-assignment` territory, a null-narrowing guard condition stays only when an r-value is a bool literal (that collapse hands off to `simplify-boolean-ternary`, whose boolean flattening loses the narrowing under Strict — a VALUE collapse keeps the narrowing and is allowed), and a comment in a dropped region is a safe miss. The condition is parenthesised only when it binds no tighter than `?:` |
| `return-reassign-ternary` | a function-tail `if (cond) x = e; return x;` — a single-branch `if` whose body is exactly one plain `=` to a LOCAL, immediately followed by a `return` of that same local — collapsible to `return cond ? e : x;`; `Info`, DEFAULT OFF (opt in via `apqlint.json` `"rules": { "return-reassign-ternary": { "enabled": true } }`, or an explicit `--rule`), `--fix` rewrites the pair. The conditional-reassignment sibling of `join-return` (which joins an UNCONDITIONAL `x = e; return x;`) and of `prefer-ternary-assignment` (which needs an `else`). FAIL-CLOSED GATES: no `else`; the body is ONE statement, a bare `x = e;` or a `{ x = e; }` wrapping exactly one, and a PLAIN `=` (a compound `+=` / `??=` is a distinct node kind, a member path `o.x = e` never resolves to a binding); the `return` is the IMMEDIATELY next sibling of the same statement list, returning the same bare identifier; `x` resolves through `Refs` to a LOCAL or PARAM, never a field (a bare field write can run a property setter whose side effect the collapse would drop); NO reference to `x`'s binding is CAPTURED by a nested function — a lambda, a named `function g() {}` or an `inline function` (`lambdaKinds` + `localFunctionKinds` + `inlineFunctionKinds`), *captured* meaning the reference sits inside a nested function that does not enclose the declaration, so a closure could observe the dropped store after the return (a reference in the nested function that OWNS the binding is not a capture, and a pair entirely local to a lambda still fires); the enclosing function declares an EXPLICIT RETURN TYPE (`TypeResolver.functionReturnTypeSource`) — in `x = e` the r-value is typed with `x`'s declared type as the EXPECTED type, and in the ternary that expected type comes from the `return` position instead, so an INFERRED return type loses it and `var x:Map<String, Int> = null; if (c) x = [];` would emit a `return c ? [] : x;` that does not typecheck; and the ONLY write to `x` in the collapsed region is that l-value — the r-value may READ `x` (`x = x + 1` evaluates before the conceptual write, exactly as the ternary's then-branch does), but a write to `x` in the CONDITION or inside `e` is refused. An r-value that already spans LINES (an object / array literal) is not flagged at all: it keeps its brace layout inside the ternary and strands the `: x` tail past the closing `}`, reading worse than the `if` it replaces — a multi-line CONDITION is fine, since the writer re-lays the whole ternary and the merge is what un-uglifies a condition the `if` header had to wrap. The condition is parenthesised only when it binds no tighter than `?:`. A comment the rebuild would drop (on the `if (` / `) ` glue, on `x =`, on the `;`, between the two statements, or on the `return`) keeps the site REPORT-ONLY — still flagged, with a message saying so, but no edit; a comment inside the condition, the r-value or the returned identifier rides along. COMPOSITION, no chaining inside the rule: once the tail merges, `x`'s only remaining write may be its initializer, and `prefer-final` then upgrades `var x = init;` to `final x = init;` on a LATER `--fix` pass — the counter-case being an accumulator (TM `src/api/API.hx`), where a `+=` in a preceding loop keeps `x` a `var` and only the tail merges |
| `prefer-if-expression-assignment` | an `if / else if / … / else` CHAIN whose every branch assigns the SAME lvalue with a plain `=`, collapsible to one assignment of an if-expression — `if (c) x.f = a; else if (d) x.f = b; else x.f = e;` → `x.f = if (c) a else if (d) b else e;`; `Info`, `--fix` rewrites it. DISJOINT from `prefer-ternary-assignment`: that owns the 2-branch case (→ ternary), this owns the chain (≥1 `else if`, terminating in a plain `else` — a chain with no final `else` is skipped). Each branch and the terminal must be exactly one plain `=` assignment (braced or unbraced) to a textually identical (whitespace-normalized) lvalue; a comment in a dropped region is a safe miss. Compound operators are excluded: a short-circuit `??=` would change behaviour (the conditions, now inside the skipped r-value, stop being evaluated when the lvalue is non-null) and an ordinary compound (`+=`) whose branch values don't unify to one type breaks the collapsed expression — plain `=` flows the lvalue's type into every branch. Unlike the ternary sibling a null-narrowing guard condition is KEPT and flagged — the `if (…)` condition is verbatim, so the branch keeps its narrowing under Strict |
| `prefer-if-expression-return` | the `return` sibling of `prefer-if-expression-assignment`: an `if / else if / … / else` CHAIN whose every branch is a valued `return`, collapsible to one `return` of an if-expression — `if (c) return a; else if (d) return b; else return e;` → `return if (c) a else if (d) b else e;`; `Info`, `--fix` rewrites it. DISJOINT from `prefer-ternary-return` (which handles the if/return + fall-through shape): only an explicit chain (≥1 `else if`, terminating in a plain `else`) of single valued-`return` branches qualifies; a bare `return;` (a distinct node kind) in any branch disqualifies, and a comment in a dropped region is a safe miss |
| `prefer-ternary-expression` | a 2-branch `if`-EXPRESSION already written in value position, rewritable as a ternary — `final s:Int = if (i > 1 \|\| !p.startsWith(':')) i else 1;` → `final s:Int = i > 1 \|\| !p.startsWith(':') ? i : 1;`; `Info`, `--fix` rewrites it. Closes the family's coverage gap: every other member matches a shape AROUND the `if` (an `if`/`else` STATEMENT assigning or returning, or a CHAIN of those), so an `if`-expression the author already wrote as a value — in an initializer, a `return`, a call argument — was flagged by nobody. Two gates, both correctness: the branches must come from `ternaryConditionUnwrapKinds`, the grammar's whitelist of kinds binding strictly tighter than `?:` (fail-closed by construction — a block, a nested `if`/`switch`/`try`, a control exit, a `macro`/`untyped` quotation, an assignment, an arrow lambda and a nested ternary are all absent, so none can reach a ternary arm); and the SLOT must accept a bare `?:` (`delimitedAllChildKinds`/`delimitedTailChildKinds`/a paren), because the `if`-expression being replaced is self-delimiting and a ternary is not — `var v = a \|\| if (c) x else y;` groups as `a \|\| (…)` but `a \|\| c ? x : y` groups as `(a \|\| c) ? x : y`, compiles, and returns a different value. Together they keep it off `prefer-if-expression-*` output: a chain HEAD's `else` is an `if`-expression (not in the whitelist) and a chain LINK's parent is that head (no delimited slot). A reification subtree is skipped wholesale; a comment in a dropped region is a safe miss. The replaced region stops at the else-branch, not at the node's end — an `if`-expression span runs on through its trailing trivia, and splicing that away welds the ternary's tail onto the next token (`q` + `else` → `qelse`, which still parses). The condition is parenthesised only when it binds no tighter than `?:` |
| `prefer-if-expression-chain` | a RIGHT-NESTED ternary chain of three or more values already written in value position, rewritable as the equivalent if-expression chain — `return a ? 1 : b ? 2 : 3;` → `return if (a) 1 else if (b) 2 else 3;`; `Info`, `--fix` rewrites it. The convergence half of the conditional canon (2 values → ternary, 3+ → if-expression chain, equality-shaped → `switch`): the statement-side rules already produce that canon from `if` STATEMENTS and `prefer-ternary-expression` produces the 2-branch half from an if-EXPRESSION, but a chain the author wrote as nested `?:` in the first place was seen by nobody. Disjoint from `prefer-ternary-expression` by RUNG COUNT, so the two cannot ping-pong — each shape is the other's 0-finding fixed point. Gates: a whitelisted host (`ifExpressionChainHostKinds` — a `return`, a local / member initializer, an assignment r-value, an arrow-lambda body), a readability whitelist rather than a correctness one, an `if`-expression being a legal expression atom wherever a ternary is; at least two conditions; at least one TERNARY rung (a pure if-chain is the canon and is left alone, a MIXED chain converges); no claim by `prefer-switch-expression`, asked directly rather than mirrored; no else-less conditional in a NON-terminal rung value, which would absorb the emitted ` else ` and silently re-parent the rest of the chain; and no comment in a dropped region, each copied span first cut back to its last TOKEN so a trailing `// …` cannot comment the ` else ` out |
| `prefer-try-expression-assignment` | a statement-position `try` whose body AND every catch clause is a single plain `=` to the SAME target, collapsible to one try-expression assignment — `var p:P = null; try { p = new P(a); } catch (e:String) { p = null; }` → `var p:P = try new P(a) catch (e:String) null;`; `Info`, `--fix` rewrites it. The `try` member of the expression-collapse family beside `prefer-if-expression-assignment` and `prefer-switch-expression-assignment`, and the decl-pairing sibling of `join-declaration-assignment`; the `var`→`final` upgrade is `prefer-final`'s job, so a full `--fix` yields `final p:P = try …`. Two arms: the DECL arm folds an immediately preceding single-variable mutable declaration (its initializer is dropped, so it must be side-effect-free, the pair must not sit inside another `try` — where an escaping exception is observable and the drop is no longer provably dead — and EVERY occurrence of the target in the `try` must be one of the collapsed assignments' own l-values, since after the fold the `try` IS the initializer: a write nested elsewhere, `p = go(() -> p = 5)`, uses a not-yet-initialized `p` just as a read would); the L-VALUE arm rewrites the `try` alone for an already-declared identifier or a field path with a side-effect-free receiver, and drops nothing, so it takes over whenever a DECL-arm gate declines (a gate on the `try` itself refuses both arms). A compound / `??=` assignment, an empty / rethrowing / multi-statement clause, disagreeing targets, and a comment in a dropped region are all safe misses. Because targets are unified by SOURCE TEXT, a clause whose exception variable SHADOWS the target is refused — it writes the shadow, a value the original discards and the collapse would promote. A DANGLING line comment refuses the site too — one with no newline after it within its own copied slice, since whatever the rebuild appends next (the following `catch (…)` header, the terminating `;`) would land behind the `//`; a `//` earlier in a multi-line slice is fine, the slice being copied newlines and all. A value whose RIGHT EDGE is an open `try` is PARENTHESISED (Haxe binds a trailing `catch` to the innermost open `try`, silently re-parenting the clauses after it); one sealed behind a closing bracket absorbs nothing and is left bare. Reads the BRANCH-AWARE projection, so a declaration and its `try` inside a `#if` region are still adjacent |
| `prefer-try-expression-return` | the `return` sibling of `prefer-try-expression-assignment`: a statement-position `try` whose body AND every catch clause is a single VALUED `return`, collapsible to one `return` of a try-expression — `try { return parse(t); } catch (e:String) { return 0; }` → `return try parse(t) catch (e:String) 0;`; `Info`, `--fix` rewrites it. A value-less `return;` (a distinct node kind), an empty / rethrowing / logging clause, a multi-statement body and a comment in a dropped region are safe misses — after the collapse such a path would have to produce a value it does not have. A DANGLING line comment is a safe miss too — one with no newline after it within its own copied slice (a `catch (e:T) // why` header, a `//` trailing the last line of an expression), since what the rebuild appends next would land behind it; a `//` earlier in a multi-line slice is fine. Each `catch (…)` header is sliced verbatim, so the exception variable's `:Type` (folded into trivia by the projection) survives exactly as written; a returned expression whose RIGHT EDGE is an open `try` is PARENTHESISED, since Haxe binds a trailing `catch` to the innermost open `try` and would otherwise re-parent every clause after it — a shape this rule's own fixed point manufactures; one sealed behind a closing bracket absorbs nothing and stays bare. Needs no statement-list walk, so it reaches a `try` in ANY position, `#if` regions included |
| `try-catch-null-guard` | a local declaration initialized by a try-EXPRESSION defaulting to bare `null` in every catch clause, immediately followed by an `if (x == null) return/throw` guard, collapsible by moving that terminator into every clause — `final p:P = try new P(a) catch (e:String) null; if (p == null) return;` → `final p:P = try new P(a) catch (e:String) return;`; `Info`, DEFAULT OFF, `--fix` rewrites it. Shape-wise the downstream of `prefer-try-expression-assignment`, whose output IS this declaration form; the keyword and `:type` are copied verbatim (the `var`→`final` upgrade is `prefer-final`'s, and a `Null<T>` annotation is left alone — narrowing a declared type is a null-flow concern, and the projection folds a local's type into trivia anyway). The load-bearing gate is that the try body is NON-NULL BY CONSTRUCTION, a WHITELIST (`new T(…)`, object / array / string literals) rather than a list of refusals: before the collapse a null body ALSO hit the guard and left the function, after it that null flows straight through — so a call, an identifier, a field read, a cast and every shape nobody has thought of yet fail closed. Two known holes need an abstract to reach and type information to close: `new T(…)` on an abstract whose inline constructor assigns a null, and an implicit `@:from` conversion on the declaration's annotated type — which is why the rule ships DEFAULT OFF. Further gates: every clause yields the BARE `null` (one that rethrows, logs, falls through or is empty refuses the site); the guard has no `else` and compares the declared name to `null` with `==` in either operand order; its body is exactly one function-leaving statement (a `break`/`continue` means nothing inside an initializer expression); the terminator neither reads the declared name (a self-reference in its own initializer) nor a name a catch clause BINDS — both asked through one predicate that ORs the scope-correct `Refs` lookup with a structural scan, since `Refs` does not project a BRACELESS `$x` interpolation read and `throw new Error('bad: $x')` slipped the self-reference gate on it alone (`catch (msg:String)` would capture a `return msg`, silently returning the caught message — the mirror of the assignment rule's shadowed-target gate, sharing its scan); and the terminator's right edge is not an open `try`, which a following `catch (…)` header would re-parent onto — a VALUE there is parenthesised, a `return`/`throw` cannot be, so the site is refused. A comment in a dropped region (the `= try` seam, each clause's `null`, the whole `if (…)` header, the guard's braces) is a safe miss, as is a DANGLING line comment in any copied slice, tested against the EMITTED region rather than the node's since both the declaration prefix and the terminator are rtrimmed. Reads the BRANCH-AWARE projection, so a declaration and its guard inside a `#if` region are still adjacent |
| `join-declaration-assignment` | a bare local declaration immediately followed by its first plain `=` assignment, joinable into an initialized declaration — `var x; x = e;` → `var x = e;`; `Info`, `--fix` rewrites it (keyword and `:type` preserved; the `var`→`final` upgrade is `prefer-final`'s job, so a full `--fix` yields `final x = e;`). Also finishes `prefer-if-expression-assignment` (which leaves `var x; x = if (…) …`). Only the IMMEDIATELY following assignment to the declared identifier itself qualifies (not `x.f =` / `x[i] =`, not a compound op); a declaration that already has an initializer, a statement between the two (evaluation would reorder), an r-value referencing the declared name (`var x; x = x + 1;` — a self-reference), a multi-declarator `var a, b;`, and a comment in a dropped region are all safe misses |
| `join-single-use-local` | a redundant intermediate local: an IMMUTABLE single-variable declaration whose initializer is a TRIVIAL PURE READ and whose one and only reference sits in the immediately following sibling statement, inlinable into that statement — `final logoBmd:Null<BitmapData> = _userLogoBmd; logoBmd?.dispose();` → `_userLogoBmd?.dispose();`; `Info`, `--fix` deletes the declaration line and substitutes the initializer at the read. Such a capture is usually residue of the strict-null-safety idiom (a FIELD does not narrow, so `if (x != null) x.dispose()` needed a local); once `prefer-safe-nav` rewrites the guard to `?.` the capture buys nothing, and a later `--fix` pass removes it. The third `join-` member beside `join-return` (which joins a declaration and its next RETURN) and `join-declaration-assignment` (its next ASSIGNMENT); registry order between them is free, since this rule refuses a next statement of exactly `return <name>;` — that pair is `join-return`'s, which owns the annotation-ascription decision. FAIL-CLOSED GATES: the declaration is a single-variable IMMUTABLE local (`localDeclKinds` minus `mutableLocalDeclKinds` minus `localDeclContinuationKinds`) with exactly one child, the initializer — a `var` is out of scope, it can be written between the capture and the read — and it OWNS its physical line (`RefactorSupport.lineExtendedSpan` must widen its span), since the fix deletes that line whole, so a shared line or a trailing comment refuses; the initializer is a bare identifier or a dotted field-read chain over one, a strict WHITELIST that refuses calls, `new`, index access, literals, casts, operators, lambdas and safe navigation (substituting `a?.b` into `x.c` turns an NPE into a silent short-circuit); the local has EXACTLY ONE reference and it is a Read (shared with `join-return` through `CheckScan.soleReferenceNameFrom` / `escapesConditionalRegion`, so a name declared in two `#if` branches and read past `#end` is refused); the name occurs NOWHERE ELSE past the declaration in the whole enclosing function other than at that one read — `Refs` indexes ordinary identifier references and nothing else, so a braceless `$name` string interpolation and a macro reification `$name` (`DollarIdentExpr`) each read a local invisibly and the fix would unbind the survivor, and rather than enumerate those channels (a negative list that leaks by category) the gate is inverted: any node carrying the name is a use unless provably not one (the read itself, or a kind whose `name` is a MEMBER — field / safe / force field access, object-literal key). The scan is scoped to the enclosing FUNCTION, not to the statement list: under the branch-aware projection that list may be a synthetic `CondBranch`, which is NOT a Haxe scope — a local declared inside `#if A … #end` stays visible past `#end`, and a `$name` read there would sit outside a block-scoped scan; no identifier the INITIALIZER reads is re-bound between the declaration and the read (`final x = n; try p catch (n:Dynamic) use(x);` would become `use(n)` reading the exception — output that COMPILES and is silently wrong); a bare read of a MUTABLE local refuses, since under strict null safety Haxe narrows an immutable binding but not a `var` a closure captures, so `final b = best; b?.span` compiles where the inlined `best?.span` does not — found by running the rule over its own source; conditional evaluation of the read (an `if` branch, a `&&` / `||` right operand, a ternary branch, a `case` body) is ACCEPTED for a bare-identifier initializer, whose read has no effect at all, and REFUSED (`RefShape.branchKinds`) for a field chain, where moving the read under a condition stops an eager null-dereference from throwing and stops a property getter's side effect from running — which is what protects the narrowing shape `final x = field; if (x != null) x.m();`, two reads, until `prefer-safe-nav` has collapsed it to one; that read sits in the IMMEDIATELY following sibling statement (an intervening statement could mutate the source); the read is evaluated AT MOST ONCE, EAGERLY — no node from that statement down to the read may be a nested function (a lambda / local `function` / `inline function` defers it past the eager capture) or a loop (statement, `do … while`, or the expression-position `for` / `while` of a comprehension — each re-evaluates the read while the body may mutate the source in between), while CONDITIONAL evaluation (an `if` branch, a `&&` / `\|\|` right operand, a ternary branch, a `case` body) is deliberately ACCEPTED, the initializer being a pure read; NOTHING IMPURE is evaluated before the read — every earlier sibling of every node on that path must be `RefactorSupport.isSideEffectFree` or itself a trivial pure read, so `_obj.m(x)` and `log(x, reset())` pass while `log(reset(), x)` refuses (an ancestor's own effect never matters: a call calls after its arguments); and the `:Type` annotation is not doing conversion work — unannotated always passes, annotated only when the annotation RE-STATES the initializer's own resolved declared type (`TypeResolver.identDeclaredTypeSource`, whitespace-insensitive), since an abstract `@:from` conversion (`final c:types.Color = str;`) or an upcast (`final b:Base = _derived;`) is real work the substitution would drop, and a field-access initializer has no ident binding to resolve so an annotated one is always refused. A comment intersecting the deleted line refuses; one BETWEEN the two statements, or anywhere in the next statement, rides along. NO PARENTHESISATION is needed or added — by the initializer gate the substituted text is always an identifier or a field-access chain, both maximal-precedence primaries |
| `prefer-single-quotes` | a double-quoted string literal that can use single quotes (the Haxe default); `Info`, `--fix` swaps the delimiters. A literal whose content has a `$` (the double quotes deliberately suppress interpolation that single quotes would trigger) or a `'` (which would terminate the single-quoted form) is left alone; every other escape (`\"`, `\n`, …) stays valid verbatim, so only the two delimiter characters change. Without types the rare case of a macro that branches on a literal's quote kind cannot be detected — hence `Info` + opt-in `--fix`, never auto-applied. Disabled for a file whose `checkstyle.json` `StringLiteral.policy` prefers double quotes (`onlyDouble` / `doubleAndInterpolation`) |
| `simplify-boolean-ternary` | a ternary with a boolean-literal branch that reduces to plain boolean logic — `cond ? false : x` → `!cond && x`, `cond ? x : true` → `!cond \|\| x`, `cond ? true : false` → `cond`, and the mirror forms; `Info`, `--fix` rewrites it. Any negation is pushed inward by De Morgan (`!(a == null \|\| b == null)` → `a != null && b != null`) so no `!( … )` is left over a compound, and operands are parenthesised only where precedence requires. A real-valued ternary, or one with the same literal both sides (collapsing would drop `cond`'s side effect), is left alone. Composes with `prefer-ternary-return` through the `--fix` fixed-point loop; a guard chain whose conditions are provably non-null `Bool` collapses to one flat boolean `return`, while one whose conditions are a `Call` / `switch` (unprovable without a typer) is reduced on the guard form by `simplify-boolean-return-chain` instead. The negation is ORDER-GATED by the same one licence the guard family uses: an ordered comparison (`< <= > >=`) flips (`(x < 0) ? false : p == true` -> `x >= 0 && p == true`) only when the run's type resolver proves BOTH operands drawn from a value set `<` orders TOTALLY — `Int` / `UInt`, or a totally ordered literal — and otherwise keeps the sound `!(s < t)` wrap, since `!(a < b)` and `a >= b` differ whenever an operand is a `null` (Haxe has no non-nullable `String`) or a NaN (`Float`). Unlike `guard-return` / `guard-continue`, a DECLINED flip does not suppress the finding: this rule eliminates a ternary rather than inverting a condition for readability, and a `!( … )` operand is already its normal output for any opaque condition (`f() ? false : x` -> `!f() && x`), so the wrap keeps a real simplification a refusal would throw away. Until that gate landed the rule flipped UNCONDITIONALLY and miscompiled `(s < t) ? false : a && b` for a null `s:String` — `true` before, `false` after (measured on `--interp` and `js`, Haxe 4.3.7) |
| `simplify-boolean-return-chain` | a contiguous run of two-or-more `if (cond) return true/false;` guards (no `else`, each boolean return bare or a `{ … }` wrapping exactly one) closed by a final `return true/false;`, reducible to one flat boolean `return` — `if (a) return true; if (b) return true; return false;` → `return a \|\| b;` (`!a && !b` / `a \|\| !b` by De Morgan); `Info`, `--fix` rewrites it. It reduces on the guard form, where each `cond` is an `if` condition (non-null `Bool` under strict null-safety, since the source already compiles) — so joining with `\|\|` / `&&` is sound and conditions are kept verbatim (an `if (a() == true)` stays `a() == true \|\| …`), succeeding where the ternary path stalls on a `Call` / `switch` condition. Registered before `prefer-ternary-return` so it reaches the chain first; a degenerate chain that would drop a condition's evaluation is left alone. A NEGATED guard condition (`if (cond) return false;`) is inverted through the same order-gated engine as `simplify-boolean-ternary`: an ordered comparison flips only where the type resolver proves both operands totally ordered by `<` (`Int` / `UInt`, or a totally ordered literal), else it keeps the `!(s < t)` wrap — and a declined flip still reduces the chain rather than refusing it. A POSITIVE guard (`return true`) is never negated, so its operand types are moot |
| `invert-negated-if-else` | an `if` STATEMENT whose condition's top-level node is a logical NOT and which carries an `else` — `if (!c) A else B` — inverted to `if (c) B else A`; `Info`, `--fix` drops the `!` and swaps the two branch source slots verbatim (braces, layout and comments travel with their branch). A pure branch swap is an exact complement, semantics-safe under every value including NaN (unlike a De Morgan rewrite). Only a literal top-level `!` fires — never `!=`, never a negated `&&`/`||` operand; a redundant paren around the not's operand becomes the plain condition (`!(a && b)` → `a && b`). Safe misses: a no-else `if`, an else-if chain (the swap would restructure it), a condition-comment the rebuild would drop. Chains with `comparison-to-boolean` through the `--fix` fixed point: `x == false` → `!x` → swapped away |
| `assignment-in-condition` | an assignment (`=`) used as a condition — `if (a = b)`, `while (a = b)`, `do … while (a = b)` — almost always a `==` typo (a Haxe condition must be `Bool`, so the assign even compiles only in the narrow case that matches a typo); `Warning`, report-only (`=` vs `==` is the author's intent). The condition slot is pinned by position (first child for `if` / `while`, last for `do … while`, unwrapping one paren layer), so an assignment in a branch body (`if (c) x = y`) is not flagged |
| `duplicate-ternary-branches` | a ternary whose then- and else-branches are textually identical (`cond ? x : x`), so it always yields `x` and the condition is dead; `Warning`. `--fix` collapses it to the branch only when `cond` is side-effect-free; a side-effecting condition (`f() ? x : x`) is report-only |
| `prefer-bind` | a zero-parameter arrow lambda wrapping a single call with arguments (`() -> f(a, b)`), replaceable with the partial application `f.bind(a, b)`; `Info`, `--fix` rewrites it. A parameter-bearing lambda (`x -> f(x)`), a block body, and a zero-argument `() -> f()` are left alone. `.bind` evaluates the callee and arguments at bind time rather than call time, so the check fires ONLY when every argument is a stable value (literal, non-interpolated string, identifier / dotted chain, negated numeric literal) — an argument that computes or allocates (`new X()`, a call, `'id-$x'`, `a + 1`) keeps the lambda, whose deferred evaluation is load-bearing |
| `prefer-arrow-callback` | an anonymous `function` literal in CALL-ARGUMENT position (`f(function() { … })`, `new T(function(x) { … })`), replaceable with the arrow form (`f(() -> …)`); `Info`. Argument position only — a local `function name() {}` statement and a `var f = function() {}` initializer are out of scope by policy. `--fix` is gated on the silent type-drift hazard (an arrow implicitly returns its last expression, so in a generic position like `array.map` the conversion changes the inferred type parameter while still compiling): a type-preserving body (explicit `return` tail, syntactically-`Void` tail, empty body — a `throw` tail is NOT preserving, the arrow form infers an open monomorph) converts structurally; an open tail converts only when the callee's parameter resolves through the SymbolIndex (member / static / constructor / local binding, supertype chain and typedef aliases walked) to a function type returning literal `Void`; a return that resolves to a callee type PARAMETER is proven generic and not reported at all; an unresolvable callee stays report-only. A single-expression or single-`return` block collapses to the bare-expression arrow; a stranded comment blocks the rewrite |
| `prefer-lambda-expression-body` | an ARROW lambda whose `{ … }` body holds exactly one statement — a value `return e;`, a bare expression `e;`, or a control-flow statement (`if` with or without `else`, `switch`, `for`, `while`, `throw`) — collapsible to an expression body: `(a, b) -> { return a - b; }` → `(a, b) -> a - b`, `() -> { if (c) f(); }` → `() -> if (c) f()`; `Info`, `--fix` rewrites it. Type-preserving by construction: a Haxe block IS an expression whose value is its last expression, so `(v) -> { push(v); }` already has the same type as `(v) -> push(v)`, and every accepted control-flow form is itself an expression (`() -> { if (c) 1 else 2; }` and `() -> if (c) 1 else 2` both yield `1`) — none of the expected-type machinery `prefer-arrow-callback` needs applies here. The `function` literal is deliberately NOT matched: `prefer-arrow-callback` owns that node and normalises it INTO an arrow form, which this check then collapses on the next fixed-point pass, so one site is never reported by both. A value-less `return;`, a declaration, a `do … while`, a `break` / `continue`, two statements and a `#if` region (one `Conditional` child, no accepted kind) all fail closed; a comment anywhere in the block but outside the copied expression blocks the rewrite. The emitted control-flow text drops the statement's own terminator STRUCTURALLY (a node whose span ends where its last child's does hands the question down; a terminator-bearing kind yields to its child's end) — an interior `;` before `else` survives, a bare `switch`'s `}` survives, and a shape whose terminator is not recoverable (`while (c);`, `if (c) return;`, or a `#if` region on the tail, whose `#end` is mandatory and whose `;` sits ahead of it) is refused. A lambda reachable at the tail of the brace-less then-branch of an `if` that HAS an `else` is refused when its body holds an else-less conditional: `if (x) cb = () -> { if (c) g(); }; else h();` collapsed would re-parent `else h()` onto the inner `if`, a semantics break whose output still parses. A `#if` region reaches that position too — its branches project as flat siblings, so a following sibling proves no separating token there and every child inherits the region's own exposure. The edit replaces the BLOCK's span only, so trailing trivia is untouched |
| `prefer-local-function` | a `function` literal BOUND to a local, rewritten into a local function DECLARATION sitting where the literal already was — `var handler:MouseEvent->Void; o.addEventListener(E.DOWN, handler = function(event:MouseEvent):Void { … }, true, 0);` → `function handler(event:MouseEvent):Void { … }` + `o.addEventListener(E.DOWN, handler, true, 0);`; `Info`, `--fix` rewrites it. Two entry shapes, one emitter: an ASSIGNMENT `name = <literal>` reached from a statement without crossing a `branchKinds` / `scopeKinds` node (so the literal is built exactly once whenever that statement runs) paired with an earlier BARE declaration of `name` in the same statement list, and a DECLARATION whose initializer IS the literal (`var g = function(x:Int):Int return x;`). Disjoint from `prefer-arrow-callback` / `prefer-lambda-expression-body` by construction: those own a literal that is a DIRECT call argument, and an assignment / declaration node always sits between here — which is exactly why the shape went uncovered. The gates come from the two ways a local function is NOT a variable: it cannot be reassigned, and it is invisible ABOVE its own declaration. Over the statement list that owns the binding (the exact extent of a Haxe local's scope) the check requires `name` to occur NOWHERE before the point the declaration moves to — this is what refuses the mutually-recursive-closure idiom (`var a = cast null; a = function() { … b … };`), whose whole reason for the `var` is the forward reference a declaration cannot express — to be WRITTEN only by the flagged assignment, and to be DECLARED exactly once. The declaration must be BARE or hold only the definite-assignment placeholder (`null`, `cast null`); a multi-declarator `var a, b;` never qualifies. EVERY parameter of the literal must carry a type: the expected type the declaration's annotation supplied is gone after the hoist, and an unannotated parameter whose body dereferences it no longer types (the RETURN needs no gate — a `function` literal with no explicit `return` is `Void` in both forms; a rest parameter is refused outright). The declaration's own annotation must be one the literal's signature REPRODUCES — absent, or a written FUNCTION type (a top-level `->`, so `Array<Int->Void>` / `Null<Void->Void>` do not count). A NOMINAL one is dropped silently and compilably: Pony's `var l:Listener1<Int> = null; l = function(n:Int):Void { … }` binds an abstract whose `@:from` wraps the literal ONCE, at the assignment, and hoisted, `l` is the raw function that every use site wraps again — so an `add(l)` / `remove(l)` pair that shared one wrapper stops doing so. When the assignment IS the whole statement the declaration REPLACES it (rewriting only the assignment would leave a `name;` no-op); an expression body regains the `;` its statement carried. A comment in a dropped span (the declaration statement, the `name = ` head, the initializer's `;`) blocks the rewrite, while one INSIDE the literal's body rides along with it; `opaqueKinds` subtrees are skipped whole. Composes with `join-declaration-assignment`, which claims the adjacent `var f; f = function(){};` pair first — this rule's edits are then deferred one fixed-point pass and land on the joined form |
| `redundant-map-iter-key` | a key-value `for` loop that discards its key with `_` (`for (_ => v in m)`), replaceable with `for (v in m)` since Haxe iterates values by default; `Info`, `--fix` drops the `_ => ` prefix. A value-only `for (_ in m)` (no `=>`) is a legitimate ignore-value loop and is left alone |
| `prefer-switch` | an `if` / `else if` chain testing one expression against literal values (`if (x == 'a') … else if (x == 'b') …`), which reads more clearly as a `switch`; `Info`, `--fix` rewrites the chain to a `switch` (each rung's literal becomes a `case`, the trailing `else` a `case _`, then-branch bodies carried verbatim). Flagged only when every rung is an equality (`==`) comparing the *same* discriminant against a constant literal (int / float / bool / null / non-interpolated string), the discriminant is call-free (a `switch` evaluates it once where the chain evaluates it per rung — a behaviour change), and there are at least two rungs. A `!=` chain, a non-equality or mixed condition, differing discriminants, and a non-literal or interpolated operand are left alone. A chain inside a `macro …` quotation is dropped (`ReificationScan`, likewise for the `prefer-switch-expression` / `-assignment` and `prefer-case-guard` siblings) — quoted source is AST the macro builds, and `EIf` → `ESwitch` is a different tree |
| `missing-visibility` | a class / abstract member declared without an explicit `public` or `private` modifier; `Warning`. Interface members (implicitly public) and enum-abstract values are exempt. Haxe defaults an unmodified member to `private`, so the omission is not a bug — but stating visibility on every member is a documented project rule. Members written inside a member-position `#if … #else … #end` are scanned too, branch by branch (the grammar emits one node with every branch's members flattened; boundaries are recovered from the directive text, as `member-order` does), and a region holding nothing but a visibility keyword is treated as a modifier for the member after `#end`, not as a region of its own — which also means a keyword reaching into a branch EXEMPTS its member rather than proving it public. A `#if` splitting a member's own signature rather than listing whole members parses as a straddling `CondSplice*` form and stays uncovered. `--fix` inserts `private` (the Haxe default, so a behaviour-preserving change) at the canonical position, after any `override` / meta and before `static` / `inline` — inside the member's own branch when it is `#if`-guarded; an `override` member instead gets the overridden member's own keyword, resolved through the SymbolIndex supertype closure (unresolvable — unindexed / colliding / unmarked base — stays report-only). An **`extern` class is not reported at all**: its unmodified members are already `public`, so `private` would break every caller and `public` is noise on a bodyless foreign-API declaration — the finding could only be "fixed" wrongly. A `@:publicFields` class IS reported (an ordinary class where `private` on one member is a real opt-out) but skipped by `--fix`, which would otherwise lower visibility |
| `modifier-order` | a member whose modifier keywords are not in the canonical order `override` → `public` / `private` → `static` → `inline` → `final`; `Info`, `--fix` reorders the run (each ranked keyword moves to its slot; unranked modifiers stay in place). A method's `final` (the grammar folds it into the `final function` wrapper rather than a sibling node) ranks last, so `final` before any other modifier is flagged; a field's `final` is the storage keyword, not a modifier, and is never ranked. Purely cosmetic — the order carries no meaning to the compiler. Modifiers with no documented order (`extern`, `dynamic`, `macro`, …) are ignored. A project `checkstyle.json` `ModifierOrder.modifiers` overrides the canonical order (mapped to the ranked kinds; `PUBLIC_PRIVATE` expands to public/private, `FINAL` to final, unranked tokens dropped) |
| `member-order` | a type whose members are not in the canonical declaration order; `Info`, `--fix` reorders them. The order: constants (static fields, public then private; not sub-split by property-ness), instance fields (public read-only properties `(default, null/never)`, public getter properties `(get, …)`/`(dynamic, …)`, public final, public var, then the same private quartet — property classification via `TypeInfoProvider.propertyAccessors`, so a grammar without the provider keeps the plain final/var split), constructor, property accessors (`get_*`/`set_*`), instance methods (public then private), static methods (public then private). Within one rank plain unconditional members carry a sub-order: `inline` members lead, then fields WITH a declaration-site initializer lead init-less ones; the accessor rank is exempt, so a get/set pair keeps its adjacency. Rank groups must be separated by EXACTLY one blank line; same-rank FIELDS are unseparated unless either neighbor's slot is comment-led (the writer inserts a blank after a doc-commented member — the check and fixer both honor that policy); same-rank METHODS keep their blank separators untouched. The autofix rebuilds the member region with normalized blanks; when non-whitespace sits between member slots (a stray `;` — `EmptySemiMember` is not a collected member) it falls back to plain slot swaps and the spacing check is disabled for that container, so every reported violation stays fixable. The sibling of `modifier-order` one level up — that orders one member's keywords, this orders one type's members (the same slot-permutation). Reordering METHODS is always safe; reordering FIELDS can change init order, so the autofix BAILS a container (report-only) when the canonical sort would change the relative order of any side-effecting field initializer, or move a field before a sibling it reads — in both bails counting only INITIALIZED, non-`inline` partners: an init-less field runs no code in the init phase, and an `inline` field's initializer is a language-mandated constant folded at compile time, so crossing either is unobservable — but only among fields of the SAME init phase (static fields init at class-load, instance fields in the constructor, so a static const reorders freely past instance fields). Pure literal consts (even many at one rank) reorder freely under the stable sort. Members guarded by `#if` conditional-compilation blocks reorder too: each member is collected with the condition it is declared under (nested `#if` conditions conjoin, an identical conjunct deduping), and the autofix regroups the sorted members under regenerated `#if … #end` directives, coalescing every member of one condition into a SINGLE block per section (a condition guarding both fields and methods therefore yields two independent blocks, each ranked on its own). Such a block moves as ONE atomic unit but sorts by its CONTENT: when every member of the block carries the same rank, the block sorts AT that rank among the plain members of its section (trailing them within the rank — crossing a rank boundary is the point, position inside one rank is not), so a guarded `public var` no longer trails the private instance fields it outranks. Content ranking is gated three ways and any doubt PINS the block back to the section END (conditional fields right before the constructor, conditional methods at the bottom of the methods section, first-occurrence order between blocks, each internally rank-sorted — the pre-existing placement): (1) all members of the block must share ONE rank — a mixed-rank block would have to be split, and atomicity beats ordering; (2) every byte of the conditional construct must be covered by a member slot, an absorbed lead doc, a REGENERABLE directive line or whitespace, and the `#end` line must end there — anything else (a stray `;` — `EmptySemiMember` is not a collected member; opaque region text; a note the rebuild cannot regenerate, on a directive line like `#if X // why` or after the `#end`) would be dropped, duplicated or re-attached to the wrong member by the rebuild, so it pins the block AND bails the whole container's reorder to the spacing-only fallback; (3) no field initializer may tie the block to its position, in EITHER direction — a field IN the block whose initializer has a side effect (call / allocation / assignment) or reads another field of the container, or a field OUTSIDE it whose initializer reads one inside — deliberately independent of the `movableArglessNew` option, since the report path resolves no per-file config and an option-dependent rank would make report and fix disagree. A gate on position-sensitive constructs in the `#if` CONDITION itself is a documented no-op for this grammar (a Haxe conditional-compilation condition is a pure compile-time define expression evaluated before parsing). One residual report-only case is specific to content ranking: a moved block that would flip with a same-phase side-effecting UNCONDITIONAL initializer is flagged and then bails to spacing-only. Demoting the block and re-sorting would close it, but the sole trigger is the one bail `movableArglessNew` relaxes, so a retry would reintroduce exactly the report/fix option disagreement the config-independent gates exist to prevent — and the finding is the same advisory shape a plain unsafe container already produces. A doc comment written before a member's `#if` travels with it inside the regenerated block. A blank line is required BEFORE a member-level `#if` and AFTER its `#end` (a directive gluing to `public function new(` masked the fields→ctor group boundary before). A BRANCHED construct (`#if` / `#elseif` / `#else` / `#end`) moves as one unit too: the parse tree flattens every branch into a single node, so the branch each member was declared in is recovered from the construct's own directive lines and becomes part of its sort key — branch before rank, so the branches keep source order and each sorts internally, and the directives are regenerated rather than dropped. Two constructs merge into one block only when their whole branch shape matches, and a doc written above a branched `#if` stays above the regenerated `#if` (it describes every branch, not just the first). It still bails — report-only — on an `#else` shape the branch model refuses: a nested conditional inside a branch, a construct whose members span two sections, one whose first branch is empty, or an orphan comment stranded in a directive gap. The REPORT also resets its rank comparison when crossing an `#else`/`#elseif` — an alternative branch is a sibling sequence, not a successor, so a container whose every branch is itself canonical is not flagged |
| `fragmented-doc-comment` | a declaration documented by SEVERAL adjacent doc-comment blocks (each opened and closed separately, on consecutive lines) instead of one — a confusing duplicate, usually from a doc edit that inserted a second block rather than replacing the first; `Info`, `--fix` merges the run into a single doc comment (block bodies concatenated). A pure comment-token scan (comments are dropped from the query projection). Only `/**` DOC blocks join a run: a plain block (license header, section banner), a blank line, a line comment, or any code between blocks breaks it — so license/section comments are never absorbed. Behaviour-safe (comments never affect compilation) |
| `empty-doc-tag` | a javadoc/haxedoc tag with no description inside a CLOSED `/** */` doc comment — content the doc extractor renders as an empty row; `Warning`, `--fix` deletes the tag's line(s), or the whole comment when nothing but gutter would survive. A pure comment-token scan (`RefactorSupport.collectCommentTokens`, string-aware; plain `/* */` blocks and unclosed `/**` never qualify). A POSITIVE whitelist, so anything else is kept by construction: `@param` bare or NAME-ONLY (the name restates the signature), bare `@return` / `@returns`, bare `@throws` / `@exception` (`@throws IOError` is KEPT — the type is information a Haxe signature does not carry), bare `@see` — and each only when every CONTINUATION line up to the next tag / interior end is content-free, so a description on the following line protects the tag. The tag name must be followed by whitespace or line end (`@param2` is not `@param`), and a tag is a LINE-START construct — a mid-line `@word` in prose, `{@link …}`, and doxygen's `@param[in]` never match. Detection and deletion deliberately use DIFFERENT gutter predicates: detection's is permissive (whitespace + any star run, so a tag "documented" only by a `***` divider is still empty), deletion's is strict (at most ONE star) — a deliberate `*`-divider line is never deleted and blocks whole-comment deletion; when `*/` shares the last flagged line, only that line's content is deleted and the closer stays. Residual interplay: the fix can reduce a doc to a divider-only or empty interior, which `empty-comment` then legitimately removes on the next fixed-point pass, and deleting a member's only doc (a lone `/** @return */`) converts a `doc-coverage`-satisfied member into a `doc-coverage` finding — intended, it unmasks a real gap (no oscillation: `doc-coverage` has no fix). Remaining doc re-flow (one-line collapse) is the canonicalizer's job, not the rule's. A pure trivia edit: comments never affect compilation |
| `misplaced-type-doc` | a `/**` doc block written between the `package` statement and the module's `import` / `using` lines — the compiler attaches a doc to the declaration it IMMEDIATELY precedes, so a block the import run separates from the module's type documents nothing: a DEAD doc, invisible to every IDE and doc generator, that still reads as documentation to the next human. `Warning`, `--fix` MOVES the block verbatim into the type declaration's leading trivia (above its `@:meta` / modifier run, where a type's doc is read), supplying the separating blank line when the anchor lacks one. The comment text is never reflowed or rewritten — a trivia MOVE, so the writer's comment guard sees the same block leave one slot and arrive in another. Import layout is what strands the doc and what keeps stranding it (`AddImport` splices a new plain import at its ordered slot, which for an import sorting first is directly below the block), so the rule is registered ahead of the import rules. Gates, each with a negative fixture: only a `/**` block with a non-blank body (a plain `/* … */` banner and a `//` run are license headers and section labels); the type must carry no doc of its own (two docs are never merged); EXACTLY ONE top-level type (with two, which one the doc was written for is a guess — `enum abstract` counts, though `RefShape.typeDeclKinds` omits it); a block ABOVE the `package` statement is a FILE header and is never touched (so a module with no package statement never fires); exactly ONE doc block in the gap (two adjacent ones are `fragmented-doc-comment`'s job, and this rule fires on the merged block at the next pass); and the block must OWN its lines, else the finding stays report-only. The file-header gate is POSITIONAL by design — it asks where the block was written, not what it says — so an attribution note below the package statement reads as a type doc and moves onto the type. That judgement about an author's intent is why the rule is DEFAULT OFF: opt in with `"misplaced-type-doc": { "enabled": true }` |
| `prefer-line-comment` | a `/* … */` BLOCK comment sitting in a STATEMENT position inside a function body — prose that reads as a line comment, written with block delimiters; `Info`, `--fix` rewrites it to `//` line comments. DEFAULT OFF (`DefaultOff`): block-vs-line comment style is a project convention, not a defect — opt in with `"rules": { "prefer-line-comment": { "enabled": true } }`. A comment-token scan (`RefactorSupport.collectCommentTokens`, string-aware, so a `/*` inside a STRING literal is never visited) crossed with the parse tree. Three positive shapes qualify: a WHOLE-LINE block, a TRAILING block (`g(); /* why */` → `g(); // why`), and a MULTI-LINE block whose delimiters own their lines. Each also needs the STATEMENT-POSITION gate — the innermost node enclosing the token must be a statement block (`RefShape.blockBodyKind` / `blockStmtKind`), which is what keeps a DECLARATION's doc out of scope (its enclosing node is the type body, not a block) and what rejects a comment nested in an EXPRESSION, whose conversion could let the writer re-join the following operand onto the commented-out line (`foo(a, /* x */` with the `b);` on the next line passes every textual test and is refused here) — plus the hard textual gate that the closing delimiter be the last non-whitespace on its line, or a `//` would swallow the code after it (this is what rejects the mid-expression `foo(a /* x */, b)`). Two shapes are deliberately ceded: a CONTENT-FREE comment (only whitespace and stars) belongs to `empty-comment`, whose fix DELETES rather than converts — converting it would strand an un-deletable `// *`; and a `/**` block immediately above a LOCAL FUNCTION declaration is a real doc for that function, the in-body analogue of the member doc the position gate already excludes (a PLAIN block above one is prose and still converts). The fix writes each body line as `// <text>`: delimiters stripped — including the star run of a `/***` open or a `**/` close, so neither leaks into the body — the block's own indent re-emitted before each continuation line, the file's own line terminator preserved (`\r\n` stays `\r\n`), and the body's COMMON leading whitespace removed so its relative indentation survives, with the CLOSING line excluded from that common prefix (it carries the wrap's structural indent, not the body's) — the same two decisions `BlockCommentNormalizer.normalize` makes. A leading `*` is CONTENT unless the block is javadoc-STYLED (three or more lines whose every non-blank interior line opens with `*`, the writer's own `isJavadocStyle` predicate), so the single-line `/* * 2 */` keeps its star (→ `// * 2`) while a gutter block loses its column — the SHAPE decides, not the open delimiter, so a plain `/*` gutter block is stripped too. A pure trivia edit: comments never affect compilation |
| `prefer-doc-comment` | a `//` comment that documents a type or member declaration — prose in the one comment form no doc extractor reads; `Info`, `--fix` turns it into a doc comment at the declaration's doc anchor. TWO comment sources: an ABOVE-LINE RUN of consecutive whole-line `//` comments immediately above the declaration (rewritten in place), and a TRAILING DECL COMMENT at the end of the declaration's own line (`public final startTime:Date = Date.now(); // when this session started`), which is RELOCATED — the tail is cut back to the code's last token and the doc inserted above the declaration's `@:meta` / modifier run. A trailing comment's ownership is positional and unambiguous, so the label gates (2, 3, 10, 11) do not apply to it; it has its own position gates instead — the declaration must be ANCHORED on that line (a continuation line of a wrapped declaration carries no anchor) and be the ONLY one there (`var a; var b; // note` has no single owner), the code before the comment must END the declaration with `;` or OPEN its body with `{` (a closing `}`, a `case` colon and a statement inside a body all fail to carry an anchor), and a `//` run directly above the declaration wins, so the two sources never stack two docs. The content gates (4-9) are shared. The gates below are stated for the run source. The inverse direction of `prefer-line-comment`, and its complement: that rule pulls a block comment OUT of a statement position, this one lifts a line comment INTO the doc slot, and neither can fire on what the other converts. DEFAULT OFF (`DefaultOff`): whether a note above a declaration is documentation or an aside is an authoring judgement — opt in with `"rules": { "prefer-doc-comment": { "enabled": true } }`. Detection is a comment-token scan (`RefactorSupport.collectCommentTokens`, string-aware) crossed with the parse tree: consecutive WHOLE-LINE `//` tokens group into a RUN, and the run qualifies when the line right after it is the declaration's first line — where "first line" is the start of the leading `@:meta` / modifier run, the same doc anchor `doc-coverage` and `misplaced-type-doc` compute (`RefShape.typeDeclKinds` + `memberDeclKinds` + `enumAbstractDeclKind`; with none of them set the check is a no-op, and a kind outside those sets — a module-level function, an enum constructor — is not an anchor). Eleven gates: (1) WHOLE-LINE OWNERSHIP — a trailing `// note` after code is a remark about that line and never joins a run; (2) ADJACENCY — zero blank lines between run and declaration, which is also what keeps a run sitting above an EXISTING doc out; (3) INDENT MATCH — every run line carries the declaration's own indent; (4) NO EXISTING DOC — a `/**` block directly above the run means the declaration is already documented, and two docs are never merged (that is `fragmented-doc-comment`'s shape); (5) NO TOOLING DIRECTIVE — `noqa`, `CHECKSTYLE:`, `@formatter:`, `#region` / `#endregion` skip the run WHOLE, since burying a machine instruction in a doc block both silences it and documents nothing; (6) NO TASK MARKER — `TODO` / `FIXME` / `HACK` / `XXX` anywhere in the run (case-insensitive), a reminder to change the code not a description of it; (7) READS AS PROSE — a POSITIVE criterion rather than a list of commented-out shapes to dodge (a negative list leaks: every shape left off it converts silently): the line's first word must not be a declaration / statement keyword, nor a control keyword with the construct's `(` or `{` behind it, and the line must carry no `=` or `@` and no punctuation in a CODE POSITION: a `;` with nothing but a trailing `//` behind it (a statement terminator — a prose semicolon joins two clauses, `… filenames in NFD; servers use NFC.`), a `{` ending the line or a `}` opening or ending it (block delimiters — `Returns {x, y} in stage space.` is prose), an identifier glued to a `(` (a call — prose puts a space before a bracket) and an identifier glued to a `:` that an identifier follows (a type annotation — a prose colon is followed by a space or ends the line). The keyword match is CASE-SENSITIVE, so `If unset, …` and `for internal use only` stay prose while `if (b) {` and `for (x in xs)` do not; (8) NOT a `///` RUN — triple-slash is a section-label convention; (9) NOT SECTION DECORATION — a run drawn only from `-=*_#`, or a DECORATED label (`// --- Mobile touch ---`: rule characters on both ends around at most four words); (10) NOT A SECTION LABEL — a note above a RUN of siblings labels the run (`// Button` over four constants), and attaching it to sibling #1 as that member's haxedoc both misdescribes the member and loses the label; two layouts express a run, so there are two readings — siblings on CONSECUTIVE lines (the blank-line group, where nesting in either direction is not sibling-hood) and blank-line-SEPARATED siblings, where the section ends at the next label — or at a separately-documented sibling, whose own doc proves the run cannot label a span across it — rather than at the next blank line, and only same-owner same-kind siblings count; (11) NOT THE ENCLOSING TYPE'S HEADING CONVENTION — a type that writes section headings also writes SINGLE-member ones (`// Slider` over the one constant that needs it) which no shape test can tell from a doc, so that type's own statistics decide: in each declaration body the runs proven to label a section are counted against those over a lone declaration, and a strict majority of the former makes every run in that body a heading. Sibling-hood and the tally are both bounded by the ENCLOSING declaration's span, never by indent — two types in one module hold their members at the same depth, so an indent-bounded scan read one type's members as another's and let a constants table's headings suppress a sibling model type's genuine docs. One grouping among nine per-member docs is an author grouping two related fields, not a convention. Every gate is ablation-verified by a fixture that flips when it alone is removed, except adjacency (2), which is enforced by construction (the anchor index is keyed on the line right after the run) and is covered jointly with the indent gate. On the consuming project the label gates take the finding count from 120 to 29, of which 2 are label-shaped. A content-free run is ceded to `empty-comment`, whose fix DELETES it, and a run whose text contains `*/` is left alone (wrapping it would end the doc mid-text). Two shapes convert that only a human can adjudicate, and are left for the review of the diff: a comment above a leading `@:meta` run becomes the TYPE's doc even when it annotates the metadata, and a provenance header (`// Based on https://…`) becomes public documentation. The fix emits `/**` + one `<indent> * <text>` line per comment line (an empty comment line becomes a bare `<indent> *`) + `<indent> */`, or a one-line `<indent>/** <text> */` for a single-line run, with the `//` and ONE following space stripped so relative indentation survives and the file's own line terminator preserved. That shape is byte-identical to what `BlockCommentNormalizer.canonicalDoc` emits under `comments.blockCommentStyle: "javadoc"`, and is a fixed point of the default `Verbatim` writer — so a fixed site does not drift on the next `fmt`. Under that style a multi-line fix output whose interior is a single content line is then COLLAPSED back to the one-line form by the writer, which is also what the single-line-run fix emits directly — the two paths converge rather than ping-pong. A pure trivia edit: comments never affect compilation |
| `explicit-type` | a member field with no `:Type`, a function parameter with no `:Type`, or a function with no return type; `Warning`. `--fix` fills the statically-certain subset: an initializer that is a literal (`String` / `Int` / `Float` / `Bool`), a `new T<…>()` with written type params, or a typed cast / check-type yields its `:T` — enclosing parentheses are peeled first, so `(-1)` pins the same `Int` as `-1`; a block-bodied function whose own scope holds no value-return and no throw (nested functions and lambdas do not count; a throw-only body unifies with any type) gains `: Void` — anything else stays report-only. A constructor (`new`) is exempt from the return-type rule and enum-abstract values from the field rule; interface members are checked like any other. Stating types everywhere is a documented project rule. The enum-abstract-value exemption follows `checkstyle.json` `Type.ignoreEnumAbstractValues` (default `true`; set `false` to flag untyped enum-abstract values too) |
| `redundant-void-return` | a value-less `return;` as the last statement of a function body — redundant, since control falls off the end to the same effect; `Info`, `--fix` deletes it (with its line). Only the body's final statement qualifies: a `return;` nested in an `if` / loop (where it guards the statements it would skip) and a value-returning `return e;` are left alone — purely structural |
| `redundant-property-access` | a property whose BOTH accessors are `default` — `var x(default, default):T` — restating to the compiler exactly what a bare `var x:T` already declares (same storage, same read, same write); `Info`, `--fix` deletes the clause (`public var isSnap(default, default):Bool = false;` → `public var isSnap:Bool = false;`). DEFAULT OFF, for two reasons. **(a)** A project may spell the plain shape on purpose: in the reference project every real-corpus site sits in a run of accessor-bearing siblings (`bold(default, set)` / `italic(default, set)` / `dispatchEvents(default, default)`), where the clause buys column parallelism the fix would break. **(b)** The two spellings are NOT identical to a `@:build` / `@:autoBuild` macro, which sees `FProp('default', 'default')` before the fix and `FVar` after — a project whose macros switch on field kind must leave the rule off. Opt in with `"redundant-property-access": { "enabled": true }` (or an explicit `--rule`). Fires on the exact pair ONLY: every other accessor is a real declaration (`get` / `set` route through a method, `dynamic` re-binds at runtime, `null` / `never` restrict write access, a method name is a custom accessor), so any of them on EITHER side, an arity other than two, or a clause the identifier scan cannot read (a `#if` directive inside it) is a safe miss. Outside the walk entirely, and so never flagged: `final` members, local declarations (`eachFieldMember` visits mutable FIELD members only) and INTERFACE members (`visibilityContainerKinds` covers classes and abstracts, not `InterfaceDecl`) — Haxe treats an interface `(default, default)` the same way, so that one is a safe miss rather than a decision. The fix is ONE edit — the clause plus the whitespace before it, `[name end, ')' + 1)` — so visibility, `static`, the type annotation, the initializer, the doc comment and the metadata all survive verbatim; `@:isVar` is metadata on the member, not part of the clause, and is left in place (inert on a plain field, which always has physical storage). **Comment guard**: a comment marker anywhere from the declaration's start through the clause's `)` withholds the finding. That is deliberately WIDER than the deleted region, because the field-name token is located by text search: a comment quoting the name (`var /* x(default, default) */ x:Int;`) would otherwise send the scan into the comment's own text and report an already-plain field A property written inside a member-position `#if … #end` is scanned too, branch by branch. |
| `unnecessary-block` | a bare `{ … }` statement block nested in another block rather than used as a control-flow body, declaring no local of its own — a pure scope with no effect; `Info`, `--fix` unwraps it (splices the statements into the parent). A block that declares a local is a real scope and is left alone, as is a metaprogramming-reification block — purely structural |
| `field-init-at-declaration` | an instance field with no declaration initializer assigned exactly once in the constructor — as an unconditional top-level `x = expr;` / `this.x = expr;` — whose `expr` is context-free (no ctor params or locals, no `this`, no instance members; literals, statics/globals and `new T()` constructions qualify, `new T(param)` does not): the user's rule is to initialize at the declaration site when the value does not depend on the constructor; `Info`, `--fix` moves `= expr` onto the declaration and deletes the ctor statement (decl-inits run BEFORE the ctor body; the reference gate makes the move order-safe, and a field READ in the ctor before its assignment rejects the candidate). Gates: single-`new` classes only, statics and properties excluded, no unresolved write to the field NAME, and cross-file `FieldWriteIndex.writeCount == 1` — OR, when the cross-file write count DIFFERS FROM ONE (the `dispose()` null-out shape), the ACCEPTED-CANDIDATE CHAIN: every top-level ctor statement before the init must itself be the init of another accepted candidate of the same ctor (an empty prefix is trivially chained). Anything else in the prefix — a leading call statement, `super(...)`, a local `var`, a branch or loop, a write to a static / property / inherited / already-decl-initialized field — breaks the chain. Declaration order is NOT constrained. The chain does NOT claim the prefix runs no foreign code (`new T()` is context-free and allocates); it rests on two pillars: (1) no prefix RHS can leak `this`, so the field's other writer is unreachable in the window the move opens, and (2) moved inits all land in the ctor prologue in whatever sequence the compiler emits, so the gate is permutation-proof rather than order-restoring — `orderSafe` requires a RHS to resolve NOTHING in-class, demanded of the chained candidate AND of every init sharing the ctor prologue with it — the sole-write ones accepted on the legacy path, plus any field that ALREADY carries a decl initializer. Read that as the strong form it is: not "no init reads a member another assigns" (Haxe forbids that anyway, which would make the gate vacuous) but NO init in that prologue directly reads in-class state at all, hence none can observe an in-class static that another one's FOREIGN CODE writes — which is why `_a:Int = s` beside a chained `_b = new Bar()` is refused though neither mentions the other. Covering the existing decl inits is also what makes `--fix` safe at a FIXPOINT: a candidate one pass refuses cannot be unblocked by the co-mover that pass moved out. A `#if` member REGION anywhere in the container refuses every CHAINED candidate (the chain's order proof needs one build's member sequence and a region's branches are several; the sole-write path still applies, and the region's own members are scanned; an `#if` inside an initializer EXPRESSION is a real node and is walked normally). `--fix` additionally skips a violation whose ctor prefix holds a candidate-shaped init that is not itself in the violation list (a `// noqa` must freeze the whole run), reporting each skip once per field on stderr. Known gaps — `orderSafe` decides what a RHS READS, never what the code it invokes DOES: foreign code reached from a moved RHS may mutate any state (external global or in-class static) and that mutation reorders with the move; a moved RHS and any other init sharing the prologue (another moved one, or a pre-existing decl initializer) can communicate through such state and still swap; and the SOLE-WRITE path is gated by none of this — its largest exposure being arbitrary EARLIER CTOR STATEMENTS (`new() { s = 5; _a = s; }`, `new() { _b = bump(); _a = n; }` both fire and both break, unchanged from before the chain), a shape the chain path bars outright since any non-candidate statement breaks it. The in-class veto is deliberately coarse — an immutable static (`static inline` / `static final`) can never be the channel yet still vetoes; measured cost zero, so left unmade. Emission order is an observation, not a contract (reverse of declaration order on `--interp` and `js`, hxcpp unmeasured) and nothing above depends on the direction. Chains with `prefer-final-field` through the `--fix` fixed point: the moved initializer makes the decl-assigned case flag `var` → `final` A field written inside a member-position `#if … #end` is scanned too, and its init moves to the declaration inside the branch; the static-member, sole-constructor and fix-side field lookups all descend into the region with it. A field name declared in TWO mutually exclusive branches is refused outright — the rival declarations share one constructor statement, only one of them is the binding it resolves to, and the move deletes that statement for both. |
| `prefer-final-field` | a private `var` field assigned only at its declaration — a mutable field the immutable `final` should replace; `Info`, `--fix` swaps `var` → `final`. Flagged only when the field has an initializer, is not a property, every write to it is confined to its file — no skip-parsed file, no `@:allow`, and no `@:access` grantee or subtype that WRITES it (a subtype that merely extends, or only reads, does not bail) — and no other write to its name appears in the file (a conservative, complete text scan — `x = …` / `this.x = …` / `x++` all count), so the initializer is provably the sole assignment. A public field is `prefer-final-public-field`'s / `prefer-read-only-field`'s job, a field of a non-confined type is left alone. A NO-INITIALIZER private `var` whose sole write in the class is exactly one unconditional top-level constructor statement also flags — Haxe allows a `final` field assigned once in the ctor — covering fields whose value depends on ctor args and so cannot move to the declaration (`field-init-at-declaration`'s complement). A THIRD arm folds a NULL-GUARDED constructor default: an INITIALIZED field whose only other write is exactly one top-level `if (p != null) x = p;` statement — `p` a nullable constructor parameter (`?p:T`, `p:Null<T>`, `p:T = null`), the declaration default MOVE-SAFE (a numeric / boolean / non-interpolated string literal, a negated numeric literal, or a dotted chain rooted at a capitalised identifier; never an allocation, a call, `this`, or a bare identifier) — is rewritten by a TWO-edit fix to `final x:T;` plus `x = p ?? <default>;`, moving the default into the constructor so the single assignment becomes unconditional A field written inside a member-position `#if … #end` is scanned too, branch by branch, and the keyword swap lands inside the branch; a write in ANY branch still blocks it (the write scan and the write index both read every branch at once). A visibility keyword a branch carries out is merged with OR — the fail-closed direction, since the AND reading would hand this file-confined private proof a field that is public in another build. |
| `prefer-final-public-field` | a public `var` field assigned only at its declaration and never reassigned anywhere in the project — the cross-file counterpart of `prefer-final-field`; `Info`, `--fix` swaps `var` → `final`. A `FieldWriteIndex` resolves every `recv.field = …` / `this.field = …` / bare `field = …` write to its receiver's declared type, so no reassignment of this type's field exists across the whole file set. Flagged only when the field has an initializer, is not a property, no subtype of it can WRITE the field, and no write to the field — resolved or unresolved-receiver — targets it; whole-project scope is required for soundness (like `unused-private`). Merely HAVING a subtype does not bail: only a `this.field` write in one, a write through a subtype-typed receiver (which `FieldWriteIndex` records against the subtype), or an `@:access(<subtype>)` grantee counts — see `MemberWriteScan`. Shares `prefer-final-field`'s null-guarded-default arm through the same `RefactorSupport.ctorConditionalDefaultFinalEdits` predicate, which here also reaches a `(default, null)` PROPERTY (`public var x(default, null):T = D;` + `if (p != null) this.x = p;` → `public final x:T;` + `this.x = p ?? D;`): `final` already forbids the external write the property head was there to block, so the head is dropped. Any other accessor pair is refused A field written inside a member-position `#if … #end` is scanned too, branch by branch, and the keyword swap lands inside the branch; every branch's writes count. |
| `prefer-read-only-field` | a public `var` field written ONLY inside its declaring class, never from another file — `Info`, `--fix` inserts `(default, null)` after the name, making it externally read-only while internally mutable. Uses the same `FieldWriteIndex`: flagged only when the field is written somewhere (else it is `prefer-final-public-field`'s job), no subtype of it can WRITE the field (`MemberWriteScan.subtypeWriteReaches` — having one is not enough), no implemented interface pins the access, no write to its name is unresolved, the type is declared in exactly one file, and no resolved write lies outside that decl — whole-project scope required, and any skip-parsed file bails. Cedes to `prefer-final-public-field` on both constructor arms (the unconditional `ctorSoleAssignmentFinalizable` one and the null-guarded-default `ctorConditionalDefaultFinalEdits` one) through those same shared predicates, so the two rules never emit conflicting fixes for one field A field written inside a member-position `#if … #end` is scanned too, branch by branch, and the `(default, null)` insert lands inside the branch. |
| `unnecessary-null-check` | a comparison against `null` (`x != null` / `null == x`) whose operand is provably non-null, so the test is constant; `Info`, report-only. Type-aware and conservative: the operand must be a plain identifier whose declared type is recovered (`TypeInfoProvider.declaredTypes`) and is either a value type the language never nulls (`Int` / `Float` / `Bool` / `UInt`), or any non-`Null<…>` nominal type while the enclosing type is `@:nullSafety` (and not `@:nullSafety(Off)`). An optional parameter (`?x:T`, nullable despite its annotation), a `Null<…>` / `Dynamic` / `Any` operand, a method-call or field-access operand, and an unannotated local are left alone — so a load-bearing null guard is never flagged. The correct rewrite (drop the guard, or collapse an `&&` operand) is context-dependent, hence report-only |
| `redundant-cast` | a typed cast whose target type already equals its operand's declared type — `cast(x, T)` / `(x : T)` where `x` is declared `T`, a no-op; `Info`, `--fix` unwraps it to the operand. Type-aware: the operand must be a plain identifier whose declared-type SOURCE is recovered (`declaredTypeSources`) and the cast's target-type SOURCE via `castTargetSources`; the two written forms are compared whitespace-insensitively. That is sound within one file — a byte-identical type spelling cannot denote two different types — so it distinguishes `Array<Int>` from `Array<String>` and `haxe.io.Eof` from `sys.io.Eof`, and the unwrap autofix is safe. The untyped `cast x` (no target type), a type mismatch, a non-identifier operand, and an operand with no recovered type are left alone. A bare name and its qualified spelling ARE reconciled when an explicit `import` justifies it — `import haxe.io.Eof; … final x:haxe.io.Eof = cast(e, haxe.io.Eof)` where `e:Eof` is flagged — via `TypeInfoProvider.importMap` (imported simple name → FQN) + `TypeResolver.canonicalTypeName` (qualified path = its own FQN, bare name resolved through the import map, sound because an unqualified name binds to one type per file). A bare name with no matching import (wildcard / implicit-std), or a differing-spelling generic, stays a conservative miss |
| `redundant-tostring` | a `.toString()` call sitting in a position that ALREADY stringifies, so the call only restates what the language does anyway; `Info`, `--fix` drops the call. **DEFAULT OFF** — opt in per project with `apqlint.json` `"rules": { "redundant-tostring": { "enabled": true } }`, or select it with `--rule redundant-tostring`. Four stringifying contexts: **interpolation** (`'${x.toString()}'` → `'$x'` for a plain identifier, `'${expr.toString()}'` → `'${expr}'` otherwise — the `$name` shorthand is used only when the block holds the call and nothing else, the next character does not continue an identifier (`'${x.toString()}abc'` would otherwise become a read of `xabc`), and the literal carries no `\x` / `\u` escape — Haxe DECODES escapes before scanning for interpolation, so `'${x.toString()}\x61'` prints `KKKa` while the collapsed `'$x\x61'` is a read of `xa`); **concatenation** — a DIRECT operand of a `+` whose OTHER operand is String-typed (a string literal, an identifier whose recovered nominal type is `String`, or a nested `+` with a String-typed operand — `+` yields a String as soon as either side is one), `s + x.toString()` → `s + x`; **`Std.string(...)`** — `Std.string(x.toString())` → `Std.string(x)`; and **identity** — the receiver is itself String-typed, so the call returns it unchanged (`s.toString()` → `s`), the one arm needing no surrounding context. A bare `.toString()` in a STRING-REQUIRED position is deliberately NOT an arm — Haxe has no implicit stringification where a `String` is EXPECTED, so `var now:String = Date.now().toString();`, `f(d.toString())` into a `String` parameter and `return d.toString();` from a `:String` function all stay silent; that silence falls out of the arms being CONTEXT-gated rather than receiver-gated, so there is no separate veto to keep in sync. **Three proofs gate the FIX; a site missing one is still REPORTED**, with the message naming the missing proof, and carries no edit — so `--fix` only ever acts on a site all three cover. **(1) The receiver is non-null**: removing the call turns a null receiver from a crash into the text `null` (compile-and-run verified on js — `o.toString()` throws where `'$o'`, `Std.string(o)` and `'x' + o` all yield `"null"`), so a fix needs a plain identifier the shared prover accepts (`TypeResolver.isProvablyNonNull`: a `nonNullableTypeNames` value type, or any recovered nominal while `@:nullSafety` is active at both its declaration and the read; an optional / default-null parameter or a `Null<…>` / `Dynamic` / `Any` type refuses), a `new T(...)` (a constructor cannot yield null), a literal, or a call whose declared return type is recovered and is not a nullable wrapper — from the curated `RefShape.staticMethodReturns` table, or through `SymbolIndex.returnNominalOf` for an instance call, which additionally requires `@:nullSafety` active at the call site. The null LITERAL is excluded explicitly rather than by its absence from `literalTypeNames`, an omission maintained for another consumer. A field access (which never narrows), an index read and an unannotated local all fall short here. **(2) The coercion calls the DECLARED `toString`** (the three context arms; the identity arm performs no coercion): the receiver's type must be declared in the analysed scope with NO `extern` declaration. This gate is load-bearing, not defensive — measured on Haxe 4.3.7 / js, `extern class Date`'s `d.toString()` yields `2023-11-15 00:13:20` where `'$d'`, `Std.string(d)` and `'' + d` all yield the native `Wed Nov 15 2023 …`, and `extern class Array`'s `a.toString()` yields `1,2` where the same three yield `[1,2]`; a NON-extern type is compiled by the compiler that also emits the coercion, so there is one `toString` and every context reaches it (verified for a class, an inherited `toString`, an `abstract` over `Array` and over `Date`, and an `enum abstract`). Consequence: a stdlib receiver resolves only when the std is in the resolution scope, and `Date` / `Array` / `haxe.Exception` stay report-only either way. **(3) `+` on the receiver really is concatenation** (the `+` arm only): its type must be a CLASS, since an `abstract` may overload `@:op(A + B)` and the overload is resolved before the String-concatenation rule (verified: an `abstract Tag(String)` with `@:op(A + B)` and `@:to String` silently changes the value once the explicit call is dropped). **A custom `toString` needs no separate gate**: once proof (2) holds the receiver's type is one the compiler itself compiles, so the method runs exactly once either way and a side-effecting `toString` is neither silenced nor duplicated. Shape gates: the call takes NO arguments (so Haxe's static `CallStack.toString(stack)` is excluded by arity) and its receiver must be a VALUE (a zero-argument static `Type.toString()` is excluded by `TypeResolver.receiverRootIsUnboundType` — dropping it would leave a bare type reference); the call must be the DIRECT operand / argument of its context node, so `s + x.toString().substr(0)` is untouched; a comment anywhere in the removed `.toString()` text suppresses the fix while keeping the finding; macro-reification subtrees (`opaqueKinds`) are not descended into |
| `redundant-null-coalescing` | a null-coalescing `a ?? b` whose left operand is provably non-null, so the right operand is dead — `Info`, `--fix` unwraps it to the left operand. Shares `unnecessary-null-check`'s non-null prover (`TypeResolver.isProvablyNonNull`): the left operand must be a plain identifier whose declared type is recovered (`TypeInfoProvider.declaredTypes`) and is either a value type the language never nulls (`Int` / `Float` / `Bool` / `UInt`), or a non-`Null<…>` nominal type while the enclosing type is `@:nullSafety` (and not `@:nullSafety(Off)`). An optional parameter (`?x:T`), a `Null<…>` / `Dynamic` / `Any` operand, a non-identifier left operand (call / field access), and an unannotated local are left alone — so a load-bearing fallback is never removed. The unwrap is sound because a non-null value makes the right operand unreachable |
| `unnecessary-safe-nav` | a null-safe access `a?.b` (or `a?.m()`) whose receiver is provably non-null, so the `?.` guard can never short-circuit — `Info`, `--fix` rewrites `?.` to `.`. Shares the non-null prover (`TypeResolver.isProvablyNonNull`) with `unnecessary-null-check` / `redundant-null-coalescing`: the receiver must be a plain identifier whose declared type is recovered (`TypeInfoProvider.declaredTypes`) and is either a value type the language never nulls (`Int` / `Float` / `Bool` / `UInt`), or a non-`Null<…>` nominal type while the enclosing type is `@:nullSafety` (and not `@:nullSafety(Off)`). An optional parameter (`?x:T`), a `Null<…>` / `Dynamic` / `Any` receiver, a non-identifier receiver (a chained `a.b?.c` / `a()?.c`), and an unannotated local are left alone — so a load-bearing `?.` is never removed. Unlike `unnecessary-null-check`, the rewrite is unambiguous (`?.` → `.` preserves semantics when the receiver is non-null), hence auto-fixable |
| `redundant-is-check` | an `is` type-check `x is T` that is provably ALWAYS TRUE — `x` is a plain identifier whose declared type already equals the checked type `T` AND is provably non-null; `Info`, report-only. Shares the non-null prover (`TypeResolver.isProvablyNonNull`) and the written-source type comparison (`TypeResolver.sameTypeSource`, whitespace- and import-aware — so `e:Eof is haxe.io.Eof` reconciles via `TypeInfoProvider.importMap`) with `redundant-cast`. Non-null is REQUIRED — `null is T` is `false`, so a nullable operand's check is not constant. Only the always-TRUE, exact-same-type case is flagged: a subtype check (`x:Sub is Base`) is a safe miss, while the always-FALSE direction (two unrelated classes) is handled by the sibling `impossible-is-check`. A `Null<…>` / `Dynamic` operand, an optional param, a non-identifier operand, and a differing checked type are left alone. Report-only: the right rewrite (drop the `if`, keep the body) is context-dependent |
| `impossible-is-check` | an `is` type-check `x is T` that is provably ALWAYS FALSE — `x`'s declared type `S` and the checked type `T` are two **unrelated classes**, so under Haxe single inheritance no value of `S` can ever be a `T`; `Warning`, report-only. Sound via the cross-file class hierarchy `SymbolIndex` already indexes: flags only when BOTH `S` and `T` resolve to a unique indexed CLASS decl (interface / abstract / enum / typedef on either side → open world or implicit conversions → skip), are distinct, and neither is a transitive supertype of the other with BOTH supertype closures **fully resolved inside the index** (`SymbolIndex.unrelatedClasses`). An unindexed supertype link (an external type, or a project file not in the lint set) makes the relation unknown → skip; generics / parametric / `Null<…>` / `Dynamic` operands or checked types never resolve to an indexed class → skip. No non-null proof is needed (`null is T` is also `false`). Every skip is a safe miss. Report-only: the dead branch's right rewrite is context-dependent |
| `impossible-cast` | the cast-sibling of `impossible-is-check`: a runtime-checked cast `cast(x, T)` that can never succeed — `x`'s declared type `S` and the target `T` are two **unrelated classes**, so the runtime `Std.isOfType` test always fails and the cast can never yield a usable `T` (it throws for any non-null value, yields `null` for a null one); `Warning`, report-only. Only the runtime `cast(x, T)` form (`RefShape.checkedCastKind` = `TypedCastExpr`) is inspected — the compile-time `(x : T)` ascription would simply not compile for unrelated types. Same soundness and conservative skips as `impossible-is-check` (both sides a unique indexed CLASS, distinct, unrelated with fully-resolved closures via `SymbolIndex.unrelatedClasses`); operand type via `TypeResolver.identTypeName`, target via the shared `TypeResolver.castTargetWithin`. No non-null proof needed. Report-only: correct the type / drop the cast is context-dependent |
| `redundant-upcast` | completes the cast triad (same type → `redundant-cast`, subtype → this, unrelated → `impossible-cast`): a runtime-checked cast `cast(x, T)` whose operand type `S` is a strict **subtype** of `T`, so the test always passes and the cast is a no-op upcast (an `S` is usable as a `T` without casting); `Info`, report-only. The subtype relation is `SymbolIndex.isSubtype` (extends + implements), so an interface target `cast(impl, I)` is flagged too. Only the runtime `cast(x, T)` form is inspected; operand type via `TypeResolver.identTypeName`, target via `TypeResolver.castTargetWithin`. No non-null proof needed (a no-op for any value). Report-only: an explicit upcast is occasionally load-bearing (overload disambiguation), so removal is left to the author |
| `redundant-cast-type` | a runtime-CHECKED cast `cast(e, T)` whose POSITION is already annotated exactly `T`, so the type argument only restates the annotation; `Info`, `--fix` rewrites it to the unchecked `cast e` (`final s:Sprite = cast(x, Sprite);` → `final s:Sprite = cast x;`). **DEFAULT OFF** — opt in per project with `apqlint.json` `"rules": { "redundant-cast-type": { "enabled": true } }`, or select it with `--rule redundant-cast-type`. Three positions, and the cast must be a DIRECT child of the position node (a cast nested in a larger expression — a ternary branch, an operand — is out of scope): **(a)** a declaration initializer carrying its OWN written annotation (`localDeclKinds` `VarStmt` / `FinalStmt` / `VarMore`, or `fieldDeclKinds` `VarMember` / `FinalMember`), the annotation read as the earliest `declaredTypeSources` entry in `[decl.from, cast.from)`; **(b)** a `return` (`valueReturnKinds`) under a function with an explicit return annotation — the enclosing function is threaded down the walk, a node that OWNS a function body sets it and a `lambdaKinds` node CLEARS it, so a lambda return always bails. The owner test is DERIVED (a `functionKinds` kind, or any node with a direct `functionBodyKinds` child) rather than an enumerated kind list, which is what reaches the nested forms no seam names — Haxe `LocalInlineFnStmt` (the `inline function h():T {}` local-helper idiom) and `NamedFnExpr` — each of which carries its OWN return annotation; enumerating kinds let their unannotated returns inherit the outer function's annotation. The annotation itself is told from a type-parameter CONSTRAINT — which projects the very same `Named` node in the very same child slot — by POSITION relative to the parameter list: with at least one declared parameter the test is purely structural (every constraint precedes the first parameter, so the sole annotation child starting past the LAST one is the return type, whatever the header contains); a PARAMETERLESS function has no such landmark, so the empty list's `)` is located in the text between the annotation and its preceding sibling, refusing outright when a comment sits in that window — a `)` can also live inside a structural constraint (`<A:{ function n():Void; }, B:Foo>`) or a header comment, and either would otherwise promote a constraint to a return type; **(c)** a call-argument slot whose parameter is written `T`, gated hard: the callee must be a bare identifier (a field-access callee `o.m(…)` bails) resolving via `TypeResolver.resolveBindingFrom` to the innermost same-named `functionKinds` declaration in this file, and EVERY parameter up to and including the slot must be plain required with NO default value — Haxe lets a call SKIP a non-trailing optional argument (`f(?a:Foo, b:Int)` accepts `f(1)`) and skips a defaulted `a:Int = 1` the same way even though it projects as required, so either ahead of the slot leaves the positional argument-to-parameter mapping unproven, and a rest parameter absorbs every remaining slot. An optional or rest BEHIND the slot cannot shift it and is fine. Type identity is `TypeResolver.sameTypeSource` — whitespace-insensitive exact equality on the two WRITTEN forms plus FQN reconciliation through the file's imports for plain nominals — so a generic matches ONLY its own token-identical spelling (`Map<String, Int>` vs `cast(v, Map<String,Int>)` yes, vs `Map<String, Float>` no) — a linter-level distinction rather than a shape real code carries, since Haxe itself rejects a parameterised checked cast (`Cast type parameters must be Dynamic`) — while `import haxe.io.Eof; … final e:haxe.io.Eof = cast(v, Eof)` reconciles. **Generics veto on the argument slot only**: the parameter type must be a plain nominal that a `SymbolIndex` DECLARES, since a type PARAMETER (`function pick<T>(v:T):T`) is declared nowhere — that vetoes the one shape where the argument's type would DRIVE inference rather than be constrained by it; (a) and (b) need no veto because their annotation FIXES the type. Consequence: a builtin-typed parameter (`p:Int`) resolves only when the std / configured libraries are in the resolution scope, a safe miss otherwise. A target whose outer nominal is `Null` / `Dynamic` / `Any` (`nullableWrapperTypeNames`) is vetoed — such a check never throws, so there is no runtime check to trade away. **Comment guard**: the fix deletes exactly the `cast(` head `[cast.from, operand.from)` and the `, T)` tail `[operand.to, cast.to)`, so a comment marker in EITHER region suppresses the finding outright (comments inside the operand survive verbatim). The unchecked `cast e` and the `(e : T)` ascription are different node kinds and are never touched; macro-reification subtrees (`opaqueKinds`) are not descended into. **Semantic trade-off** (measured on Haxe 4 `--interp`, user-approved): `cast(null, Foo)` and `cast null` both yield `null` and neither throws, so a null value behaves identically — but `cast(<a Bar instance>, Foo)` THROWS while `cast <a Bar instance>` silently yields the `Bar`. The rewrite therefore trades a runtime type check for brevity, confined to a NON-NULL type mismatch; that is exactly why it fires only on provably-annotated positions and why it is default off. Overlapping with `redundant-cast` / `redundant-upcast` needs no gate: `Cli.computeFileLintEdits` defers a later check whose edits overlap an earlier one, and those siblings delete the cast entirely — a strictly better fix that converges on the next `--fix` pass |
| `redundant-ascription` | a parenthesised ascription `(new T(...) : T)` whose ascribed type restates the constructed class name — the construction already has type `T`, so the ascription is a no-op; `Info`, `--fix` unwraps it to the bare `new T(...)`. **DEFAULT OFF** — opt in per project with `apqlint.json` `"rules": { "redundant-ascription": { "enabled": true } }`, or select it with `--rule redundant-ascription`. **HARD BOUNDARY**: only a direct `new T(...)` operand (`RefShape.newExprKind`) is ever flagged — an identifier, a call, a field access, or any OTHER ascribed expression is left alone no matter how its written type compares, because an identifier ascription can be load-bearing null-safety narrowing a construction can never need (`new T(...)` is never null, so there is nothing to narrow) — proven on real code, where removing a `(x : Int)` narrowing a `Null<Int>` local for a non-null-typed object-literal field fails to compile. Comparison is byte-equal (`TypeResolver.stripWs` equality on the two WRITTEN forms) and deliberately NOT reconciled through the file's imports the way `redundant-cast` is — `(new haxe.ds.StringMap() : StringMap)` is a safe miss even though both spellings name the same type. The ascribed type must also carry NO type parameters (`indexOf('<') == -1`), kept as an explicit gate independent of the equality check even though `HxNewExpr.type` itself never carries `<...>` (its type arguments are the separate `params` field). The fix deletes exactly the `(` head `[node.from, operand.from)` and the ` : T)` tail `[operand.to, node.to)`, so a comment marker in either region suppresses the finding outright — a comment inside the constructor call's own arguments rides along verbatim in the replacement text |
| `unreachable-catch` | a `catch` clause that can never run because an EARLIER clause in the same `try` already catches everything it would; `Warning`, report-only. Three covered-by relations: a **catch-all** earlier clause (`RefShape.catchAllTypeNames` — `Dynamic` / `Any` — or an untyped `catch (e)`, which binds the exception root); a **duplicate** (same written type source, reconciled import-aware via `TypeResolver.sameTypeSource` — so `e:Eof` and `e:haxe.io.Eof` match); and **subtype-after-supertype** (the later type transitively extends/implements the earlier one — `SymbolIndex.isSubtype`, the cross-file hierarchy the index builds). The exception type is recovered from the clause header source (between the first `:` and the closing `)`, bounded by the handler block); a non-nominal type (generic / function / anon) yields no simple name → safe miss; the subtype check is simple-name based (an unindexed supertype link ends the chain — a safe miss). Report-only: removing the dead clause vs. fixing its type / order is context-dependent |
| `catch-dynamic` | a `catch` clause whose declared exception type is `Dynamic` (or `Any`) — a raw catch-all; the user's rule is `catch (exception:Exception)`, whose wrapper binds a richer, uniform API rather than the raw thrown value; `Warning`, report-only. The type is read from the clause header source (between the first `:` and the closing `)`, bounded by the handler block) and reduced to a simple nominal name (`TypeResolver.simpleNominalName`); a hit against `RefShape.catchAllTypeNames` (`Dynamic` / `Any`) is flagged, and only the `(var:Type)` parameter region is spanned. A typed catch of any other name (`Exception`, a custom class, `String`) and an untyped `catch (e)` (no type text) are left alone; a non-nominal type (generic / function) yields no simple name → safe miss. Report-only — no `--fix`: `catch (e:Dynamic)` binds the RAW thrown value while `catch (e:Exception)` binds a WRAPPER with a different API, so an automatic type swap would silently change what the body operates on |
| `prefer-typed-throw` | a `throw` of a raw string LITERAL (`throw 'boom'`, `throw 'bad $id'`) replaceable with the canonical typed form `throw new Exception('…')`; `Info`, DEFAULT OFF (opt in via `apqlint.json` `"rules": { "prefer-typed-throw": { "enabled": true } }`, or an explicit `--rule`). Only a literal / interpolated string operand qualifies (parentheses peeled) — `throw <expr>` of any other shape is out of scope, being neither provably a raw String nor movable into a constructor without an evaluation-order question. `--fix` replaces the literal with `new <exception>(<literal verbatim>)`, the exception reference spelled by `TypeRefPrinter`: the short name when the type is already visible, the short name PLUS an inserted `import haxe.Exception;` when that name is free in the file, else the fully-qualified `haxe.Exception` — a file that already binds `Exception` to something else is never retargeted; a throw inside `#if … #end` always emits the qualified path and adds no import (the `catch-dynamic` convention). FAIL-CLOSED GATE: boxing stops a `catch (e:String)` — and a `catch (e:haxe.ValueException)`, the wrapper the runtime builds for a RAW throw — matching at all, and rebinds what a `catch (e:Dynamic)` / `catch (e:Any)` body sees, so the check scans the PROJECT half of the resolution scope (report files UNION the configured library roots) for such a clause — textually, since a parse of every indexed source would dominate the run and an over-match only degrades further — and DEGRADES WHOLESALE to report-only when one exists (a source the index could not PARSE is unreadable rather than absent, so a skipped project file degrades too; the `--fix` fixed-point loop runs the rule full-scope every pass). The auto-discovered Haxe std is EXCLUDED from that scan (`StdResolver.isStdFile`, keyed off the same discovered `stdDir` the scope was built from, never a `/std/` path-string guess): a `String` / `Dynamic` catch inside std guards std-internal throws within std's own call boundaries, and a project throw boxed into `haxe.Exception` cannot change which std-internal throws those clauses see — scanning them only imported a verdict about code the rewrite can never reach, and since the std ships ~31 such clauses across 10 files (`haxe.ds.BalancedTree` catches `String`; `haxe.io.Bytes`, `haxe.io.BytesInput`, `haxe.Template`, `sys.Http` catch `Dynamic`) the rule used to be report-only on every Haxe-equipped machine. Two known unsound misses: a clause typed through a `typedef Raw = String` alias (the scan does not resolve names), and a project callback handed INTO std and invoked inside a std `try` (bought deliberately by the std exclusion) |
| `shorten-type-ref` | a DOTTED type reference the file itself spells differently — `TypeRefPrinter` decides how the reference should be written here, and any disagreement is the finding; `Warning`, DEFAULT OFF (opt in via `apqlint.json` `"rules": { "shorten-type-ref": { "enabled": true } }`, or an explicit `--rule`). Three arms. ARM 1, the correctness one: the `pack.SubType` HYBRID for a SECONDARY type — `api.model.folders.FolderContentEntity` written in a file carrying `import api.model.folders.FolderContent.FolderContentEntity;` — which resolves ONLY while that import survives and breaks silently at an untouched site the day it goes; `--fix` repairs it to the short name (import present) or to the module-qualified `api.model.folders.FolderContent.FolderContentEntity` (import absent). ARM 2: plain over-qualification, where the short name is already visible via an exact import, an alias (the ALIAS is printed), a same-module type, the MAIN type of a same-package module, or an always-in-scope builtin — no occurrence threshold. `shadowedLocally` keeps a path that is long BECAUSE the short name is taken — by a plain import (`api.model.folders.FolderContent` in a file whose bare `FolderContent` means `api.namespace.FolderContent`), an alias, a module-local or same-package type, a BULK import (`import other.*;` / `using other.Widget;`, decided against the index), or a `#if`-GUARDED import binding that name to another path (the `import openfl.system.Capabilities;` + `#if flash import flash.system.Capabilities; #end` shape — the plain-import map is a top-level scan and cannot see the guarded one). ARM 3, ADD THE IMPORT: a path with no short form in scope, written out >= 2 times OUTSIDE `#if`, whose simple name nothing else in the file binds — `--fix` inserts `import <path>;` through `ImportOrder` (an already-sorted block keeps its sort, an unsorted one is appended to) and shortens those occurrences. The arm needs `print`'s `owned` exemption: route 2's freeness gate refuses an import whose simple name occurs anywhere in the source, and the very text being shortened carries that name as its own last segment; only the occurrences of the path ITSELF are exempted, so a local type declaration, a second import, a type parameter or a `#if`-guarded import of the same name still keep the path long. SURFACE: the scan runs over the grammar's TYPE-REFS projection (`parseFileTypeRefs`), so every type position is one node with an exact span — `is` right-hand sides, `new`, heritage, type-param constraints, `from` / `to` clauses, typedef right-hand sides, and local / field / parameter / return / anonymous-field / enum-ctor-param annotations alike, each generic component on its own — plus static-access chains in expression position (`sys.FileSystem.exists(…)`), rebuilt from the field-access nesting and gated on a lower-initial receiver chain whose root resolves to no value binding. FAIL-CLOSED GATES: a changed reference is fixed only when `TypeRefPrinter.resolvePath` proves against the resolution index WHICH DECLARATION the original names (no index, or no indexed declaration at that path → the finding stays, the edit does not, and arm 3 is not offered at all, so an unproven path never promises an import); an occurrence inside `#if … #end` is never rewritten and never counted toward the threshold (what is in scope there depends on the build) though it IS exempted from the freeness scan, being the path's own text; METADATA ARGUMENTS are skipped wholesale — `@:access(pkg.Type)` and its siblings are dot-paths the compiler resolves WITHOUT the file's imports, verified against the compiler: `@:access(Type)` beside an `import pkg.Type;`, and even from inside `pkg` itself, silently grants nothing and the build then fails at the private access (an expression-position `@:privateAccess pkg.Type.f()` body IS ordinary resolution and is not skipped); a static chain under an upper-initial field access (`a.b.Module.SubType`) is refused as a sub-module access; an `opaqueKinds` reification subtree is skipped. VERIFIED, NOT TRUSTED: the proof is `canonicalize`'s, so it inherits `canonicalize`'s precondition — a `pack.SubType` is read as a hybrid only when the index declares no type AT that path, and a module the index never saw (outside the lint scope, or one it could not parse) removes that veto, repairing a REAL `pack.SubType` main type onto an imported sub-type of the same simple name. No index can detect its own incompleteness, so the rule is a `RiskyFix`: `--fix` applies its edits speculatively, typechecks, and reverts any file the edit breaks; with no `compilerOracle` configured it stays report-only wholesale. KNOWN MISS: an anonymous structure used as a GENERIC ARGUMENT (`Array<{x:pkg.T}>`) — the type-refs projection stops at the generic head there, so no node carries the nominal inside it; every other nesting projects |
| `dead-null-guard` | a null comparison (`x != null` / `x == null`) whose operand is already non-null **by flow** on every path reaching it — a dead guard whose controlled branch is constant; `Info`, report-only. The flow-only complement of `unnecessary-null-check`: non-null-ness comes from a prior `!= null` guard or a syntactically non-null assignment (`x = new T()`, including `x ??= <non-null>`), tracked by the intra-procedural `NullFlow` engine — a per-variable-**name** `Unknown`/`NonNull` lattice with sound joins (after an `if` the two arms' exit states are intersected — an arm that exits via `return`/`throw`/`break`/`continue` contributes no path, so `if (x == null) return;` narrows the fall-through; a `switch` intersects its branches' exit states the same way, plus the no-branch-matched path unless a `default:` or an unguarded `case _:` proves exhaustiveness, with case-pattern captures cleared as fresh bindings; a `try` intersects the body's exit state with each catch clause's, where a clause starts from the entry state with every body-written name cleared — the throw may fire at any point inside — and its catch variable cleared; a loop clears every name it assigns *before* its body so a back-edge carries no stale fact; a short-circuit `&&`/`||` walks its right side as a conditional path — on a copy narrowed by the left side (`&&` as a then-arm, `||` as an else-arm) — and intersects the exit back, so an RHS write never leaks onto the skip path while `x != null && x != null` flags the dead second conjunct; a plain `??` fallback gets the same conditional join, and condition narrowing never keeps a fact for a name the condition itself writes — the comparison may predate the write; closure-captured names plus `macro` reification subtrees are excluded, and metadata arguments (`@:m(...)`) are skipped as compile-time data that never executes). An operand the declared type already proves non-null (`TypeResolver.isProvablyNonNull`) is left to `unnecessary-null-check`, so a redundant null check is reported exactly once. Soundness-first: any uncertainty collapses to `Unknown` (a safe miss), never a false positive. Report-only: the correct rewrite (drop the guard, or collapse an `&&` conjunct) is context-dependent |
| `dead-safe-nav` | a null-safe access `a?.b` whose receiver is already non-null **by flow** on every path reaching it — the `?.` can never short-circuit; `Info`, report-only. The flow-only complement of `unnecessary-safe-nav`: non-null-ness comes from `NullFlow` (a prior `!= null` guard's then-arm, an `== null` guard's else-arm, or a syntactically non-null assignment), and an operand the declared type already proves non-null (`TypeResolver.isProvablyNonNull`) is left to `unnecessary-safe-nav`, so a redundant `?.` is reported exactly once. Same soundness as `dead-null-guard` (any uncertainty → `Unknown`, a safe miss). `--fix` rewrites `?.`→`.` (the same unambiguous rewrite `unnecessary-safe-nav` applies) |
| `dead-null-coalescing` | a null-coalescing `a ?? b` whose left operand is already non-null **by flow** on every path reaching it — the fallback is dead; `Info`, report-only. The flow-only complement of `redundant-null-coalescing`: non-null-ness comes from `NullFlow` (a prior `!= null` guard's then-arm, an `== null` guard's else-arm, or a syntactically non-null assignment), and a left operand the declared type already proves non-null is left to `redundant-null-coalescing`, so a dead fallback is reported exactly once. Same soundness as `dead-null-guard`. `--fix` unwraps to the left operand (the same rewrite `redundant-null-coalescing` applies) |
| `always-null-comparison` | a null comparison (`x == null` / `x != null`) whose operand is provably **null** by flow on every path reaching it — a constant comparison (`== null` always true, `!= null` always false), so the controlled branch is dead; `Info`, report-only. The mirror of `dead-null-guard` (the non-null operand) on the same `NullFlow` engine, now three-valued (`NonNull` / `Null` / `Unknown`): null-ness is established only by flow events — an earlier `x = null` / `var x = null`, or the `== null` arm of a guard narrowing this path — with the same sound joins and conservative `Unknown` collapse (reassignment, loop back-edge, closure capture, `macro` subtree). Only a function unit's own names (its parameters and locally-declared `var`/`final`s) are narrowed — a captured outer variable or an implicit-`this` field is a non-local a call could mutate, so it is left `Unknown` (mirroring the language's own strict null-safety). No point-wise twin (no declared type is "always null"), so unlike `dead-null-guard` there is nothing to defer to. Report-only: the correct rewrite (drop the dead branch) is context-dependent |
| `null-dereference` | a dereference whose receiver is provably **null** by flow on every path reaching it — a guaranteed runtime NPE; `Warning`, report-only. The headline bug-finder of the definite-null arc — the first check that reports an actual defect rather than a redundancy. Four receiver forms: a field / method access (`x.field` is a `FieldAccess` on `x`, and `x.method()` a `Call` whose callee is that same `FieldAccess` — one node kind covers both), a force-unwrap access (`x!.b` — forcing a known-null is exactly the crash the sigil promises away), an index access (`x[i]`), and a bare call (`x()` on a plain-identifier callee); the null-safe `x?.b` is a distinct kind that short-circuits and is never flagged. Null-ness comes purely from `NullFlow` (an `x = null` / `var x = null`, or the `== null` arm of a guard) and only a function unit's own names are narrowed, so a static access (`SomeClass.staticFn()`), `this`, or an enum is never reported. Same conservative `Unknown` collapse as the rest of the arc. Report-only — a null dereference has no mechanical fix (the surrounding logic is wrong) |
| `dead-store` | an assignment to a local / parameter whose value is provably never read on any path (overwritten, or the function exits first) — a wasted computation that usually indicates a logic slip; `Info`, report-only. Two forms: a plain assignment (`x = e;` then never read) and a `var` initializer reassigned before any read (`var x = e; x = f;`). Backward-**liveness** engine embedded in the check (the dual of `NullFlow`, over-approximated in the opposite direction — every uncertainty makes MORE names live, so a report means dead-on-all-paths): branches union their arms; a loop keeps every name read anywhere inside it live at its boundaries and branch seams (back-edge safety, and the pre-loop state survives a zero-iteration run — a reassign-then-reassign within one straight-line run is still caught), `switch` / `try` seeded likewise; a `return` clears the state but a `throw` makes every name live (its continuation is an unmodeled `catch`); `break` / `continue` (which project as plain identifier expressions — `RefShape.loopJumpNames`) make every name live; a short-circuit right operand (`&&` / `\|\|` / `??`) or the arguments of a call whose callee chain contains a `?.` evaluate conditionally, so their kills never leak into the skip path; a name a closure reads or writes is excluded entirely, as is a name bound more than once in the unit (name-keyed liveness cannot tell shadowed bindings apart); a multi-binding `var a = 1, b = 2;` (projected as one node) is never reported though its initializers' reads still count; `macro` reification makes everything live; a `'$name'` interpolation counts as a read (`RefShape.stringInterpIdentKind`). Partition with `unused-local`: that check owns the binding never referenced at all (its text scan counts a write as a reference), so a written-then-never-read local is this check's finding, and a dead `var` initializer is reported only when the name IS referenced elsewhere in its enclosing scope; a `final` initializer is never flagged. Report-only: deleting a store is behavior-preserving only when its right-hand side is side-effect-free — an autofix promotion can follow once the engine has earned trust |
| `thread-safety` | config-driven thread-context analysis over the `CallGraph` — the two classic main-thread stall shapes: **(a)** a MAIN-context function directly calling a configured blocking `sink` (chain evidence in the message), **(b)** a lock held across a call that transitively reaches a sink (`lockPairs: ["Type.lock/unlock"]`). Context propagation: graph roots start MAIN; a callback passed to a `spawns` target goes BG, to a `marshals` target comes back MAIN, any other callback inherits the registrar's context; a node with no resolved callers is ASSUMED main (finder-grade over-approximation); a `macro` function is compile-time code and stays out of the graph; a ternary callback argument (`spawn(cond ? f : g)`) refs BOTH branches; sinks inside a `marshals` function's own body are not reported (the marshal IS the thread boundary). `Warning`, report-only, INERT without a `thread-safety` config in `apqlint.json` (`sinks` required; patterns match by last two dot-segments, `Type.*` covers a whole type; `exclude` drops files whose path contains an entry as a '/'-bounded segment run) |
| `unchecked-nullable` | the result of a `Null<T>`-returning call used directly as a non-null number with no null check in between — the silent-null bug. Slice 1 of the nullable-source family recognises `Std.parseInt` / `Std.parseFloat` (both `Null<Int>` / `Null<Float>`, matched structurally as a `Std.<method>` field-access call): their result appearing as an operand of a numeric operator (`- * / % < > <= >= & \| ^ << >> >>>`, unary `-` / `~`, or `+` outside a string context) or as an array-index position assumes non-null, yet the parse can fail and yield `null`. `Warning`, report-only (a guard restructures the surrounding code, so there is no single safe edit). Null-tolerant `==` / `!=` and the type-incompatible logical `&&` / `\|\|` are excluded; a numeric operator with a string-literal operand is treated as concatenation (`n + "x"`) and skipped — only the LITERAL string is spared, `n + strVar` still flags (telling those apart needs a typechecker). Grammar-agnostic via `RefShape.nullableNumericReturnCalls` / `numericOperatorKinds` / `stringLiteralKinds`, all extensible for later nullable sources (map access, `.pop()` / `.shift()`, declared `Null<T>` returns) |
| `possible-null-dereference` | a dereference of a provably-nullable expression with no null check, a possible NPE; `Info`, report-only. Slice 1 of the reference-nullable family (mechanism B — the type-driven, point-wise sibling of the flow-sensitive `null-dereference`). Three nullable sources: a `Map`-family index `m[k]` (a `Null<V>`), an `Array` / `List` / `Map` nullable-returning method call (`Array.pop` / `Array.shift` / `List.pop` / `List.first` / `List.last` / `Map.get` & friends, each a `Null<T>` / `Null<V>`), and a call to a function whose declared return type is `Null<T>` — covering `m[k].field` / `m[k].method()` / `m[k]!.field`, `arr.pop().field` / `m.get(k).method()` / `lst.first().field`, and `findUser().field`. Type-aware and it must be: `m[k].field` shares an identical AST with `arr[i].field`, `arr.pop().field` with a `pop()` call on any type, and `findUser().field` with any call; only the receiver's declared type / the callee's declared return type tells them apart — a `Map` index yields `Null<V>` (an `Array` / `String` index a non-null `T`), `Array.pop` / `Array.shift` / `List.pop` yield `Null<T>` (a same-named method on an unrelated type does not), and a `Null<T>`-returning function yields a nullable result. So the deref flags only when the receiver is a `RefShape.nullableIndexTypeNames` index (`Map` / `StringMap` / `IntMap` / `ObjectMap` / `EnumValueMap` / `WeakMap`) or a `RefShape.nullableInstanceReturnCalls` (`Array.pop` / `Array.shift` / `List.pop` / `List.first` / `List.last` / `Map.get` and the concrete-map `.get`s) call on a plain identifier of matching declared type (`TypeResolver.identTypeName`), or a call whose plain-identifier callee binds to a function whose `TypeInfoProvider.returnTypes` outer nominal is a `RefShape.nullableReturnMarkerTypes` (`Null`); all resolution requires `plugin is TypeInfoProvider`. An `Array` / `String` / unannotated / `Null<Map<…>>` receiver, an unrelated-type method, a non-`Null<…>` or unannotated return, and a qualified `this.f()` / `obj.f()` callee are safe misses. Point-wise, NOT flow-sensitive — `if (m.exists(k)) m[k].field`, `if (arr.length > 0) arr.pop().f` and a guarded `findUser().f` are still flagged (the guard is invisible without flow), which is why the severity is `Info` (advisory), not the `Warning` a flow-sensitive engine would earn. A bare `m[k]` / `arr.pop()` / `m.get(k)` / `findUser()` with no deref is left alone; macro-reification subtrees are not descended into. A cross-file `Type.static()` / `obj.method()` whose return is `Null<T>` now resolves via `SymbolIndex.returnNominalOf` (member return nominals indexed in `MemberInfo.returnNominal`), including a member INHERITED through the (unanimous, cycle-guarded) supertype closure, conservative under a simple-name collision; a bare `this.f()` stays a safe miss |
| `unguarded-nullable-deref` | a dereference of a local **bound from** a nullable source with no null check on the path reaching it — a possible NPE; `Warning`, report-only. The flow-sensitive (mechanism A) sibling of `possible-null-dereference` (mechanism B): where B sees only a deref of the nullable *expression* itself (`m[k].f`), this catches the binding-then-use B is structurally blind to — `final u = m[k]; doStuff(); u.name;`. A `NullFlow` walk seeds a fourth lattice value `MaybeNull` whenever a local is assigned a nullable source (the shared `NullableSource.describe` over `TypeInfoProvider.declaredTypes` / `returnTypes` — a `Map`-family index or `.get`, or a `Null<T>`-returning call; the length-guarded collection accessors `Array` / `List` `pop` / `shift` / `first` / `last` are excluded (`RefShape.nullableFlowExcludedCalls`) since their dominant `while (c.length > 0) c.pop()` idiom is safe by a guard flow cannot model, so they stay `Info`-only under the point-wise check), and the same guards the engine already models narrow it away (`if (u != null)`, an early `if (u == null) return;`, an `&&` right side, a non-null reassignment, `??=`) — so a guarded deref is a safe miss. The four receiver forms match `null-dereference` (`u.f` / `u.m()` / `u!.f` / `u[i]` / `u()`); only a function unit's own names are tracked, so a field / static / `this` receiver is never reported. The three null-deref checks are disjoint by the receiver's flow state / shape — `null-dereference` on a `Null`-by-flow ident, `possible-null-dereference` on a nullable-source *expression* receiver, this one on a `MaybeNull`-bound *ident* receiver. `Warning` (not the point-wise `Info` twin) because flow removes the guard-blindness false-positive class; report-only |
| `optional-param-shorthand` | a function parameter written `name:Null<T> = null` or `name:T = null` — a nullable-or-plain type with a `null` default — that the `?` optional-parameter shorthand `?name:T` replaces, and an already-`?` parameter carrying a redundant `null` default (`?name:T = null`) (the user's rule: prefer `?style:ScrollBarStyle` over `style:Null<ScrollBarStyle> = null`, and — since a `null`-defaulted plain-type parameter types identically — `?position:Point` over `position:Point = null`); `Info`, `--fix` rewrites it — unwrapping ONE `Null<>` layer when present (else keeping the type as-is), dropping the ` = null`, and prepending `?`; on an already-`?` parameter the type stays verbatim (no unwrap — `?x:Null<T> = null` fixes to `?x:Null<T>`) and only the ` = null` goes. The forms are equivalent for a nullable-defaulted parameter (a live compiler probe: `$type` gives `(?p:Null<T>) -> Void` for BOTH `p:Int = null` and `?p:Int`, and the `haxe.PosInfos` call-site auto-fill magic fires in both forms; an omitted `?x:T` argument already yields `null`, so its explicit `= null` default changes nothing), so no call site changes. Grammar-agnostic over `RefShape.paramKinds`; the parameter's last child (the default) must be exactly the `null` literal, and the type text between the name's `:` and the default's `=` is — for a required parameter — either a single balanced `Null<...>` (nested `<>` and a function-type `->` balanced correctly, the matching `>` at the type's end) or any other non-empty type text that does not itself open as a `Null<` wrapper, and — for an already-`?` parameter — any non-empty type text, kept verbatim. Covers class methods, constructors, and local functions. Deliberate misses: `name:Null<T> = <non-null default>` (different semantics), an already-`?` parameter WITHOUT a `null` default (`?x:T` / `?x:Null<T>` — the nested-unwrap fix itself lands at `?x:Null<T>`, which the one-layer rule leaves as-is), a `Null<T>` with no default, an untyped `name = null` / `?name = null` (no type text to carry the rewrite), and a `Null<`-prefixed type text the unwrapper rejects (decorated — e.g. a comment between the type and the `=` — or malformed; coercing it into the bare-type arm would prepend `?` without unwrapping). The whole parameter span is replaced, so commas, parentheses, and sibling parameters stay intact |
| `prefer-final-class` | a class declaration carrying the `@:final` metadata tag — the user's rule: use `final class`, not the `@:final` meta, for a class not designed for inheritance; `Info`, `--fix` removes the `@:final` meta and inserts the `final` modifier before the `class` keyword, and for a redundant `@:final final class` removes the meta only. Grammar-agnostic over `RefShape.finalClassMetaName` / `plainClassDeclKind` / `finalClassDeclKind` plus `MetaShape.metaKinds`. The decorated declaration is the meta's nearest following sibling that is not itself a meta, resolved by SOURCE order (span, so child-array order and an intervening meta / doc comment do not mislead); the finding spans only the `@:final` token (not the class body, so a `// noqa` inside cannot swallow it). The fix removes the meta and its trailing whitespace back to the next non-whitespace char (no blank residue) as ONE combined `[meta, keyword)` replacement when only whitespace separates them, else two disjoint edits so a doc comment or another meta sitting between the meta and the class is preserved. Deliberate misses: `@:final` on an interface / enum / enum abstract / typedef / `abstract class` (none is a class the `final` modifier applies to — `final abstract class` is not valid Haxe), and `@:final` on a method or field (its decorated decl is a member, not a class); a plain `final class` with no meta is already idiomatic. Multi-meta order variants (`@:final @:keep class` / `@:keep @:final class`) fix cleanly, keeping the other metas |
| `prefer-safe-nav` | a null guard that safe navigation replaces — the user's rule: prefer `x?.m()` over `if (x != null) x.m()`; `Info`, with an autofix. THREE arms. STATEMENT: `if (x != null) x.m(...)` (and the braced `if (x != null) { x.m(...); }`, plus the reversed `if (null != x) …`) — `--fix` replaces the whole `if` statement with the body statement, turning the FIRST dot off the guarded identifier into `?.` (`if (x != null) x.a.b();` → `x?.a.b();`; inner dots stay the author's concern); a CONJUNCTION guard whose LAST conjunct is the null-check (`if (C && ... && x != null) x.m(...)`) drops that conjunct and keeps the rest as the surviving condition (`if (C && ...) x?.m(...);`). TERNARY: the expression form of the same guard — `x == null ? null : x.m(...)` and its dual `x != null ? x.m(...) : null` (the `null` operand on either side of the comparison) collapse to `x?.m(...)`, the whole ternary replaced by its guarded branch with that same first dot turned into `?.`; `?.` short-circuits the entire receiver chain, so a multi-step branch (`x.map(f).join(',')`, `x.arr[0].c`) collapses in one step. ASSIGNMENT: a local declared `= null` immediately followed by its guarded assignment — `var r:Null<T> = null; if (x != null) r = x.chain(...);` folds into the declaration as `var r:Null<T> = x?.chain(...);`, sound only in that adjacency (the fold makes the guard's false path assign `null`, which is what the initializer already gave it), and taking no conjunction at all since the `if` disappears entirely. Grammar-agnostic over `RefShape.ifStatementKinds` / `notEqKind` / `nullLiteralKind` / `callKind` / `fieldAccessKind` / `exprStatementKind` / `blockStmtKind` / `logicalAndKind`, plus `ternaryKind` for the ternary arm (unset → that arm alone is off) with `eqKind` adding its `x == null ? null : …` polarity, and `indexAccessKind` for an index step on a chain, with `selfReferenceText` for the `this` receiver, `localDeclKinds` / `paramKinds` / `scopeKinds` for binding resolution, `parenKind` to unwrap a parenthesized condition or ternary branch, and `opaqueKinds` to skip macro reification. Soundness gates: the guard SUBJECT is a plain identifier or a one-step `<ident>.<field>` path (a deeper `a.b.c` and an already-safe `x?.f` refuse), and every part of it must be provably accessor-free — the guard reads the subject twice, `?.` reads it once, so a property getter on the way would change semantics. The root identifier qualifies as a LOCAL or PARAM whose scope encloses it, the self reference `this` (a keyword that can carry no accessor, which is what enables the abstract idiom `@:to function toString() return this == null ? null : this.map(f).join(',');`), or a FIELD the enclosing NON-extern type declares as a physical `var` / `final` with no read accessor; the `.field` step of a path is proven separately by `TypeResolver.isPlainFieldRead` against the run's resolution-scoped `SymbolIndex` (`memberGetter` walks supertypes), so a getter property, a type the index cannot resolve, and a run with no resolution scope each leave the guard unflagged — that step proof reads the member's DECLARED shape and so carries no extern gate, unlike the enclosing-type field proof. The then-branch must be exactly ONE expression statement whose expression is a CALL rooted at that subject, so a multi-statement block (grouped deliberately), an assignment l-value (`x.f = v` — `x?.f = v` does not compile), a body already using `?.` on the root, and an `else` branch (a real two-way choice) are all safe misses. The guard subject must NOT appear anywhere in the body but as the chain root (`x.m(x)`, `x.a(x).b()`, `x.arr[x]`) — after `x?.` it stays typed `Null<T>`, breaking `@:nullSafety(Strict)`. A subject rooted at a LOCAL counts as a whole, so its bare root elsewhere (`x.f?.m(x)`) rides along; a member of the ENCLOSING object is the exception, since `f` and `this.f` are one narrowed value under two spellings and either spelling elsewhere in the body refuses the guard. In the conjunction form the null-check must be the LAST conjunct (a later conjunct may rely on `x` being non-null: `if (x != null && x.len > 0) …` is a safe miss). BOTH arms require the kept region to BE the receiver chain and nothing more: the descent (`subjectAccess`) walks only field-access / call / index nodes down to the subject and its junction must be a plain `.`, so an index or call MID-chain (`x.arr[0].c`, `x.map(f).join(',')`) collapses fine while an index root (`x[0].f`), an already-safe `x?.f` root, and a `cast` / parenthesis / ascription / operator on the spine (`(cast x.a).m()` → `(cast x?.a).m()` would dereference null; `x == null ? null : x.f + 1` → `x?.f + 1` would add to a `Null<T>`) are all refused. In the ternary form the other branch must be exactly the `null` literal (`x == null ? 0 : x.f` guards a real fallback — `prefer-null-coalescing` territory at most). A comment inside a DROPPED part of the removed region would be lost, so such a guard is left unflagged; a comment between the receiver and its dot would swallow the inserted `?`, so that one is reported but left unfixed |
| `english-comments` | a comment (line, block, or doc) containing a NON-LATIN LETTER — the user's rule: all code comments and documentation must be in English only; `Info`, report-only (translating a comment is a human task, not a mechanical rewrite). A pure comment-token scan (`RefactorSupport.collectCommentTokens`, string-aware, so non-Latin text inside a string LITERAL is legal data and never visited); the FIRST offending code unit in each token produces ONE finding for the whole comment (not one per character), spanned at that unit with a short single-line excerpt. Flags the well-known BMP non-Latin letter blocks — Cyrillic (`U+0400..U+052F`), Hebrew, Arabic, Hangul (Jamo + syllables), Hiragana/Katakana, and CJK unified — via UTF-16 code-unit range checks (no surrogate decoding; astral CJK extensions out of scope). Deliberate non-flags: GREEK is excluded (its dominant use in code comments is mathematical / symbolic — e.g. an omega anchor tag — so flagging it would be almost all false positives); Latin-extended accents (`é` / `ü` / `ñ`, e.g. in an attribution name) are Latin script, not a separate script; and emoji, box-drawing, arrows, and typographic punctuation (`— « » … ° ×`) are not letters. No parse needed (comments are dropped from the query projection), so it is grammar-agnostic by construction |
| `prefer-comprehension` | an empty-array local declaration `var/final a = []` immediately followed by a `for` loop whose only effect is `a.push(<expr>)` — the user's rule: use a `[for]` array comprehension instead of `[] + push` when each iteration produces exactly one element; `Info`, `--fix` replaces both statements with `final a<:T> = [for (x in xs) <expr>];`. Grammar-agnostic over `RefShape.forStmtKind` / `localDeclKinds` / `callKind` / `fieldAccessKind` / `exprStatementKind` / `blockStmtKind` / `identKind` (any required one unset → no-op), plus `ifStatementKinds` for the guard form and `opaqueKinds` to skip macro reification. The comprehension is transcribed from source, so a key-value `for (k => v in m)`, nested `for (a) for (b)` and one trailing `if (cond)` guard all transfer verbatim; the produced element is the push argument. Soundness gates: the initializer's source (whitespace-stripped) must be EXACTLY `[]` (`new Array()` is left to `prefer-array-literal`, whose `--fix` produces `[]` this then catches next run); every body layer must be a `for`, a single-statement block, a no-`else` `if`, or the terminal `a.push(e)`, so any second statement, an `else`, a non-push call, or a `break`/`continue` (which would be an extra statement) is a safe miss; `a` must not appear in the element, an iterable, or a guard condition (a self-reference would change semantics); `a` must be read after the loop (else it feeds no one — `unused-local`'s territory); and the `for` must be strictly adjacent to the declaration (a comment in the gap would be lost, so the pair is skipped). The fix always emits `final` (the binding becomes single-assignment, matching `prefer-final`) and preserves the type annotation verbatim |
| `prefer-find` | a manual first-match `for` loop replaceable with `Lambda.find` — the user's rule: use `.find()` instead of manually iterating to find the first matching element; `Info`, REPORT-ONLY (the `xs.find(x -> cond)` rewrite needs `using Lambda` in scope, so the import surgery and shadow analysis are out of v1). Two shapes: Form A `for (x in xs) if (cond) return x;` whose immediately-following sibling is a value-returning `return` (the `if`-body bare or a `{ … }` wrapping one return, the `for`-body optionally a single-statement block) — a `return null;` fallback is the plain `xs.find(x -> cond)`, any other value fallback the `xs.find(x -> cond) ?? <fallback>`; and Form B `var r = null; for (x in xs) if (cond) { r = x; break; }` — a null-initialized local then a loop whose `if`-body is exactly the loop-variable-to-local assignment then a `break`. Grammar-agnostic over `RefShape.forStmtKind` / `returnStatementKind` / `blockStmtKind` / `nullLiteralKind` / `identKind` / `ifStatementKinds` (any unset → no-op), plus `opaqueKinds` to skip reification; Form B additionally needs `localDeclKinds` / `exprStatementKind` / `assignKind` / `breakStatementKind` (any unset disables the break form, return form still runs). Soundness gates: the returned / assigned value must be exactly the loop variable (`return x.field` or a transformed value is `map`/`filter` territory); the `if` must have no `else`; the Form-B body must be exactly assign-then-`break` (a `continue` would find the LAST match, so the dedicated break kind is required) with a `null` initializer; a key-value `for (k => v in m)` is skipped (`.find` iterates values, not KV pairs); and the two statements must be immediately adjacent block siblings |
| `prefer-static-extension` | a STATIC UTILITY call on a configured module rewritable to extension-method style — `Lambda.map(xs, f)` -> `xs.map(f)`, `StringTools.trim(s)` -> `s.trim()`; `Info`, `--fix` rewrites it and inserts a `using <module>;` when the file lacks one. Configured per project (`"types"`, default `["Lambda", "StringTools"]`; `"addUsing"`, default true — with it false a file lacking the `using` is reported but never fixed, since the rewrite would not compile). A `types` entry may be QUALIFIED (`utils.TextUtil`): the call site matches on the SIMPLE name, the full entry drives the `using` test / insert text / `knownExtensionMethods` lookup. Shape: a `callKind` whose callee is a `fieldAccessKind` over a single `identKind` named after a configured module, with at least one argument (a zero-arg `Ext.now()` has no receiver to move); the type identifier must not be VALUE-BOUND (a local named `Lambda` shadows the module), and when the grammar knows the module's extension methods the called method must be among them. THE SHADOW GATE is the heart of it: Haxe resolves an instance member BEFORE a static extension, so a receiver type (or supertype) declaring the same name — `Lambda.map(arr, f)` -> `arr.map(f)` silently becoming `Array.map`, which COMPILES while changing the result type — is dropped outright via `SymbolIndex.typeDeclaresMember` / `supertypeDeclaresMember`. A member reachable only through a link those two do not follow — a `typedef` alias, a `@:forward` abstract's underlying — is caught one gate later by `typeProvablyLacksMember` (which DOES follow both) failing to prove absence, and degrades to REPORT-ONLY rather than dropping. Likewise dropped: a `Dynamic` receiver (dispatches nothing at runtime) and a conflicting second `using` that could also supply the method (deliberate disambiguation). The receiver's nominal comes from `RefactorSupport.valueTypeNominal` plus `literalTypeNames` for a string literal; a call / ternary / `?.` chain / bare `this` / `Null<…>` type stays unresolved, and a NUMERIC literal is a deliberately skipped fixable class rather than an unresolvable one (`255.hex(2)` compiles fine under `using StringTools`, but `5.trim()` types as the float `5.` applied to `trim`, so safety depends on the literal's exact spelling), and an unresolved receiver or an unresolvable member closure degrades the finding to REPORT-ONLY with its own message. The fix emits TWO edits per site (delete `[call.from, recv.from)`, rewrite `[recv.to, rest.from)` to `.m(`), so nested candidates compose in ONE pass; a comment in either dropped region refuses the edit. The inserted `using` goes ABOVE any existing `using` run, never below: Haxe resolves static extensions in REVERSE declaration order, so appending would give the new module top priority and silently re-target the file's existing extension calls. When the module's WHOLE body sits inside one `#if … #end` region (a debug- or platform-only module, whose import run is inside the guard too), the insert joins THAT header rather than landing on an island between `package` and the `#if`, and a `using` already declared there counts as present — the same descent decides all three `UsingScan` questions. Three gates keep it sound: one region at the top level with no type declared outside it, the region declaring a type itself, and no `#else` / `#elseif` seam of its OWN (branches project as flat siblings, so an anchor cannot be told from one in another branch); any of them failing falls back to the top-level position. `fix` re-derives its gates against the plugin's RESOLUTION index in preference to the report-scoped one it is handed (the narrower scope would degrade every std-typed site to report-only). Blind spots: a fully qualified call site (`haxe.io.Path.withoutExtension(p)`) never matches the shape (the callee root is a field-access chain, not an identifier), and an `import.hx`-provided `using` is invisible. Requires `plugin is TypeInfoProvider` |
| `loop-guard` | a loop (`for` / `while`) whose braced block OPENS with a bare `if (g) continue;` guard — that block being the loop body itself, or the then-branch of a header `if` the loop body already is — the user's rule: combine `for` + `if` into `for (...) if (...) {`, preferred over a leading `if (!cond) continue;` that inverts the condition and wastes a line; `Info`, `--fix` lifts the guard into the loop header with an INVERTED condition: `for (x in xs) { if (c) continue; REST }` → `for (x in xs) if (INV) { REST }`. TWO ARMS share that equivalence — a `continue` in the FIRST position of a block IS "do not enter the body". The LIFT arm is the one just shown (the loop body IS the block, and the inverted guard becomes the header the loop did not have). The MERGE arm covers a loop whose body is ALREADY a lifted header `if (c) { … }` whose braced block opens with the guard: `for (x in xs) if (c) { if (g) continue; REST }` → `for (x in xs) if (c && INV(g)) { REST }`, joining the inversion onto the existing header with `RefShape.andOperatorText` instead of creating a second `if`. Precedence is handled on BOTH sides: the KEPT header condition is parenthesised when its kind is in `RefShape.andLowerPrecedenceKinds` (the list `collapsible-if` merges with), and the INVERSION is parenthesised when it binds looser than `&&` — a De Morgan `a && b` guard negates to a top-level `\|\|`, so `if (!o.locked) { if (o is P && !(t && n == o.type)) continue; … }` merges to `if (!o.locked && (!(o is P) \|\| t && n == o.type)) { … }`. That slot is SYNTHETIC (there is no `&&` node to pass as a parent), so `NegationScan.negateConditionText` / `BooleanLogicSupport.negateCondition` take the slot by KIND (`slotKind`) where `simplify-negated-compound` passes a parent node. The inversion is sound over IEEE floats AND over `null`: `!e` → `e` (stripping a redundant paren), `a == b` → `a != b` and `a != b` → `a == b` (NaN-safe — `NaN == x` is false, `NaN != x` true, so `!(a == b)` ≡ `a != b`), and everything else — ordered comparisons `<` / `<=` / `>` / `>=` (whose flip differs from the wrap under NaN and under a null operand alike, UNLESS both operands resolve to a type `<` orders TOTALLY — `Int` / `UInt`, or a non-float literal — through the run's `SymbolIndex`, which licenses the flip `a >= b`; `String` is NOT such a type, Haxe having no non-nullable string), `&&`, `\|\|`, a call — is wrapped `!(c)` rather than flipped, with the parens dropped when `c` binds tighter than unary `!` (a bare identifier / call / field access → `!c`). Grammar-agnostic over `RefShape.loopStatementKinds` (loops whose body is the last child) / `continueStatementKind` / `ifStatementKinds` / `blockStmtKind` (any unset → no-op), plus `notKind` / `eqKind` / `notEqKind` / `parenKind` and the atomic-expression kinds for the inversion, and `opaqueKinds` to skip macro reification. Gates: the inversion must be WORTH doing — when the negation engine has to DECLINE an ordered-comparison flip it knows how to perform (the operands are not provably free of NaN and `null`), the lifted header would read `if (!(x < 10))`, which buys nothing over the `continue` guard, and the site is NOT flagged (`NegationScan.negationIsClean`); a wrap the engine could never avoid — `!(a is B)`, `!(a ?? b)` — is not a decline and still fires. Also: the guard is the body's FIRST statement, the body a braced block with at least one statement after it (a guard-only body is a no-op loop, left alone), and the guard a bare `if` (no `else`) whose then-branch is exactly `continue;` (bare or braced-single). A CASCADE — the statement right after the first guard is ANOTHER `if`-continue — is NOT flagged (the user keeps sequential `continue` guards, each introducing its own checks, rather than nesting them); a later `continue` deeper in the body is fine (the wrapped body still continues the same loop) and does not block the flag; an unbraced body, an `else` branch and a comment inside the guard (the rewrite would drop it) are safe misses. The MERGE arm adds three gates of its own: the header `if` must carry NO `else` (that branch fires on `!c` today and would fire on `!c \|\| g` after the merge), its then-branch must be a BRACED block, and BOTH `&&` seams must be set — `RefShape.andOperatorText` for the joiner and `RefShape.logicalAndKind` for the slot the inversion is parenthesised against (either unset, only the LIFT arm runs; a merge emitted without the slot would drop the pair and re-associate) |
| `guard-return` | a block whose LAST TWO statements are a bare `if (cond) { BLOCK }` (no `else`) and a lone trailing `return TAIL;`, invertible into an early-return guard so the bulk of the work sheds an indentation level — the user's rule: a guard clause reads better than a positive branch wrapping the whole body; `Info`, `--fix` rewrites the pair to `if (!cond) return TAIL; BLOCK`. The inversion runs through the same `NegationScan.negateConditionText` engine as `loop-guard` / `guard-continue`: De Morgan pushed inward (`a && b` -> `!a || !b`), `==` / `!=` flipped NaN-safely, an ordered comparison (`< <= > >=`) kept wrapped `!(a < b)` (its flip differs under NaN) unless BOTH its operands resolve to a type `<` orders TOTALLY — `Int` / `UInt`, or a non-float literal, but never `String`, which Haxe cannot prove non-null — through the run's `SymbolIndex`, which licenses the flip (`x.length > 0` → `x.length <= 0` rather than `!(x.length > 0)`), a bare atomic negated `!c`; a comment inside the condition falls back to the verbatim `!(cond)` wrap. A STRANDED-NARROWING gate also declines De Morgan: an `&&` chain negates into a flat `||` chain, and Haxe strict null-safety carries a narrowing fact into a later `||` operand from the FIRST operand only, so `a != null && b != null && p(a.length, b.length)` (which compiles) would become `a == null || b == null || p(a.length, b.length)` (which does not) — such a condition takes the verbatim wrap instead, whose interior is the original `&&` chain; when that condition already spans lines the site takes no fix at all (the nested multi-line `!( … )` would read worse than the branch it replaces). Runs to a fixpoint, so `if (p) { if (q) { … return 1; } return 2; } return 3;` flattens over two passes. Gates (all fail closed): the inversion must be WORTH doing — a condition whose negation makes the engine DECLINE an ordered-comparison flip (operands not provably free of NaN and `null`) is NOT flagged at all, since `if (!(a < b)) return …;` reads worse than the positive branch it would replace (`NegationScan.negationIsClean`; a wrap the engine could never avoid, `!(a is B)`, is not a decline and still fires). Then: the `if` and the `return` are the block's last two statements IN THAT ORDER (an intervening statement would be reordered); the `if` is statement-position, has NO `else`, and its BRACED then-branch is TERMINAL (`ControlFlowSupport.isTerminal` — without it, falling off the end reached the trailing `return` before the de-nest and would fall out of the block after) and holds at least TWO statements (a one-statement branch is `prefer-ternary-return`'s shape, left alone). Refused: a `#if … #end` region anywhere in the rewritten span (its interior is preserved raw and the de-nest re-indents), a comment in the dropped `if (` / `) {` glue, in the gap before the trailing `return`, or trailing that `return`'s line, and a top-level then-branch local re-declaring a preceding sibling's name (de-nesting would turn a nested shadow into a same-scope re-declaration). A comment inside the condition, the then-branch or the returned expression rides along verbatim. A SECOND ARM covers the IMPLICIT TAIL: a bare `if (cond) { BLOCK }` that is the LAST statement of a provably-Void FUNCTION BODY becomes `if (!cond) return; BLOCK` — there is no trailing `return` to move, so the rewritten span ends at the `if` and the guard returns no value (the literal `return;`, as `guard-continue` emits the literal `continue;`). Tail position is the function's block body, plus every `CondBranch` of a `#if` region that is the body's LAST statement (the branches of a region are mutually exclusive configurations, so each is the tail in its own); a loop body, a nested `{ … }` block, an `if`'s own then-branch and a lambda body are NOT tails — a trailing `if` inside a loop stays `guard-continue`'s turf, and nested Void tails flatten one level per `--fix` pass. The Void proof reads the `blockBodyKind` / `voidTypeName` seams plus `CheckScan.returnAnnotationText` (shared with `redundant-cast-type`, which tells a return annotation from a type-parameter CONSTRAINT by position relative to the parameter list — the two project the same node kind in the same child slot): a function with NO return annotation qualifies when its own scope holds no value-`return` (nested / inline-local functions and lambdas excluded) — that infers `Void`, a block body does not yield its last expression's value, and a `throw`-only body infers `Void` too, so no throw guard is needed; a function that DOES annotate qualifies only when the annotation's source text is exactly `Void`, and that branch is the SAFETY gate — `function f():Int { throw 'x'; if (c) { a(); b(); } }` compiles, has no value-`return`, and a spliced `return;` would not. Sharing the annotation locator is load-bearing rather than tidy: `<T: Void>` IS legal Haxe, so a hand-rolled "child before the body" read would prove Void on a parameterless constrained generic that returns something else. Constructors qualify through the inference branch. LAMBDAS ARE DELIBERATELY EXCLUDED — an arrow function with a block body DOES yield its last expression's value, so de-nesting could silently change its inferred return type. This arm shares every gate but the TERMINAL one, which it does not need: its "tail" IS falling off the end of the body, which is exactly where the de-nested run now ends, so both forms exit identically (and no `break` / `continue` hazard can arise, a function body never being inside a loop of that same function). It additionally refuses a comment trailing the `if`'s closing brace, which would otherwise end up documenting the last de-nested statement. Disabled outright unless `voidReturnKind`, `blockBodyKind` and `valueReturnKinds` are all set; the explicit-pair arm stays live regardless |
| `guard-continue` | a loop (`for` / `while` / `do … while`) whose braced body's LAST statement is a bare `if (cond) { … }` (no `else`) preceded by at least one other statement, de-nestable into an early-`continue` guard so the body sheds an indentation level — the user's rule: sequential null checks each introducing their own variable read better as flat `continue` guards than as growing nesting; `Info`, `--fix` rewrites it to `if (!cond) continue; BODY`. Runs to a fixpoint, so a two-level `if (a) { … if (b) { … } }` nest flattens over two passes. The inversion runs through the same `NegationScan.negateConditionText` engine as `guard-return` / `loop-guard`: De Morgan pushed inward (`a && b` -> `!a \|\| !b`), `==` / `!=` flipped NaN-safely, an ordered comparison (`< <= > >=`) kept wrapped `!(a < b)` unless BOTH its operands resolve to a type `<` orders TOTALLY — `Int` / `UInt`, or a totally ordered literal, but never the `String` NOMINAL, which Haxe cannot prove non-null — through the run's `SymbolIndex`, which licenses the flip; a seam-less grammar, a comment inside the condition the De Morgan rewrite would drop, or a condition whose flattened `\|\|` chain would STRAND a null-safety narrowing (`NegationScan.narrowingStranded`) falls back to the verbatim `!(cond)` wrap, which preserves the comment and compiles either way. The same worth gate as `guard-return` applies (`NegationScan.negationIsClean`): a condition whose negation makes the engine DECLINE an ordered-comparison flip is NOT flagged at all, since `if (!(a < b)) continue;` reads worse than the positive branch it would replace (a wrap the engine could never avoid, `!(a is B)`, is not a decline and still fires). Gates (all fail closed): the `if` is the loop body's LAST statement — exactly, or followed ONLY by a HOISTABLE TAIL (independent plain `=` assignments of read-free literals — bool / null / numeric, never strings, whose interpolation could read state the body writes — to locals that are not the iterator and are mentioned nowhere in the `if`), emitted ABOVE the guard, which is order-safe but only when the then-branch cannot escape the iteration early (no `return` / `throw` / grammar exit outside nested functions, no `break` / `continue` outside an inner loop): an escaping path SKIPPED that tail in the original, so hoisting would newly execute it. The `if` must also be preceded by ≥1 statement or a non-empty tail (a SOLE-`if` body is the positive `for (…) if (cond) …` combine form, which `loop-guard` owns and this check does not fight), have NO `else` (a branch the `continue` form would lose), a braced non-empty then-branch, and DIRECT-child position in the loop's own body block (an `if` nested in an inner loop / `switch` / `try` targets a different `continue` or a `finally`). Also refused: a comment in the `if (` or `) {` glue, or between the `if` and its hoisted tail, that the rewrite drops (comments in the condition or the then-body ride along verbatim). THE COLLISION RENAME: a then-branch top-level local whose name collides with a preceding sibling local or the loop iterator is NOT refused — de-nesting would widen it into the loop scope and same-scope re-declare it — it is AUTO-RENAMED inside the moved block to the first free `<name>2`…`<name>99` (`path` -> `path2`: the binder token plus every read, plain identifiers and `$name` interpolations alike), "free" judged against the WHOLE parse tree, since an unqualified read of a same-named class field binds silently. That rename — and with it the de-nest — is refused when the name is re-declared DEEPER in the then-branch, when a nested function / lambda subtree mentions it, when the grammar exposes no string-interpolation seam, when every suffix is taken, when an occurrence sits AHEAD of the declaring statement's end (there it still binds the OUTER name, as in `final path = path + '/sub';`), or when the then-branch holds a standalone textual occurrence no rewritten span accounts for (an invisible binder — a `catch` variable, a `k => v` value iterator, a lambda parameter — or a mention in a comment or plain string). A flow exit (`break` / `continue` / `return` / `throw`) anywhere in the then-branch does NOT refuse the plain (tail-less) de-nest: the statements move into the SAME loop body, in the same function, under the same condition, so every jump target is unchanged — only the HOIST variant re-checks escapes |
| `map-keys-lookup` | a `for (k in m.keys())` loop whose body re-looks-up the same map by the same key (`m[k]` or `m.get(k)`) — the user's rule: use `for (k => v in m)`, since the keys-then-lookup form re-looks-up every value; `Info`, with an autofix: it drops `.keys()`, binds the value once, and replaces every `m[k]` / `m.get(k)` with that binding — reusing the name of a leading immutable value local (and deleting its declaration) when its annotation is a grammar-guaranteed builtin, so the annotation cannot be a nullable alias whose widening the rewrite would lose. The iterable must be EXACTLY `<recv>.keys()`, where `<recv>` is an identifier or a PATH of plain field accesses over one (`session.files`, `this.files`); a link that is a call, an index access or a `?.` disqualifies the path, since those carry side effects of their own. Every gate below is keyed on that whole path, and a leading `this` is normalised away in the SKIP gates (never in the match gate — widening a skip can only lose findings, widening the match would equate a local with a same-named member). The body must contain at least one lookup of that same path by the same key, as an index access `m[k]` or a `m.get(k)` call (a different key `m[j]` or a different map `n[k]` is not a match). Grammar-agnostic over `RefShape.forStmtKind` / `identKind` / `callKind` / `fieldAccessKind` / `indexAccessKind` (any unset → no-op), plus `writeParentKinds` for the mutation gate, `localDeclKinds` / `paramKinds` for the re-binding gate, `nullableIndexTypeNames` / `nullableWrapperTypeNames` and `SymbolIndex` member resolution for the type gate, and `opaqueKinds` to skip macro reification. Conservative gates (a miss over a wrong flag): any mutation of the map in the body — `m[...] =` (or a compound-assign / increment via `writeParentKinds`), or a `m.set(…)` / `m.remove(…)` / `m.clear(…)` call — SKIPS the loop, since iterating `keys()` while mutating may be deliberate; so does RE-BINDING what the path denotes (`m = other`, `o.files = other`, `o = p` — a write target that is a prefix of the iterable, inclusive of equality; a deeper write like `o.files.inner = 1` is the mutation gate's business, not this one); a re-binding of the path's root or the key name anywhere in the body (a local, a nested-function parameter, or a nested loop variable) SKIPS it; and, when the plugin provides type info, a receiver whose type resolves to a concrete NON-map-family nominal is SKIPPED — for a path this resolves the ROOT and then walks each field segment's member type, which is CROSS-FILE, so the rule is registered as full-scope in the `--fix` fixed-point loop (on a changed-files subset a type declared elsewhere would read as unresolvable and re-flag the very loops the gate skips). An unresolvable receiver still flags — a deliberate false-positive tolerance, identical for paths and identifiers, not a proof of Map-ness. The finding is spanned at the `.keys()` iterable |
| `prefer-range-loop` | a manual counter loop — a `var i = A;` (NOT `final`) immediately followed by `while (i < B) { … i++; }` — that a range `for` replaces: the user's rule is `for (i in A...B)` instead of a `while` with a manual `i++`; `Info`, REPORT-ONLY in v1 (the rewrite is sound only under every gate below, and several — `i`-not-read-after, no-other-write — are exactly the analyses most likely to hide a subtle bug, so the detector ships before the transform). The suggestion transcribes `A` and `B` verbatim from source as the range bounds. Grammar-agnostic over `RefShape.whileStmtKind` / `ltKind` / `postIncrKind` / `blockStmtKind` / `exprStatementKind` / `continueStatementKind` / `mutableLocalDeclKinds` (any unset → no-op), plus `identKind` / `writeParentKinds` (the write scan), `numericLiteralKinds` (a literal bound), `localDeclKinds` (the shadow gate) and `opaqueKinds` to skip macro reification. Soundness gates (all required): the declaration is a single-variable `var` (a `final` cannot `i++`, and a multi-declaration `var i, j = n` — top-level comma — is skipped) with one initializer child, the immediately-preceding block sibling of the `while`; the condition is exactly `i < B` with the loop variable on the LEFT (`i <= B`, a reversed `B > i` and a `!=` form are not a range); the body is a braced block whose LAST statement is exactly `i++;` (a `++i` / `i += 1` / `i = i + 1` is not matched) and no OTHER statement writes `i`; `i` is not read after the loop within the enclosing scope (a range `for` scopes `i` to the loop, whereas the `while` leaves `i == B` visible afterwards — positional `RefactorSupport.referencedInRange`); `B` is stable — a numeric literal, or a plain identifier that is not `i` and is never written in the body (`A...B` evaluates `B` ONCE, `while (i < B)` each iteration, so a call / field / array-access bound is skipped); the body contains no `continue` (which in the `while` form SKIPS the trailing `i++`, but in a range `for` advances — a `break` is fine); the body holds at least one statement besides the `i++`; and nothing in the body re-declares `i`. The finding is spanned at the `var i = A;` declaration |
| `prefer-keyvalue-loop` | an INDEXED `for` that only wanted the element — `for (i in 0...X.length)` whose body OPENS with `final v = X[i];` — which Haxe's key-value iteration writes directly: `for (i => v in X)`, with that first statement gone; `Info`, `--fix` splices the header and drops the declaration. DEFAULT OFF (`DefaultOff`) — the two spellings are equivalent, so which one a project wants is a style choice: opt in with `"prefer-keyvalue-loop": { "enabled": true }`. The index stays BOUND, which is what makes this worth having over `for (v in X)`: an inner `for (j in i + 1...X.length)`, an `i`-keyed lookup elsewhere in the body, a `trace(i)` all keep working untouched (a body that never reads `i` again is still rewritten to the key-value form — collapsing THAT case to `for (v in X)` is a different rewrite and deliberately out of scope). Grammar-agnostic over the shared `LoopScan` seams (`forStmtKind` / `blockStmtKind` / `fieldAccessKind` / `indexAccessKind` / `callKind` / `numericLiteralKinds` / `localDeclKinds`, plus `nullSafeAccessKind` / `forceFieldAccessKind`, `writeParentKinds`, `lambdaKinds` / `localFunctionKinds` and `opaqueKinds`) and `intervalKind`; any unset → no-op. `length` is the one language-specific token, spelled as a constant. Soundness gates (all required): the iterable is exactly `0...X.length` for a BARE IDENTIFIER `X` (a path receiver such as `this.items` is skipped — the type gate reads a BINDING's annotation and a check's `run` has no `SymbolIndex` to walk a path with; a bare identifier that binds to a FIELD does resolve and is accepted); the body is a braced block of ≥2 statements whose FIRST is a single-variable `var`/`final` (annotated or not, no multi-declaration comma) initialised by exactly `X[i]`; `X`'s LENGTH cannot move — every mention of `X` in the body must be a READ of `length` on it or an index READ `X[e]`, so a call on it (`X.push(v)`), a method VALUE taken off it (`f(X.pop)`), a bare mention handed to a callee (`f(X)`), a write to it and a write THROUGH it (`X.length = 0`, `X[k] = v`, which on Haxe arrays EXTENDS) are all refused — because `0...X.length` evaluates the bound ONCE while `for (i => v in X)` re-asks the iterator every step, so an append would turn a terminating loop into a runaway one. That scan is a whitelist of two positions, and its limit is that it is BODY-LOCAL: an alias handed out before the loop, or a call that mutates `X` through a field the callee owns, is outside what a per-file check can see — which is part of why the rule is `Info` and opt-in. Also required: EXACTLY ONE `X[i]` (any other would have to become `v`, a rename this rule does not attempt); nothing writes `i` or `v`; no statement after the consumed declaration re-declares `i`, `v` or `X` as a local `var`/`final` (binders of other kinds — a `catch` variable, a lambda parameter, a case capture, a nested loop binder — are not scanned and need not be: each shadows the moved header binding exactly as it shadowed the block-scoped one); and `i`, `v`, `X` are three distinct names. No closure gate is needed, unlike the sibling rule: a block-scoped `final v` and a Haxe loop binder are both fresh per iteration. A container that RESOLVES to something other than `Array` is not reported at all (the message would name a form that does not compile for it); an UNRESOLVED one still reports, and there the FIX additionally needs the element type PROVABLE, since it drops the declaration and with it any annotation: `X`'s binding must be declared `Array<E>` and the annotation — when present — exactly `E`. A widening annotation (`Dynamic`, a supertype), or a comment anywhere in the replaced region THROUGH THE END OF THE DECLARATION'S LINE (so a trailing comment cannot silently migrate onto the loop header), leaves the finding report-only. The finding is spanned at the `for` |
| `dead-binder-counter-loop` | a `for`-in that iterates a collection ONLY to count it — `var i = 0;` immediately followed by `for (x in coll) { … i++; }` where NOTHING reads the binder `x` — which a range `for` says directly; `Info`, `--fix` rewrites the pair to `for (i in 0...coll.length) { … }`, dropping both the declaration and the trailing increment. DEFAULT OFF (`DefaultOff`) — the input compiles and behaves correctly, so replacing it is a style choice: opt in with `"dead-binder-counter-loop": { "enabled": true }`. A container with no `length` is counted through `Lambda.count` (`for (i in 0...coll.count())`) and a `using Lambda;` is inserted when the file lacks one — which costs ONE extra traversal the original did not pay (`count()` walks, then the range loop walks again); a rival `using` that could also supply `count` refuses those rewrites (the inserted one would lose to it, silently retargeting the call), and the `using` is decided only from the edits that SURVIVE containment filtering, so a swallowed nested rewrite cannot leave an unused one behind. Grammar-agnostic over the shared `LoopScan` seams plus `postIncrKind` / `continueStatementKind` / `exprStatementKind` / `mutableLocalDeclKinds` and `GrammarPlugin.controlFlowSupport`; any unset → no-op. The pair scan runs over STATEMENT LISTS only (`ControlFlowSupport.blockKinds()`), which deliberately EXCLUDES the conditional-compilation node: a `#if` region projects as one node holding every branch's statements as flat siblings, so pairing across it would splice through the `#else` and delete it — a pair written inside a `#if` is simply not matched. Soundness gates (all required): the declaration is a single-variable `var i = 0;` (NOT `final` — the counter is incremented) whose declared type, if any, is `Int`, and it is the immediately-preceding sibling of the `for`; the binder is DEAD — proved by a TEXT scan over the body span, not a node walk, because a bare `'$x'` interpolation read and a `macro` reification subtree are both invisible to the tree and a missed mention here is a licence to DELETE the binder; `i` is a pure counter — the trailing `i++;` is its ONLY write, it is the body's LAST statement, nothing after the loop reads it (a range `for` scopes `i` to the loop, whereas the `var` leaves the final count visible), nothing re-declares it in the body, and no closure in the enclosing scope captures it; no `continue` (which in the `for`-in form SKIPS the increment, leaving `i` below the element count, but in a range `for` advances — a `break` is fine, both forms stop at the same iteration); and `coll`'s length cannot move (the same body-local stable-collection scan `prefer-keyvalue-loop` uses, with the same documented aliasing limit). Unlike its sibling, this check does NOT report an unresolved container: the replacement TEXT depends on the type, so a finding it cannot spell would be noise. `coll` must be a bare identifier whose binding is declared `Array` / `List` (→ `length`) or `Map` / `StringMap` / `IntMap` / `ObjectMap` / `EnumValueMap` / `BalancedTree` (→ `count()`), spelled bare or `haxe.`-qualified — the match is on the SIMPLE nominal, so a qualified project type of the same name is refused outright and an unqualified one is the residual (it then fails LOUDLY at compile time). Everything else — a path receiver, a call, an unannotated binding, a range — is silently skipped. That list is a WHITELIST on purpose: `Lambda.count` needs an `Iterable`, and "everything `for (x in …)` accepts" is strictly wider — an `Iterator` is accepted by `for` but CONSUMED by a count, which would silently run the rewritten loop zero times, and `haxe.ds.HashMap` iterates but is an abstract that does not unify with `Iterable` at all; `Vector` is off the list because a geometry `Vector`'s `length` is a magnitude, not a count. A comment in either dropped region (the declaration plus the `for` header, or between the increment and the body's `}`) refuses that rewrite, leaving the finding report-only; the body interior is carried verbatim, so comments there survive. The finding is spanned at the `var i = 0;` declaration |
| `trivial-getter` | a property accessor that does nothing but forward a private backing field, collapsible into direct storage; `Info`, `--fix` rewrites the property, deletes the dead accessor(s) and the backing field, and renames every in-class reference of the backing field to the property name. Four shapes: `(get, never)`/`(get, null)` with a trivial getter → `(default, null)` (the original read-only case); `(get, set)` with a trivial getter and a REAL setter → `(default, set)` — external writes to the backing field outside `set_x` (each a deliberate setter bypass; renaming one would start routing through the setter) pick the arm: NONE → the plain collapse; 1..`maxBypassWrites` (an `apqlint.json` option on the rule, default 3), ALL statement-level (`_x = …;` a whole statement) → the collapse proceeds and `--fix` prefixes each such write with `@:bypassAccessor` (a bypass write on `(default, set)` is a direct field write — the original semantics, exactly); MORE than the cap, or any write nested inside a larger expression → no collapse, `--fix` instead marks the trivial getter `inline` (the finding is skipped entirely when the getter is already `inline` or carries `override`). Reads anywhere are safe since `(default, set)` reads are direct, and a backing-field declaration initializer (or the one movable constructor-init literal) moves onto the property; `(get, set)` with BOTH accessors trivial → a plain `var`; a REAL getter with a trivial setter → `(get, default)` — READ-GATED: no read of the backing field outside `get_x` (a `(get, default)` getter reading the property compiles to a direct physical read, verified against Haxe codegen), where compound-assign/incr/decr targets COUNT as reads (`x += 5` lowers to `get_x()` + set) and only a pure `=` target does not. Every class body — plain / `final` / `abstract class` — is inspected; the accessor must be the standard `get_`/`set_` pair, non-`dynamic`, its body exactly `return _x;` / an equivalent single assignment, and the backing field a PRIVATE same-class field whose declared type MATCHES the property's (`declaredTypesMatch` — a getter performing an implicit conversion, e.g. `Array` -> `ReadOnlyArray`, would retype the merged slot and break mutating uses). A renamed reference is qualified `this.x` whenever ANY binding of the property name is visible in the enclosing function — parameter, local (`static` local and multi-var continuation included), loop / comprehension iterator, the value slot of a `for (k => v in m)` header, catch variable, case-pattern capture (`case x:` and `case var x:`), local function (plain / `inline`), lambda parameter — and `C.x` for a static property or inside a static method, so a collapse can never re-bind a reference to the shadowing binding (`if (color == _color)` in a `for (color in …)` loop must not become the always-true `color == color`). Assumes no direct `get_x()`/`set_x()` call sites (the rule family's standing accessor assumption) A property, backing field and getter written inside a member-position `#if … #end` are scanned too, and the collapse's edits — accessor clause, field deletion, getter deletion, modifier runs included — land inside the branch. Every member the collapse touches — property, backing field, accessors — must sit in ONE branch (a getter duplicated per branch would otherwise lose the other branch's body silently), a guarded property whose backing field is mentioned outside its branch is refused (the collapse renames that field into the property's name, which only exists where that branch compiles), and a collapse that would empty a region is refused too. |
| `prefer-inline` | a method whose inlining BUYS something — the body is one of three benefit classes: EMPTY (the call compiles away), an accessor / thin forward / trivial mutator (a bare field chain, a call through a chain with only chain / literal arguments, an assignment / increment over a chain — collapses to a direct read / write / call), or a constant / small-arithmetic expression (literals, chains, operators only — folds at the call site), the latter two bounded by a 32-AST-node budget (`MAX_BODY_NODES`); `Info`, `--fix` inserts `inline ` before the `function` keyword. Everything outside the benefit classes — an allocation (`new`, array / object / interpolated-string literal, a `macro` reification), a lambda / computed argument, a loop, a switch, a multi-statement body — is never a candidate: inlining it would only duplicate codegen at every call site. Every class body — plain / `final` / `abstract class` (`CheckScan.isClassBodyKind`) — is inspected. Soundness gates (a miss over a wrong flag), each conservative: a method referenced ANYWHERE in scope as a VALUE — a method-value cannot be inlined — skips it, detected by a project-wide name scan for a value-position occurrence (not a call callee) via `IdentExpr` / `FieldAccess` / `SafeFieldAccess` / `ForceFieldAccess` (so `.bind`, a callback argument, a stored reference — resolvable or not — all block it); an `override` method and a method OVERRIDDEN by a subtype (`SymbolIndex.hasSubtype` plus a member-name lookup across strict subtypes) is skipped (inlining breaks the override); a method filling an abstract-superclass slot is skipped (`SymbolIndex.supertypeDeclaresMember` — Haxe requires no `override` on such an implementation); a method an implemented interface declares is skipped (`SymbolIndex.typeProvablyLacksMember`, which also refuses an unresolvable interface); a `dynamic` / `macro` / constructor (`new`) / `@:keep` method, any method carrying a compiler annotation not on the inline-neutral list (the target-binding family `@:native` / `@:hlNative` / `@:cs.native` / … and the code-injection family `@:functionCode` / `@:functionTailCode` make the written body a placeholder the backend discards, so inlining substitutes the placeholder and the redirect never happens — a USER annotation is accepted, no backend reads it), an `extern` field, and any method whose name is passed to `Reflect.*` as a string literal anywhere (reflection-accessed), is skipped; and a self-recursive body (a bare `foo` / `this.foo`) is skipped — a delegation to a same-named method on ANOTHER receiver (`other.foo()`) is not self-reference and stays a candidate. An already-`inline` method is a no-op. The finding is spanned at the method's `function` keyword; mirrors `trivial-getter`'s soundness model A method written inside a member-position `#if … #end` is scanned too, branch by branch — so an `override` in one branch binds to that branch's method only, and a guarded `inline` is no longer missed — and the `inline` insert lands inside the branch. A method whose modifier run only SOME builds see is skipped. |
| `redundant-bypass-accessor` | an `@:bypassAccessor` on a plain `=` assignment whose lvalue (bare `x` or `this.x`) resolves via `SymbolIndex` to an own-class member with NO set-accessor — a plain `var`/`final`, or a property whose write slot is `default`/`null`/`never` — so the meta bypasses a setter that does not exist (a stale leftover of a former `(default, set)`); `Info`, `--fix` removes the meta token, preserving other stacked metadata. Not a risky fix: with no setter the assignment already compiles to a direct field write, so dropping the meta is behavior-identical. Compound assigns/incr/decr are distinct node kinds and never match (their reads would need the getter side too); `@:bypassAccessor` on property READS never matches; an unresolved / local / inherited / cross-class lvalue is a safe miss |
| `orphan-accessor` | a `get_X` / `set_X` method that no property slot ever reaches — the accessor half of a property whose declaration was changed to `(default, set)` / a plain field, or removed, while the method stayed behind. Haxe calls an accessor only because the property's `(get, …)` / `(…, set)` clause names it, so with the slot gone the body is dead weight that still reads like live behaviour. `Warning`, DEFAULT OFF (opt in via `apqlint.json` `"rules": { "orphan-accessor": { "enabled": true } }`, or an explicit `--rule`), `--fix` DELETES the method with its modifier/metadata run and its leading doc comment. The property is resolved through the WHOLE inheritance chain — the class, then its `extends` superclass and every `implements` interface, transitively — and a slot counts as declared when the accessor ident is anything but `default`/`null`/`never`, so `dynamic` declares it (`MemberInfo.hasGetter`/`hasSetter`, populated only where the plugin supplies a `TypeInfoProvider`, so the rule is meaningful only where accessors are reported). An `override` accessor needs no property redeclaration in the subclass. Statics and instance members are separate namespaces, so a candidate matches only a member of its own staticness; its staticness and its `@:keep` come from the modifier/metadata run, which resets at every MEMBER and is read per conditional BRANCH (a `static` only some builds see would stop an instance accessor pairing, so a run the branches disagree on refuses the member) (a `static` on a preceding FIELD is that field's). Haxe resolves an accessor UPWARD from the property, so a property declared by a SUBTYPE is legitimately served by a method inherited from here — checked via `SymbolIndex.subtypeDeclaresMember` on the found-nothing arm only, since an `X` found at or above the class forbids a subtype redeclaring the field; that query walks from the subtype DECLARATION and treats an ambiguous supertype link as a MAY, because here `true` means leave the method alone. Three arms: (1) `X` IS declared in the chain but WITHOUT the slot — proven orphan, and proven even under an unresolvable supertype, since Haxe forbids redeclaring an inherited field so a second `X` cannot hide behind it; (2) no `X` anywhere in or below the chain AND every supertype link resolved — proven orphan; (3) no `X` but a supertype did not resolve — `Info`, report-only, never fixed. Two macro shapes take the class out of scope: `@:build` on the class itself, and `@:autoBuild` on ANY ancestor (that one generates into DESCENDANTS, not its carrier, so it is read while walking the chain — the canonical `@:autoBuild interface` + `implements` idiom). A supertype the index cannot resolve by IMPORT VISIBILITY — Haxe needs no import for a PARENT package, which `simpleRefInScope` does not model — is still walked when its simple name has exactly one project-wide declaration, but SPECULATIVELY: it may prove a slot EXISTS, never that the property is declared without one, and the link stays counted unresolved, so such a chain is never deleted from. The DELETE adds four gates: no report file skip-parses; neither the class nor the member is `@:keep`; the method name has ZERO direct call or value references in report scope (no `IdentExpr`/`FieldAccess`/string-interpolation ident carries it); and no string literal there names it — an INTERPOLATED string is matched the other way round, its static `Literal` fragments collected and a fragment CONTAINED IN the name blocking the delete (`'get_$suffix'` may name it at runtime; fragments shorter than an accessor prefix are ignored). That zero-reference gate is NOT the orphan test — a REAL accessor is invoked through property access and shows zero textual calls too, which is exactly why the orphan test is the property-slot absence; it only catches a `get_X` that is also called by hand. REPORT SCOPE bounds every scan here, so a subdirectory run narrows both halves: `--fix` can delete a method the rest of the project calls, and a REPORT can raise a `Warning` on an accessor whose property a subtype outside the scope declares — run it whole-project. Scope is class bodies (`class` / `final class` / `abstract class`): an `interface` declares no accessor bodies, and an `abstract` type's accessors are left alone. Registered in the `--fix` loop's full-scope set, since the chain resolution, the subtype query and the reference scans all mis-answer on an active subset An accessor written inside a member-position `#if … #end` is scanned too, branch by branch; the property slot is resolved through the SymbolIndex, which collects every branch's members at once, so a slot in ANY branch pairs and the accessor stays unreported. Its deletion carries the doc / modifier run from inside the region, and is withheld when it — together with the pass's other deletions in the same class — would take a region's member count to zero (an emptied region is a shape the grammar does not model, and the rejected splice would drop every other edit the pass had for that file). |
| `redundant-replace-loop` | a `while (x.indexOf(S) != -1) x = x.replace(S, B);` loop — redundant BY CONSTRUCTION, since `StringTools.replace` already replaces EVERY occurrence of `S` in one call (verified live), so the loop either does nothing extra or never stops. **DEFAULT OFF** (opt in via `apqlint.json` `"rules": { "redundant-replace-loop": { "enabled": true } }`, or an explicit `--rule`). Three arms. **Arm A — `S` and `B` both LITERALS, `B` does NOT contain `S`**: after one `replace` the guard is false, so the loop ran at most once; `Info`, `--fix` collapses the whole loop to the single unconditional assignment `x = x.replace(S, B);` — sound even for an input with zero occurrences, since `replace` returns a no-match string unchanged. **Arm B — both LITERALS, `B` CONTAINS `S`**: every replacement reinserts `S`, so the loop is INFINITE for any input that ever contains `S` — a real bug, `Warning`, report-only (there is no mechanical fix for "the author meant something else"). **Arm C — at least one of `S` / `B` is a PARAMETER of the enclosing function**: whether `B` contains `S` is the CALLER's choice (`replaceWord(line, 'a', 'aa')` loops forever, `replaceWord(line, 'a', 'b')` returns), so `Info`, report-only, with both operands' VERBATIM source in the message. That wording covers two parameters and the LITERAL `S` / PARAMETER `B` mix (`line.replace(' ', sep)`), where the literal says nothing about the caller's `sep`. **The reverse mix — a PARAMETER `S` with a LITERAL `B` — splits out of arm C, since the literal decides the verdict on its own** (arm C's phrasing produced plain nonsense on it: `stripWord(line, word)` looping on `line.replace(word, '')` was reported as "potential infinite loop when `''` contains word", though an empty literal contains no non-empty word and `replace(word, '')` REMOVES rather than reinserts). An **EMPTY literal `B`** makes the loop merely REDUNDANT — one `replace` deletes every occurrence, so a single unconditional assignment does the same work; `Info`, still REPORT-ONLY, because `indexOf('') == 0` means the ORIGINAL loop spins forever on a degenerate empty `S` while the collapsed form returns at once, and no autofix may silently trade a hang for a return (the message carries that hazard for the same reason). Being a redundancy verdict rather than a hazard one, it is the single arm no dominating guard suppresses. A **NON-EMPTY literal `B`** keeps the infinite-loop finding but states its condition exactly — the loop runs forever for precisely those `S` that occur in that literal, the equal `S == B` included, with the literal quoted verbatim — and keeps the full dominating-guard treatment (a containment guard suppresses it; an `S == B` equality guard rules out exactly one offending `S`, which its caveat clause says in those terms). The guard accepts three spellings of "does `x` contain `S`": `x.indexOf(S) != -1`, its reversed operand order `-1 != x.indexOf(S)`, and `x.contains(S)`. Each of `S` (the guard's argument AND `replace`'s first) and `B` (`replace`'s second) must be EITHER a plain string literal (`StringFoldSupport.literalOf` — no interpolation) OR a bare identifier bound to a PARAMETER of the enclosing function or LAMBDA (`RefShape.functionKinds` / `lambdaKinds` + `paramKinds` — a lambda's parameters are just as caller-chosen as a method's); a field access, a call, an index access, a LOCAL `var` and an unresolvable identifier are all left alone. The two `S` positions must be the SAME thing (equal literal content, or the same parameter binding — a literal / parameter MIX across them is not a match). The receiver `x` must be a single identifier — never `this.x` / `obj.x` — bound to the SAME declaration in the guard, the `replace` call and the assignment target (`TypeResolver.identBindingFrom`), and that declaration must carry an explicit `:String` annotation (`TypeInfoProvider.declaredTypes`); an untyped or non-`String` receiver is a silent miss, never a wrong flag. The body must be EXACTLY the one assignment (braced or bare) — any extra statement means the loop is doing more. **Arm C's one static suppression is a CONTAINMENT test that DOMINATES the loop**: `if (B.indexOf(S) != -1) return …;` / its reversed order / `if (B.contains(S)) …`, read by the same `matchGuard` and held to the same operand identity, whose then-branch unconditionally exits (`CheckScan.branchAlwaysExits` — a `return` / `throw` / `break` / `continue`, bare or as the LAST statement of a block, so `{ trace(…); return line; }` counts). Dominance is walked structurally: from the `while` UP through the enclosing statement lists (`ControlFlowSupport.blockKinds`), reading only the PRECEDING SIBLINGS at each level. Preceding in the TEXT is not enough — a guard nested inside another `if`, in an `else` arm, in a loop or `switch` body, inside a `#if` region (one `Conditional` node, never a statement list, so it protects ONE target and would silence every other), or in a nested function or lambda nothing calls, may never have run when the loop is reached. An EQUALITY guard never suppresses: `if (S == B) return …;` rules out only the degenerate `B == S` while every `B` that merely CONTAINS `S` still loops forever — it reads like protection, which is exactly why the finding survives it and the message says so. Arms A / B are untouched by either guard (with two literals the verdict is already decided). Grammar-agnostic, and the optional seams fail in OPPOSITE directions: `functionKinds` / `lambdaKinds` / `paramKinds` ENABLE arm C (unset → arm C unreachable, literals-only behaviour), while `ifStatementKinds`, `controlFlowSupport()` and `eqKind` drive the SUPPRESSION (unset → arm C reports strictly MORE; `eqKind` alone only drops the message caveat). `opaqueKinds` subtrees (macro reification) are not descended into |
| `nullable-switch-missing-null` | a `switch` over a provably-nullable subject whose branches include a catch-all wildcard (`case _:` / `default:`) but NO `case null` — a latent NPE; `Warning`, REPORT-ONLY. Haxe's `case _:` / `default:` do NOT match `null` (the compiler skips the null check for them), so a null subject falls through every arm — a SEGFAULT on hxcpp for a null enum, a silent no-match elsewhere; the author believes the wildcard covers everything, and null is the one value it does not. The fix is `case null, _:` or an explicit `case null:` arm, but the null case's BODY is the author's call (the wildcard body might be right for null, might not), so v1 does not auto-insert. Trigger (all three): the subject is provably nullable, some branch is an UNGUARDED wildcard (a `default:`, or a `case _:` with no guard), and no branch mentions `null` (`case null:` / `case null, _:` / any null-literal pattern, guarded or not, counts as handling null) — a wildcard-free switch is left to the compiler's own exhaustiveness (which forces the null case on enums). Nullability is routed through the `NullFlow` engine (the mechanism-A machinery `unguarded-nullable-deref` uses), so a subject narrowed non-null on the path (`if (x == null) return; switch x`) is a safe miss and a local bound from a nullable source is caught: a bare-identifier subject is nullable when flow does not prove it non-null AND either flow proves it `MaybeNull` (bound from a `Map` index / `Null<T>` call — source 2) or `Null`, its declared outer nominal is a `Null<…>` wrapper (`RefShape.nullableReturnMarkerTypes`, so `Dynamic` / `Any` are excluded — source 1a), or it binds to an optional `?param` (`RefShape.optionalParamKind` — source 1b); a non-identifier subject is nullable when it is itself a nullable-source expression (a `Map` index, an `Array` / `List` nullable call, a `Null<T>`-returning call — `NullableSource.describe`, source 3). A `?`-coalesced subject (`switch (x ?? d)`, `RefShape.nullCoalesceKind`) is never nullable (the `??` removes null). Grammar-agnostic over `RefShape.switchKinds` / `caseBranchKind` / `plainCasePatternKind` / `identKind` / `nullLiteralKind` / `wildcardPatternName` (any unset → no-op), plus `defaultBranchKind` / `parenKind` (guard detection) / `nullCoalesceKind` / `optionalParamKind` / `nullableReturnMarkerTypes`; needs `plugin is TypeInfoProvider`. Documented misses: a bare field / `this.f` subject, and a field-initializer switch (only function-body switches are in the flow engine's scope) |
| `collapse-nested-switch` | an outer `case P(b):` whose body is EXACTLY one `switch b { … }` on that binder — collapsible into DEEP PATTERN MATCHING: every inner `case Q_i: body_i` is spliced back as an outer `case P(Q_i): body_i`, replacing the whole outer arm. `Info`, `--fix` performs the splice. DEFAULT OFF — how deep a pattern should read is a project style call, so opt in with `"collapse-nested-switch": { "enabled": true }`. Only the SWITCHED binder's slot substitutes; every other slot of a multi-binder pattern keeps its text verbatim (`case P(a, b):` over `b` becomes `case P(a, Q):`, and `a` stays bound for the body). The ONE way the rewrite can change behaviour is a value that matched `P(...)` but no inner arm: before, it fell out of the nested switch with the outer arm CONSUMED; after, it resumes matching at the outer switch's LATER arms. The COVERAGE gate closes that from two sides. A trailing inner catch-all is normally EMITTED as an explicit `case P(_): <its body>` backstop, so the spliced run matches everything the outer arm did and nothing falls through — which is also what makes an inner GUARD safe to carry over verbatim, a failing guard falling to the next spliced arm in the same order. It is MERGED away (dropped) only when its body is EMPTY *and* falling through is provably harmless: the outer switch is in STATEMENT position (an expression switch's arms must each yield a value, so it has no empty arm to fall into and this path is never sound for one), a later outer arm exists, EVERY later arm is unguarded, EXTRACTOR-FREE, and empty-bodied (nothing new runs — no guard and no extractor, which RUNS CODE while matching, newly evaluated), and at least one of them is a catch-all (what keeps the arm list exhaustive once coverage shrinks). With NO inner catch-all at all, that same harmlessness is REQUIRED instead. A catch-all in a NON-final position is never merged — that would revive the arms it currently makes unreachable — and an emitted run that would be EMPTY is refused as dead-arm removal rather than pattern deepening. Shape gates, each ruling out a splice that is not well-formed: the arm is a case branch with EXACTLY ONE pattern holding one node (`case A(c), B(c):` offers two slots for one substitution), no outer guard, exactly ONE body statement and that statement IS a switch with a subject and at least one arm (a block-wrapped `{ switch b { … } }` is left to `unnecessary-block`, and the two compose across `--fix` passes), a subject that is a bare identifier NOT opening with an uppercase ASCII letter (a capitalised bare identifier in a pattern is a CONSTRUCTOR or constant reference, not a capture binder, and substituting a sub-pattern into a constant's slot would change what the arm matches — the same family-spelling assumption `prefer-case-guard` makes, licensed by the same opt-in: a grammar spelling patterns differently leaves the pattern seams unset and the check stays inert), BINDER ISOLATION (the name is mentioned exactly TWICE in the arm — once in the outer pattern, once as the subject; a third mention is a read the splice would STRAND or a re-binding that changes what the name means, and all three mention shapes count: an ordinary identifier, a `'$b'` string-interpolation identifier, and a `case var b:` pattern binder), no `=`-CAPTURE anywhere in the outer pattern (`case b = P(x):` / `case P(b = Q):` put the binder's name where only a plain name may stand — invisible to binder isolation, since a capture's left side is itself the pattern's one in-pattern mention, so splicing would emit text the compiler rejects), no conditional-compilation region and no reification node anywhere in the arm — nor the arm itself INSIDE a reification, since a splice in either position may carry a read of the binder that no source scan resolves (both of anyparse's OWN matches for this rule were arms written inside a `macro { … }`) — and EVERY child of the nested switch after its subject a case branch — that one test refusing an inner `default:` (no pattern to splice) and an `#if`-wrapped arm run alike — and BINDER FRESHNESS, no inner pattern binding a name the outer pattern already binds in another slot (nested, the inner binding merely shadowed the outer one; spliced, the two would land in one pattern, and Haxe rejects `case P(a, Q(a))` with `Variable a is bound multiple times`). The emission reproduces ONLY the `case` lead, the outer pattern's prefix / suffix around the binder slot, each inner pattern, separator, guard and body, and the label terminator — every one of them read out of the SOURCE, so no keyword spelling is hardcoded; everything else in the region is DISCARDED, so a comment in the `switch b {` header, in the closing brace run, in an inter-arm gap, or in the outer pattern's prefix / suffix refuses the collapse (prefix and suffix are reproduced once per spliced arm, so a comment there would be DUPLICATED, not merely moved — which also refuses the rare pattern carrying a `//` inside a string literal, a documented conservative miss). A comment INSIDE an inner body survives, the body being taken verbatim. Seams: `switchKinds`, `switchStatementKinds`, `caseBranchKind`, `defaultBranchKind`, `plainCasePatternKind`, `wildcardPatternName`, `identKind`, `parenKind`, `assignKind`, `stringInterpIdentKind`, `casePatternBinderKinds`, `casePatternExtractorKinds`, `conditionalMemberKind`, `opaqueKinds`; no resolution index and no line budget are consulted, and every gate is a structurally-provable shape invariant, so the edit is applied unverified rather than through the `RiskyFix` oracle. Idempotent: after the fix each arm's body is the former inner body, so a second pass finds nothing — unless that body is ITSELF a nested switch on its own binder, in which case the next pass collapses one more level, converging. |
| `unused-case-binder` | a switch case-pattern BINDER that neither the arm's guard nor its body ever reads — dead weight the wildcard should replace; `Warning` (the rank `unused-local` and `unused-parameter` carry), `--fix` rewrites it. `case _data:` becomes `case _:`, `case Node(x, y): use(y);` becomes `case Node(_, y): use(y);`, `case var q:` becomes `case _:`, and an unread `=`-capture head is DROPPED outright (`case q = P(v):` becomes `case P(v):`). A binder counts as unread only when the number of nodes MENTIONING its name anywhere in the whole arm — pattern, guard, body, a nested switch's own labels — equals the number of BINDER occurrences the pattern scan found, so a guard read (`case x if (x > 0):`), a `'$x'` string-interpolation read and a nested re-binding of the same name all block the fix; the count is deliberately coarse, since over-counting can only REFUSE. Or-pattern alternatives are unbound TOGETHER — Haxe requires `case A(x), B(x):` to bind the same set in every alternative, so one finding carries every occurrence's edit and `case A(_), B(_):` stays legal, while an alternative that still reads the name blocks the whole group. Binder positions are a WHITELIST (shared with `redundant-case-body` in `CasePatternScan`): an identifier, a `var x` capture, an `=`-capture's left slot, a constructor call's ARGUMENTS (never its callee), an array element, a structure field's VALUE (never its label), a parenthesised sub-pattern, and an extractor's RIGHT side — its left side is an EXPRESSION evaluated on the subject, so identifiers there are reads. Constant leaves (a dotted path, a string / numeric / boolean / null literal, a negated numeric) bind nothing; anything else makes the scan REFUSE the arm, so an unmodelled grammar shape can never read as "binds nothing". The family SPELLING assumption `prefer-case-guard` and `collapse-nested-switch` already make — a bare identifier is a capture binder unless it opens with an uppercase ASCII letter — is not universally true in Haxe: a LOWERCASE `enum` / `enum abstract` value AND any `static inline` field resolve unqualified in a pattern too (the latter both inside its own type and through a static import), so `case one:` may denote a CONSTANT — and widening a constant to `_` would change what the arm matches while still COMPILING, the one failure mode a compiler oracle could not catch. Two gates bound that, and both apply ONLY to bare identifiers (a `var x` capture and an `=`-capture head are binders by syntax): (1) NO DECLARED CONSTANT of that name — `CasePatternScan.declaredConstantNames` collects, from every file in the lint scope, each member of an exhaustiveness-checked type (`bareConstructorTypeKinds`), each member of ANY abstract or alias declaration (`aliasingDeclKinds` — which is what reaches a legacy `@:enum abstract`, projected as a plain abstract with no marker the scan can read), and each STATIC member of any type, so a single-file run refuses LESS than a project-wide one and project-wide is the intended `--fix` scope; (2) a binder that IS THE WHOLE PATTERN must sit in the LAST arm of its switch and be that arm's only pattern. Under the binder reading the arm is a catch-all and `_` is exactly equivalent — Haxe compiles a bare capture to a wildcard plus a binding, so the two decide identically for every subject, `null` included (checked on `--interp` and `-js`); under the constant reading the arm list compiles today, which for an `enum` / `enum abstract` subject means the compiler proved it EXHAUSTIVE, so at the last position `_` covers precisely the values the constant did. Both readings agree without knowing which holds — a NON-final position has no such agreement (`case one: a; case two: b;` would turn `two` into dead code) and neither does a subject the compiler does not check for exhaustiveness (a `String` switch), which is exactly what gate 1 has to carry. A NESTED bare identifier rests on the spelling assumption plus gate 1 alone: the rule's one documented residual, a constant declared OUTSIDE the lint scope. It is NOT the residual the sibling rules carry — `prefer-case-guard` excludes bare lowercase identifiers from its whitelist outright and its own residual is a hard compile error, not a silent behaviour change. An arm holding a conditional-compilation region or a macro reification is skipped outright, and the walk never descends into a reification subtree. Seam-driven via `CasePatternScan.seamsOf` (a required kind unset → no-op, report and fix alike). Idempotent: every flagged name becomes `_`, which is never a binder |
| `redundant-case-body` | a switch arm whose body its IMMEDIATE neighbour repeats, in the two shapes where the duplication can go without changing what any value matches; `Info`, `--fix` performs the fold. SUBSUME — the next arm is a CATCH-ALL (`case _:` / `default:`) with the same body, so this arm only routes its values to a body they would reach anyway, and it is DELETED (`case 'User': t(k); case _: t(k);` becomes `case _: t(k);`). MERGE — the next arm is an ordinary arm with the same body, so the two labels become one or-pattern (`case A: r(); case B: r();` becomes `case A, B: r();`). ADJACENCY is load-bearing, not convenience: folding ACROSS an intervening pattern would reroute every value that arm matches, so a non-adjacent twin is never touched — and a chain folds one neighbour at a time, three arms sharing a body merging in ONE pass because the two edits are disjoint. Gates, each a correctness one: NEITHER ARM GUARDED (a guard may reject, and both rewrites assume the label alone decides); THE EARLIER ARM BINDS NOTHING, and for a merge its partner too — two bodies can be byte-identical and MEAN different things when a pattern binds (`var x = 1; switch v { case Foo(x): use(x); case _: use(x); }` reads the binder in one arm and the OUTER `x` in the other), and the empty binding set is also what settles or-pattern legality, since Haxe requires every alternative to bind the same names; a pattern whose bindings `CasePatternScan` cannot fully model counts AS binding, so an unmodelled shape refuses rather than passes; NO EXTRACTOR in either pattern (it RUNS code while matching, so deleting the arm would drop that call); NO `null` LITERAL in either pattern — whether `case _` matches `null` is exactly the question `nullable-switch-missing-null` exists for and the answer is target-dependent, so refusing the literal outright keeps this rule out of that argument and a `case null:` arm is never subsumed by a wildcard; IDENTICAL BODIES proved TWICE — structural equality of the statement lists (`RefactorSupport.structurallyEqual`, which compares literal CONTENT, so `f("a  b")` and `f("a b")` stay distinct) AND whitespace-normalised source equality (`CheckScan.normalizeSpan`, which a comment inside either region makes differ), neither test alone sufficing since shape equality is blind to an interior comment and normalised source collapses whitespace inside string literals; NO COMMENT in the region the edit DISTURBS — for a subsume that is everything from the END of the previous arm to the START of the next one, wider than the deleted arm's own span on BOTH sides and deliberately so (a comment before the arm would survive to document the CATCH-ALL instead, and a trailing `// …` is TRIVIA the span excludes, which the writer re-attaches to whatever node is left; both were caught folding anyparse's own tree), and for a merge it is the earlier arm's terminator and body plus the later arm's `case` lead; and NO CONDITIONAL-COMPILATION REGION or macro reification in either arm. Exhaustiveness is never lost: a subsume deletes an arm only when a catch-all IMMEDIATELY follows it, so the arm list already covered everything and still does, and a merge preserves the matched set exactly — which is why the check needs no compiler oracle. Candidates whose edits OVERLAP (a merge and a subsume claiming one middle arm) are reduced to the document-earliest in `fix`, the loser firing cleanly on the next fixed-point pass. Composes with `unused-case-binder` across passes: unbinding a trailing catch-all's dead binder is what turns the arm above it into a redundant twin |
| `empty-case-arm` | the TRAILING arm of a STATEMENT `switch` whose body is EMPTY, when the switch has no catch-all and its subject's type carries no exhaustiveness obligation — dead syntax the compiler already ignores; `Info`, `--fix` DELETES the arm together with its line. Such an arm matches values and does nothing, and being LAST it has nothing below it, so the values it names have nowhere else to go: in a statement switch an unmatched value already no-ops, which makes the deletion behaviour-identical. The arm may itself be an empty `case _:` or an empty `default:` — it is then the lone catch-all, and a catch-all that does nothing is the same no-op. Gates, each a correctness one: THE ARM IS LAST — a mid-switch empty arm is the OPPOSITE of dead, it deliberately SUPPRESSES the arm or catch-all below it for the values it names (`case 2:` above `case _: warn();` is how a value opts out of the warning), so deleting one reroutes everything it matched; TWO ARMS AT LEAST — deleting the sole arm leaves a degenerate `switch x {}` rather than a smaller switch; THE ARM IS EMPTY, UNGUARDED and BINDS NOTHING — a guard RUNS, so an arm carrying one is not silent even with an empty body, and bindings are asked through `CasePatternScan`, whose null answer ("this label may bind something I cannot see") refuses rather than passes; NO EXTRACTOR and NO `null` LITERAL in the pattern (`CasePatternScan.patternsModellable`, shared with `redundant-case-body`) — an extractor RUNS code while matching, and whether a switch reaches a `case null:` arm is the target-dependent question `nullable-switch-missing-null` exists for, so refusing the literal keeps this rule out of that argument; NO CONDITIONAL-COMPILATION REGION AMONG THE ARMS — an `#if` there projects as a SIBLING of the arms holding arms the scan cannot enumerate, and it defeats the two gates around it at once, since a catch-all may hide inside it and the arms OUTSIDE it may be the only ones a given build keeps (deleting the candidate could leave `switch x { #if … #end }`), while a macro reification inside the candidate arm is refused for the neighbouring reason; NO OTHER CATCH-ALL in the switch — with a `default:` / `case _:` above it the trailing arm is UNREACHABLE rather than merely silent, and what an author meant by an unreachable label is not a question a deletion should answer (the CANDIDATE itself may of course be that wildcard, and a whole-pattern capture binder or a pattern shape the scan cannot model counts as a catch-all too, since reading an arm as one only ever REFUSES); and NO COMMENT in the region the deletion disturbs — from the NEWLINE ENDING the previous arm's line to the switch's closing brace, wider than the arm's own span on BOTH sides because a comment ABOVE the arm survives the deletion and then documents whatever follows it, while a trailing `// …` on the ARM's own line is TRIVIA the span excludes that the writer re-attaches to a node it never described (the lower bound stops at that newline rather than at the previous arm's last token, since a `// …` on THAT line lies behind everything the deletion can reach). EXHAUSTIVENESS IS THE WHOLE PROOF: a Haxe `switch` over an `enum`, an `enum abstract` or `Bool` is exhaustiveness-checked — its arm list is part of the compile, and dropping an arm there can turn a compiling switch into a non-compiling one — so the subject's type is resolved (`RefactorSupport.valueTypeNominal`, through the symbol index, so a `d.code` / `S.mode` path resolves too) and matched against the `openSwitchSubjectTypes` WHITELIST, `Int` / `UInt` / `Float` / `Single` / `String` for Haxe, the types no arm list is ever checked against; an unresolved subject reads as null and REFUSES, which is what lets the check need no compiler oracle. Being a STATEMENT switch is the other half of that proof and a SEPARATE gate (`switchStatementKinds`): an EXPRESSION switch has to yield a value from every arm, so its arm list is checked whatever the subject is. Seam-driven via `CasePatternScan.seamsOf` plus those two `RefShape` lists (a required one unset → no-op, report and fix alike). `fix` re-derives its candidates from the re-parsed tree, so a reported span that no longer names a deletable arm produces no edit, and a subject resolved through ANOTHER file re-derives only when the caller supplies the report-scoped index (`Cli.computeFileLintEdits` does; a direct call falls back to an index over the one file). A RUN of empty trailing arms is peeled one per pass — after a deletion the arm above becomes the trailing one — until the two-arm gate stops the last one standing |
| `unnecessary-switch` | a `switch` whose ONLY arm is an unconditional catch-all — it decides nothing; `Info`, `--fix` puts the arm's body in its place. The TERMINATOR of the case-arm cascade: `unused-case-binder`, `redundant-case-body` and `empty-case-arm` each reduce a switch toward one catch-all and each stops there by design (`empty-case-arm`'s two-arm gate says so outright), so the husk they leave had no owner. A hand-written one is rare; one PRODUCED by a `--fix` pass is not. Flagged: a `switchKinds` node with exactly TWO children — subject and one arm — whose arm `CasePatternScan.isCatchAll` accepts (a `default:`, or one unguarded `_`; a guard RUNS and may reject). A BINDER instead of the wildcard is refused — unwrapping one means introducing `final q = subject;`, a different rewrite, and `shadowing-case-binder` has something to say about that shape first. The two-child rule carries the conditional-compilation gate for free: an `#if` among the arms projects as a SIBLING of them. The SUBJECT must be DROPPABLE, which is not the same as hoistable: purity comes from `PurityScan` (a field read passes only when the member is provably not a property getter — including the UNQUALIFIED spelling, where a bare `p` is a `get_p()` call that `isSafeKind` would wave through), plus a collection LITERAL, admitted HERE and deliberately not in `PurityScan` — dropping an allocation is unobservable, hoisting one merges two distinct references into one, so the safe direction differs per caller. The STATEMENT fix splices a BLOCK, never bare statements: braces are self-TERMINATING, legal as a brace-less body (`if (c) switch x { case _: a(); b(); }` must not leave only `a()` guarded), and scope-preserving (an arm body declaring a local must not widen it) — `unnecessary-block` then unwraps the block exactly where that is provable, two rules each proving their own half. An EMPTY arm body is deleted outright, and only from a statement-list position, since deleting the body of `if (c)` would leave the `if` with none. The EXPRESSION fix splices the arm's single VALUE (not its statement, so no terminator rides along) and OWES one when the switch's closing brace was itself the enclosing statement's terminator: `return switch x { case _: 42; }` is legal Haxe and `return 42` is `Missing ;` — measured by the gap between the switch's end and its statement's end being blank (a statement span absorbs trailing trivia, so an exact offset match answers false for every real case). A multi-statement expression arm is refused. NO COMMENT in the disturbed region — between the `switch` keyword and the body, or between the body and the closing brace; one INSIDE the body travels with it. One behaviour deliberately changes: on hxcpp a `switch` over a null enum crashes, so deleting one removes a latent crash — whether a wildcard is reached for `null` at all stays `nullable-switch-missing-null`'s question. Seam-driven via `CasePatternScan.seamsOf` plus `switchStatementKinds` / `exprStatementKind`; `fix` re-derives from the re-parsed tree, and after it there is no switch left at that position |
| `shadowing-case-binder` | a bare case-pattern BINDER that IS a whole pattern, takes a name already declared in scope, and never reads it; `Warning`, REPORT-ONLY. Haxe resolves a bare lowercase identifier in a pattern as a CAPTURE — it does not compare the subject against a same-named binding, and it does not warn that it declined to: `case closeAction:` over a field of that name matches EVERY value and compiles clean (verified on `--interp` across three distinct subjects). Only an enum / enum-abstract value or a `static inline` field resolves as a pattern CONSTANT, never an ordinary field, so the author who wrote the name of something in scope almost always meant a comparison and got a silent catch-all that shadows it. No fix is offered: which of an `==` test, a case guard (`case v if (v == closeAction):`) or a genuine capture under a free name was meant is the author's call. Flagged: a BARE binder (`CasePatternScan.binders` with `bare` — a `var x` capture and an `=`-capture head are binders by SYNTAX, so no reader could have meant a comparison there) that is the WHOLE pattern, not in `declaredConstantNames`, never READ in the arm (the same mention count `unused-case-binder` uses — a binder that is read is a deliberate capture, whatever it shadows), whose name `CasePatternScan.shadowedDeclaration` resolves to a visible declaration by walking the ancestor chain OUTWARD: a function-like ancestor is asked for a parameter and then for a binding declared BEFORE the arm (locals, loop and catch binders, local functions — one declared after is not in scope there), a type ancestor for a member and, through the symbol index, for an INHERITED one. WHOLE-pattern is the precondition the real tree taught: at the top level a bare name is the arm's entire decision, so the comparison misreading is dominant and the arm silently becomes a catch-all, but NESTED in a constructor pattern (`case TArray(e, …)`) a capture is the ordinary reading and the shadowing is incidental — heaps' `HlslOut` carries exactly that, copy-pasted from the arm above, where the right advice is `unused-case-binder`'s "spell it `_`"; reporting there would have been a wrong diagnosis AND would have suppressed a correct fix. Arms holding a conditional-compilation region or a macro reification are skipped. This rule is why `unused-case-binder` keeps its fix honest: that rule now asks the SAME predicate and REFUSES to spell such a binder `_`, a rewrite that preserves behaviour while erasing the only surviving evidence of the mistake — after which `unnecessary-switch` would be entitled to delete the husk and the intent would be gone from the file |
| `duplicate-code` | a run of three or more consecutive statements that appears, identical up to whitespace, in two or more places within the SAME file — a copy-paste clone the user's rule says to always extract ("duplication is a bug, not a design choice"); `Info`, REPORT-ONLY (extraction is a refactoring — `hxq extract-method` — not a mechanical span edit). Purely structural (no type information). v1 scope, deliberate: IN-FILE only (cross-file clone detection is a future axis needing a project-wide index and noise control); WHITESPACE-ONLY normalization (statements compare by source text with every run of spaces / tabs / newlines collapsed to a single space and the ends trimmed — NO identifier / alpha-renaming normalization, so only exact-logic clones match and the check has zero false positives by construction; a comment inside a statement span makes it textually different — not a clone; a comment between statements is trivia and does not affect equality). A run is a maximal sequence of direct-child statements of a `ControlFlowSupport.blockKinds()` node (function body / nested block / block expression — the same seam `dead-code` uses); the two occurrences may sit in different blocks and different block kinds (a method body vs an `if` body). A five-statement clone is reported ONCE as its maximal run, not as three overlapping three-statement windows (span-containment drops the sub-windows). Occurrences of one clone family are NON-OVERLAPPING — the document-earliest is the original, each later non-overlapping occurrence is a finding, so three occurrences produce two findings and a self-repeat like `a; a; a; a` (whose only second window overlaps the first) yields none. Content gate: a run must total at least three statements AND at least 40 non-whitespace characters, so a triple of trivial one-liners (`i++; j++; k++;`) is not flagged. Runs entirely inside an `opaqueKinds` (macro reification) subtree are skipped. The finding is spanned from the first to the last statement of the duplicated run at the later occurrence, and the message points at the first occurrence's line. Grammar-agnostic: a grammar with no statement / block concept (a binary format) returns null from `controlFlowSupport` and the check is a no-op |
| `tail-merge` | a branch of an `if` chain whose TRAILING statements repeat the ENTIRE run that follows the chain — a duplicated tail the branch can simply drop, letting control fall through onto the shared copy (`if (c) { work(); helper(v); return v; }` followed by `helper(v); return v;` becomes `if (c) { work(); }` followed by the same shared pair); `Info`, `--fix` deletes the branch's trailing run together with the trivia in front of it (`[previousStatement.to, lastTail.to)`), leaving the branch to fall out. Purely structural (no type information). The walk threads a fall-through descriptor: a block hands each statement the run that follows it and its LAST statement the run it inherited, and an `if` hands the same run to every branch, an `else if` chain included (all arms complete into the same place) — while EVERY other node (a loop, a `switch`, a `try`, a function declaration, an expression) resets the run to empty for its children, and a macro-reification subtree (`opaqueKinds`) is not walked at all (its statements may be spliced into a scope the walk never sees). That reset is the correctness mechanism, not a special case: it is why a chain at the end of a LOOP body is never flagged against the code after the loop (the body iterates rather than completing into it) and why a `switch` CASE body is out of scope entirely (its continuation is the `switch`'s). Gates, all required: TERMINAL — the fall-through run ends in a `return` or a `throw` (`RefShape.valueReturnKinds` + `throwKinds` + `voidReturnKind`), with `break` / `continue` DELIBERATELY excluded (hence the explicit kind set rather than `controlExitKinds`: a loop-exit tail is not worth the extra reasoning, and leaving it out costs only a missed finding); IDENTITY — the branch's trailing statements and the fall-through run are token-identical under `sameStatement`, which requires BOTH `RefactorSupport.structurallyEqual` (shared with the pattern matcher) AND whitespace-normalized source equality (`CheckScan.normalizeSpan`, shared with `duplicate-code`) — neither alone suffices, since shape equality is blind to a comment sitting INSIDE a statement span while normalized source collapses whitespace inside string literals and would equate `f("a  b")` with `f("a b")`; FULL COVERAGE — the matched suffix must cover the WHOLE fall-through run, so a branch ending `r(); return v;` against a shared `q(); r(); return v;` is a safe miss (falling through would additionally run `q()`); NON-EMPTY REMAINDER — at least one statement survives in the branch, since a branch emptied by the edit wants deleting (possibly with an inverted condition), which this rule does not do; NO CONDITIONAL COMPILATION — neither the removed region nor the kept run may contain `#if` / `#elseif` / `#else` / `#end` (a plain text scan, so a directive spelled inside a string literal refuses too — conservative in the safe direction); NO SHADOWING — no name bound between the fall-through run's scope and the branch (the branch's own local variables, in statement or expression form and through any metadata wrapper via `RefactorSupport.topLevelDeclaredNode`, its local functions plain and `inline`, plus any block scope entered on the way) may be referenced as an identifier (`identKind`, plus `stringInterpIdentKind` when set) anywhere in the removed tail, because `var t = c(); helper(t); return t;` inside a branch is token-identical to an outer `helper(t); return t;` and yet means something else, and a local `function helper()` hides an outer `helper` the same way — a HARD gate, no finding at all, since the duplication is only apparent. A declaration whose bound names the projection cannot enumerate refuses outright rather than being scanned: a variable declaration's children are its INITIALIZERS, one per initialized declarator, so a multi-declarator is detected by a second child (`var a = 1, t = 2;`) or by a declarator-separating comma in the text outside the initializers (`var a, b;`, `var a:Int, b = 1;`, `var a = 1, b;`) — a comma at bracket depth zero, so a generic type parameter list (`var m:Map<String, Int> = f(a, b);`) does not count. The tail's OWN declarations are exempt — the shared copy declares the same names, so falling through re-binds them identically. Two further COMMENT gates apply to the FIX only (the finding is still reported, it just yields no edit): every comment in the removed region must also stand before the kept run (a duplicated explanation goes with its duplicated code, a unique one would be lost), and no comment may sit between the last removed statement and the branch's closing brace (a trailing `// …` there would be stranded). Grammar-agnostic over `RefShape.ifStatementKinds`, `RefShape.localDeclKinds`, `ControlFlowSupport.blockKinds` and the terminal-kind seams (`valueReturnKinds` / `throwKinds` / `voidReturnKind`) — any one of those unset → no-op, `localDeclKinds` included, since it is what the shadowing gate proves absence against and an unset seam would silently switch that gate off — plus `identKind` / `stringInterpIdentKind`, `localDeclExprKinds` / `localFunctionKinds` / `inlineFunctionKinds` and `MetaShape.metaKinds` for the shadowing gate and `opaqueKinds` for the reification skip; nested findings pass through `RefactorSupport.dropContainedEdits` so no two deletions overlap |

### Grammar platform

A `@:build` macro reads a grammar (Haxe types annotated with `@:lit`, `@:kw`,
`@:infix`, `@:prefix`, `@:postfix`, …) plus a `@:schema(Format)` and emits:

- **`buildParser`** — a specialized parser, in either **Fast** mode (bare types, throw
  on error, maximum throughput) or **Tolerant** mode (spans, error recovery, IDE-class
  use). Two parsers from one grammar, chosen at the call site.
- **`buildWriter`** — a writer/pretty-printer driving a Wadler-style Doc IR. **One AST,
  one writer** — no "emit text, then re-parse and reformat" two-pass pipeline.
- **`buildTransform`** — a deep, bottom-up whole-tree rewrite with per-node-type hooks
  (the multi-type generalization of `ExprTools.map`).

Parsing strategies are plugins (`Pratt`, `Kw`, `Lit`, `Prefix`/`Postfix`/`Ternary`,
`Re`, `Skip`, and `Bin` for binary formats). Grammars shipped today:

- **haxe** — the full language grammar (all five top-level decls, members, control flow,
  31 binary + prefix/postfix operators via Pratt, string interpolation, lambdas,
  literals) — parser + writer + the query plugin behind `hxq`.
- **json** — a text format: parser + writer.
- **ar** — a binary archive (Unix `ar`): byte-perfect parser + writer through the same
  pipeline.
- **sexpr** — S-expression writer, used for the `ast` dump output.

## Design goals

The project deliberately started from a universal-parser framing: be ready for the next
format or language rather than rewrite the same parser machinery each time. The points
below are the design direction — the Status and Features sections above mark what is
delivered today.

- **Format-agnostic** — grammars for any format (JSON, XML, YAML, binary, custom)
  described declaratively as Haxe types with metadata. *Today: JSON, Haxe and the `ar`
  binary format.*
- **Language-agnostic** — programming languages handled by the same engine through
  Pratt, keyword/literal and (planned) indent-sensitive strategies.
- **Plugin architecture** — grammars and format descriptions live in their own packages;
  adding a language or format is a new package, not a core change.
- **Cross-family ready** — common AST types for language families (curly-brace, Lisp, ML)
  are themselves plugins, with a structural round-trip between families as an
  architectural contract. *Not yet implemented — see
  [`docs/cross-family-contract.md`](docs/cross-family-contract.md).*
- **Performance** — generated parsers and writers are specialized per type at compile
  time, targeting hand-written speed.

The **Fast/Tolerant** two-mode build and the **one-AST-one-writer** guarantee (described
under *Grammar platform* above) are part of this direction and already in place.

## Non-goals (by design)

- **Not an automatic cross-language translator.** Deep semantic translation between
  unlike languages (Python ↔ Rust) is out of scope; the platform provides infrastructure
  for *user-written* transforms, not magic.
- **Not a native code generator.** Integrate with LLVM/WASM if binary emission is needed.
- **Not a live-background incremental parser.** On-demand reparse with caching is in
  scope; continuous tree-sitter-style incrementality is not.

## Running the tests

```sh
haxe test.hxml          # neko (fast compile, fast run, default)
haxe test-js.hxml       # js/node — then: node bin/test.js
haxe test-interp.hxml   # Haxe macro interpreter (no compile step)
```

`test.hxml` and `test-interp.hxml` are self-contained: no compile-server
dependency, so they build correctly whether or not a server happens to be
running. `test-server.hxml` / `test-interp-server.hxml` are opt-in
`--connect 7822` variants for a faster edit loop with a dedicated compile
server (`haxe --wait 7822`, once) — only use them when you know a
compatible server (started from this checkout) is listening; a stray or
mismatched one on that port answers `--connect` without error and skips
the build silently (exit 0, no artifact, no output).

The corpus round-trip layer runs only when `ANYPARSE_HXFORMAT_FORK` points at a
haxe-formatter fixtures checkout.

## Documentation

Deeper reference lives in [`docs/`](docs/): `architecture.md`, `design-principles.md`,
`roadmap.md`, `strategies.md`, `formats.md`, `cli-query-tool.md`, and `testing.md`.

## License

MIT. See `LICENSE`.
