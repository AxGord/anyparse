# Dedicated CLI compile server (separate from vshaxe). Start once:
#   haxe --wait 7822
# Caches macro-expanded modules between runs. Restart the server if a
# stale synthesized-type error appears.
--connect 7822
-cp src
-cp test
-lib utest
-main RunTests
-js bin/test.js
--cmd node bin/test.js
